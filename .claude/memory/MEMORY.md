# Memory Index — ACMP

> Compacted 2026-08-15 (4th). One line per memory; detail lives in the topic files and in the
> Tamheed package (`prm-next.md` is the kickoff). Read the linked file before acting.

## ★ Tamheed 4.4.1 · `acs-met` PASSES · the build queue is DONE · the deploy waits on an OPERATOR RUN

> **START HERE: `tamheed-package/prompts/prm-next.md`** — the durable kickoff, fully self-contained
> (edit it, never add another `prm-*.md`). `handoff/RESUME.md` is now historical detail.

- ★★ [**#282 — `DEF-056`'s "measured blocker" WAS NOT REAL**](an-absence-needs-a-proven-instrument.md) — the rows were written all along (the failing run's own log printed them); the helper read `AuditEvent.Action`, **NULL on the v1 rows every refusal writes**. ⚠ Its two `NotContain` **controls passed VACUOUSLY**. The "unresolvable" `IAuthorizationMiddlewareResultHandler` was a **missing `using`**. **An absence is only evidence if the instrument is proven present.** ⚠ **`AV-159` was wrong too** — `AC-003`'s AuthEvent already existed as `Authentication.NoRoleClaim` (`PE-359` corrects it).
- ★ [**#275 reconciliation + #276/#277 voting eligibility**](reconciliation-and-voting-eligibility.md) — ⚠⚠ **`DEF-065`+`DEF-038` STAY OPEN: the deploy is blocked on an OPERATOR RUN, not code.** 5 lessons incl. **JSDOM does not render — LOOK at visual changes** and `DEF-072` (`gate_run()` AFTER writing).
- ★★ [**"always you must interview me" — NEVER BANK THE OPERATOR'S ANSWERS**](ask-every-time-never-bank-answers.md) — an answer given in one context is **evidence about the past, never consent for the present**. Banking one turns a **decision** into a **default**, and the damage is in the RECORD, not the build. Now **`LL-002`, Approved + PINNED** (2026-08-16) — it binds every session via the note, beside `LL-001`.
- ⚠⚠ [**TAMHEED 4.4.0 — `lesson` (`LL-`) + `skill` (`SKL-`)**](tamheed-v4-and-liveness.md) — `package_migrate` registry-sync FIRST on each upgrade. ⚠ **`handoff_emit` must target `tamheed-package`** (4.4.0 fixed the root-aimed misfire; verified by reproduction). Approving/promoting a lesson now **refuses without `operator_confirm: true`**, demands `confirmed_by` on that write, and **rejects content drift** — byte-identical or supersede. Findings: `findings_19`, `findings_20`.
- ⚠⚠ [**THE STORE WAS MIGRATED v3 → v4**](tamheed-v4-and-liveness.md) — `status` → **`lifecycle_status`** on defects/DW/OQs; `entity_upsert` needs **FULL rows** over **truncated** data (read the JSONL, not the query); `WVR-` waivers are operator-only; typed progress has a **`correction` event**. Root `CLAUDE.md` now **imports** the tool-owned note. Sweep + traps: `findings_17.md`.
- ⚠⚠ **TWO TRAPS ABOUT TRUSTING YOUR OWN INSTRUMENTS** (`findings_18.md`): **(1)** a generated payload must be **PASTED, not re-typed** — now **`LL-001`, Approved + pinned**, so it binds every session via the note, and 4.4.0 **enforces it mechanically**. **(2) A HOLLOW `pass` is worse than an `indeterminate`.**
- **Gates 7/7 · `readiness_check` ready:false** — ✅ **`acs-met` now PASSES** (`AC-041` built; `AC-048`→`AC-094` narrowed; `AC-101`→`AC-108` Met; `AC-084` retired). Blocking left: **`defects-closed` (`DEF-065`, `DEF-078` — both wait on an OPERATOR RUN, not code)** + `risks-discharged` (13). Correct, not a bug. ⭐ **QUEUE: the build track is DONE** — `DW-026` merged (#284), `DEF-067` merged (#285), `DEF-012` dispositioned. **Left = `DEF-039` (the S3/MinIO probe, and it needs `/readyz` measured first), `DEF-038`, `DEF-077`, the 13-risk review, and the two operator runs.** Six interview rounds on `DEC-046`–`DEC-055`; nothing carried in anyone's head. Deploy = `deploy/RECONCILE-RUNBOOK.md` + `scripts/verify-reconcile.mjs`. ⚠ Read tallies live — this line has gone stale three times.
- ★★★ [**`DEF-078` CONFIRMED: prod `/readyz` = 503, and TWO blind controls hid it**](a-green-control-can-be-blind.md) — measured via SSM under delegation. ⚠ **The command the defect row prescribed could not measure anything**: `/api/readyz` is **404 by construction** (nginx proxies `/api/` un-stripped; health is mapped at the API root) and bare `/readyz` returns **200 serving `index.html`** via the SPA fallback — *a status-only check passes with the API stone dead*. I nearly reported "refuted" from it. ⚠⚠ **`DEF-079`**: the container said `Up 3 days (healthy)` because its healthcheck probes `/healthz`, mapped `Predicate = _ => false` — **evaluates ZERO checks**. ⚠⚠ **`DEF-081`**: gitleaks IS gating over full history and passed 153 commits because `.gitleaks.toml:13` allowlists **`.*\.md`** — every markdown file, by path. **I claimed "no gate exists"; that was false and checking it produced the better finding.** Fix = `BucketExistsAsync` (PR #286).
- ⚠ **A new `.cs` file written by a tool has NO BOM and fails CI's format gate** — `.editorconfig:12` demands `utf-8-bom`; the error is `CHARSET: Fix file encoding`, which never says "BOM". Builds and tests pass locally first. Verify with `dotnet format --verify-no-changes` (exit code), not by eye.
- ★★ **`DEF-012` CLOSED WON'T-FIX (`DEC-055`) — and the rule that would have "fixed" it was UNSOUND.** The operator had overruled the close at `DEC-054 d4`; shown the new facts they said *"do it by your self"*, so the reversal is delegated, not quiet. The only mechanical per-item rule — *a leaf closes when every requirement it names is Implemented* — **closes `WBS-20.4`, the EMAIL ADAPTER**, on the Implemented `FR-130`, while *no email in v1* is a hard constraint. **No filter repairs that**, so **zero** of the 155 `wbs_items` were written (verified two ways: row count + `wbs_items.csv` unchanged). ⚠ **THE REAL CAUSE, and it re-frames every status in the package:** of 222 requirements, **exactly the 60 with an AC are Implemented and exactly the 162 without one are Approved** — because a requirement advances ONLY via the AC auto-advance trigger. **So requirement status measures whether anyone WROTE an AC, not whether it was built**, and `v_backlog` reports that faithfully. Closing the WBS rows would have made the view read clean while **hiding** the gap the `execution_state_note` discloses. Remediation carried as **`DW-029`** (162 requirements, conditional trigger).
- ⚠ **`DEF-067` sat Open for a day with its fix already on `main`** — `progress_update` was done (`PE-383`), `entity_upsert` on the row was **missed**. Half a two-part obligation is drift. **No gate objected** (`defects-minor` is advisory — it *listed* the row every run). Caught only as a side effect of a re-derivation script printing open defects. ⚠ **And the row's own prescription was WRONG**: it forbade a timeout; `findBy`/`waitFor` were tried FIRST and failed, and the real cause was **duration** (5090ms vs a 5000ms ceiling), fixed class-wide in `vitest.config.ts`. **A defect row's predicted cause is a hypothesis, not evidence.**
- ⚠ **Ancestry is the wrong test for "is this branch's work already shipped"** — `git branch -d` refuses a branch whose commits are unreachable even when its TREE is identical (squash-merge signature). Use the **three-dot tree diff**. Branch audit 2026-08-15: only `scaffold/ph0-p1-foundation` was empty (deleted); the other six hold unique commits.
- **Prod runs `65e45d4` and is UNUSED** (0 topics, 1 of 26 members ever signed in). **Nothing in this stream is deployed**; `e9b2155` (idle sign-out) published but NOT deployed, by `DEC-044`.
- ⚠ **Read `ADR-0043`, NOT `ADR-0042`** — 0042 is Superseded: its context wrongly claimed Guest is stream-bounded (E.1 scopes Member/Reviewer/Submitter only; E.3 bounds a guest by a TIME WINDOW). All 7 decision clauses carried over verbatim.
- **ADR-0043 steps 1-8 all shipped** — PRs #266/#267/#268/#269/#270; `DEF-057` closed (stream scope RUNS) and `AC-010` MET. **The slice is DONE.** Per-PR detail in the package.
- ⚠⚠ **`DEF-066`: stream assignment had NEVER worked on a real DB** — `member_streams.StreamId` was an IDENTITY column, so steps 3+4 were non-functional as shipped. Four suites green over it. See [[inmemory-provider-hides-db-refusals]].
- ⚠⚠ **`DEF-068`** — the landmine was `PermissionMatrixTests`: it evaluates `A` cells with NO resource, and a 2-param handler is never invoked without one, so `TopicEdit` failed for the **Chairman** too. Fixed by asserting the real property — **a stream-scoped policy is RESOURCE-ONLY**.
- ⚠ **`AC-010` is reachable through `PrepareTopic` and nowhere else** — `UpdateTopic` pre-Accept skips authz for the submitter and fails a non-owner on the CAPABILITY check before streams; post-Accept it uses `TopicTriage`. `PrepareTopic` = `TopicEdit` on an Accepted topic where grant-on-accept satisfies the capability, so stream scope alone decides.
- ✅ **The wildcard IS read now** (`DW-027`) — `UserStreamProvider` returns codes + `IsWildcard` from ONE query; a flag from a column, never a code match. ⚠ **CORRECTED 2026-08-16: `DW-027` is `Done`**, closed by `DEC-048 d1` — this index called it "stale-Open" and that was wrong. `DW-026` (the *architecture-test* guard) has fired a **5th** time and is **Activated**.
- ⚠ **`DEF-069`: `check-coverage.mjs` merges EVERY `TestResults` dir** and `dotnet test --collect` writes a new one per run — so gating locally twice scores an edited file against its OLD self. It indicted a file after its code was moved out. **`rm -rf tests/*/TestResults` before trusting a local coverage run.** CI is unaffected (fresh checkout).
- ⚠ **The LSP diagnostics panel can be stale** — it reported a duplicate migration and a missing test helper; neither was real (`git status` clean, helper at `AcmpWebApplicationFactory.cs:174`). Same shape as "a measurement that indicts known-good code is measuring itself".
- **`DEC-046`/`DEC-047` items: ALL DONE** — both branches merged, `DEF-069`+`DEF-070` gate guards, `DEF-061`, `DEF-055`, **`DEF-065` reconciliation code (#275)**, **`DEF-041` (#276)**. **Left: `DEF-039`** — the S3/MinIO health tile, which needs an environment-aware **PROBE**, not a config flag.
- ⚠ **`DEF-064`/`DEF-070` (mine, fixed): never write Arabic as unicode escapes materialised with `.encode().decode("unicode_escape")`** — it reads UTF-8 as Latin-1 and destroys it while leaving the ENGLISH half perfect, so nothing looks wrong; 7 values shipped broken. **`check-i18n.mjs` compares KEY SETS, not values**, so "parity OK" is true and meaningless — it now also fails on the signature in a VALUE. ⚠ The same escape trap breaks Python heredocs — write literal UTF-8.
- **Stale branches** (all pre-date `4c1b356`, so they carry `DEF-064`'s broken `ar.json` — guarded, since merging one now fails `check-i18n` loudly): `chore/design-update-round2`, `chore/docs-v8-local-design`, `feat/budget-notification-observer`, `feat/p13-webex-integration`, `docs/defer-p14-tarseem`, `feat/audit-adr`. Inventory + audit notes in `prm-next.md`.
- **Tamheed acceptance series `findings_13`–`findings_16` complete except §6:** the CLAUDE.md obligations note demonstrably TRANSFERS to fresh contexts; whether a fresh session DISCHARGES it is unproven. Only an **interactive** fresh session can close it.

## Shipped, reference only (detail in the package / RESUME §3)

- **ADR-0038 in-app user management** — #234/#235/#236/#237/#238/#246. `FR-156/157/158` done.
- **ADR-0039 `AC-090`** (#239) — was **unsatisfiable by construction**: authorization is token-driven, so a removed role survived a full access-token lifetime (**300s, measured**). Now per-request revalidation in **middleware**, fail-closed. ⚠ **An unknown subject must be ALLOWED** (ADR-0004 provisions JIT, so failing closed refuses every first login). Seam = `IPrincipalRevalidator` → `PrincipalVerdict`; window column = `AccessExpiresAt`. ⚠ `RolesChangedAt` / `GuestAccess.Grace` are **not real symbols**.
- **ADR-0040 `FR-159` guest presenters** (#241/#242, `DEC-040`) — window = `ScheduledEnd + 24h`, widened by the operator so no grace 401s a presenter mid-presentation. Invite = a **Meetings** use case over ONE Membership port. **`ADR-0021` already fixed that pattern — read it before any cross-module seam.**
- **`DEF-052`: there was NO read-side role gate anywhere** — every named policy is a WRITE capability. Fixed by `GuestSurfaceMiddleware`, **deny-by-default rather than a policy per group**, because an opt-in list silently exempts every route added later.
- **Guest-expiry sweep** (#240) hourly. ⚠ Predicate is `AccessExpiresAt != null && < now`, so **an invited member (role `Guest`, null window) is NOT swept**.
- **`SC-005`: an ADR named a seam its own tests could not reach** (`OnTokenValidated`; the API harness uses a synthetic scheme). **Check the harness can reach a named seam before approving the ADR.** Still true in 4.3.0 — it is why `AC-003` needed a live leg.
- **`DW-025`'s premise was FALSE and I wrote it: ACMP HAS NO RESCHEDULE.** If one is built it MUST call `IGuestWindowWriter`.
- **`DEC-041`'s four items** — #243 · #244 · #245 · #246. **Two were built against my recommendation** — read `DEC-041` before re-opening either.

## Standing rules & gotchas (read before editing)

- [★ Read the implementation before calling it a defect](read-before-calling-it-a-defect.md) — **nine** instances; never caught by a gate. It has made defects smaller, made one **disappear**, and this session **killed one in three minutes** (the sweep-vs-invitee scare). **Read the predicate, not the doc comment describing it.**
- [★ The InMemory provider hides DB refusals](inmemory-provider-hides-db-refusals.md) — **`DEF-066`: stream assignment had NEVER worked on SQL Server**, and ADR-0043 steps 3+4 shipped on top of it with four suites green. Ask "has this write ever run against SQL Server?" Also: EF **cannot** scaffold an identity removal; `ef migrations add` rewrites the snapshot as CRLF; semgrep taints a method **parameter** reaching `CommandText`.
- [★ Controls must DETECT **and** TELL](controls-must-detect-and-tell.md) — now **nine** (DEF-023/030/031/032, OQ-068, DEF-051, DEF-054, + both DEF-066/step-5 migration guards, which are the first ones actually **mutation-tested** rather than asserted in a comment). The "tell" half is normally the untested half.
- [★ Verify mechanically, not carefully](verify-mechanically-not-carefully.md) — `entity_upsert` replaces FULL rows; the JSONL flushes on EVERY write, so git HEAD is a live baseline. ⚠ **Also now: a measurement that indicts known-good code is measuring itself** (`grep -c $'\r'` degraded to `grep -c ''`), and `/acmp/*` env params are **LF**, not CRLF.
- ⚠ **PowerShell joins arrays with SPACES.** `aws ... --output text` returns an **array of lines**; `[IO.File]::WriteAllText(path,$array)` writes one space-joined line and would have **destroyed the SSM env file**. Use `($v -join "`n")` and verify the line count before publishing.
- ⚠ **`gh pr create --body` and `git commit -m` with backticks/quotes break under PowerShell** — always `--body-file` / `-F <file>`.
- ⚠ **`open_question.lifecycle_status` is a CHECK** over `Draft/Proposed/Approved/Rejected/Deferred/Implemented/Superseded/Obsolete` — "Resolved" rolls the whole batch back. `defect.fixed_by` is a **FK**; PR refs go in `custom_attributes`.
- ⚠ **The keycloak container's `docker exec` shell has no `KC_BOOTSTRAP_ADMIN_PASSWORD`** — the entrypoint exports it for its own process only. Read `/run/secrets/kc_bootstrap_admin_password`.
- ⚠ **Windows `python3` cannot see Git Bash's `/tmp`** — pass Windows-style absolute paths when building SSM `--parameters file://` payloads.
- [⚠ Baselines are numbers, not properties](baselines-as-numbers-not-properties.md) — a count-based test on a shared topic can never discriminate.
- [⚠ Immutable history → cleanup is asymmetric](immutable-history-cleanup-asymmetry.md) — deleting a Keycloak user ORPHANS its member rows forever. **Disable, never delete.**
- [A static file cannot configure a live realm](a-static-file-cannot-configure-a-live-realm.md) — `realm-export.json` reaches **fresh stacks only**; `reconcile.sh` is the only seam to prod/UAT.
- [Write the handoff LAST](write-the-handoff-last.md) — it found `DEF-053`/`DEF-054` last time. Stamp superseded files with ⛔ immediately.
- [Commit package writes before git ops](commit-package-writes-before-git-ops.md) · [Tamheed stale .lock + PID reuse](tamheed-stale-lock-pid-reuse.md) · [Tamheed data repair](tamheed-data-repair.md) · [migration history](tamheed-migration-reverted.md)
- [Localhost CI hides load races](localhost-ci-hides-load-races.md) · [Git push hang → `gh auth setup-git`](git-push-hang-fix.md) · [Run CI gates locally pre-push](ci-gates-run-locally-pre-push.md) · [Always stage .claude/memory in commits](always-stage-claude-memory-in-commits.md)
- [Coverage & E2E mandate](coverage-and-e2e-mandate.md) — ≥95% FE+BE + adversarial E2E. ⚠ **The Playwright suite is NOT UAT-only** (`e2e.yml` runs 7 services with a real Keycloak per PR) **but runs with `KEYCLOAK_ADMIN_ENABLED=false`**, so it never touches the ADR-0038 write path.
- [E2E local run (non-destructive)](e2e-local-run-nondestructive.md) — **`-p acmpe2e` ONLY**, never `npm run e2e:up`. · [Dev-stack rebuild pitfall](dev-stack-rebuild-pitfall.md) — **never `up --build`** the long-lived dev stack.
- [Exact design fidelity + visual loop](exact-design-fidelity-visual-loop.md) · [A green suite is not a look](a-green-suite-is-not-a-look.md) — ⚠ the throwaway harness must import **only** the stylesheets the real route imports.
- [Design: breadcrumb spacing](breadcrumb-spacing-rule.md) · [i18n parity ≠ completeness](i18n-parity-not-completeness.md) · [Web visual-verify cache busting](web-visual-verify-cache-busting.md)
- ⚠ **`.adm-detail-card` has no padding and clips its children** — anything opening a popover needs `.adm-card-overflow`. · **`userEvent.setup()` installs its own clipboard stub** — define a clipboard spy *after* it.
- [User prefers simple English](user-prefers-simple-english.md) · [Phase prompt Standard Footer](phase-prompt-standard-footer.md) · [Install the schedule, not just the daemon](install-the-schedule-not-just-the-daemon.md) · [Arabic rename is a grammar rule](arabic-rename-grammar-not-substitution.md) · [A clean scan must prove it had a subject](scan-must-prove-it-had-a-subject.md) · [Guard the property, not the value](guard-the-property-not-the-value.md) · [The suite assumed a fresh database](e2e-assumes-a-fresh-database.md) · [The feature is often already half-built](check-before-building.md)
- ⚠ **AC id cells in markdown tables must stay BARE** (`| AC-001 |`, never bolded) — bolding breaks the Keystone G-PROGRESS gate.
- ⚠ **A new advisory can turn `main` red with no code change** — `GHSA-q939-rpr3-3284` (SSH.NET) blocked every merge mid-session. "It's only tests" is how a blocking gate becomes advisory.
- ⚠ **A compose `secrets:` entry whose FILE IS MISSING fails the WHOLE stack** — any mounted secret must be written **unconditionally** by `gen-secrets`.

## Completed ladder P1–P19 + PH-5 (reference only — do not re-open)

- [PH-5 / SL-025 — UAT is live and LOGIN WORKS](ph5-sl025-uat-live.md) · [PH-5 AWS deployment](ph5-aws-deployment.md) · [P19 release readiness + D-23](p19-release-readiness.md) · [P18 deployment](p18-deployment.md) · [P17b decision-issuance UI](p17a-test-hygiene.md) · [P17/P18/P19 slice notes](next-p17-p18-p19.md)
- [P16 hardening B2b/B3/B4](p16-hardening-b2b-b3-b4.md) · [P16b CI security gates](p16b-ci-security-gates.md) · [P16a audit & vote crypto](p16a-audit-vote-crypto.md)
- [P15 Research & Knowledge](p15-research-knowledge-plan.md) · [P15 audit remediation](p15-audit-remediation.md) · [P15f/g search](p15f-search-progress.md)
- [P13 Webex](p13-webex-integration-plan.md) · [P13 audit remediation](p13-audit-remediation.md) · [P13 recording upload](p13-recording-upload.md) · [Webex coverage-gate exclusion](webex-coverage-gate-async-exclusion.md)
- [P12 Dashboards & Reports](p12-dashboards-reports-plan.md) · [P11 ADRs & Invariants](p11-adrs-invariants-plan.md) · [P10 Risks/Deps/Traceability](p10-risks-deps-traceability-plan.md)
- [P9 Voting](p9-voting-plan.md) · [P8 Actions](p8-actions-plan.md) · [P7 Minutes & Decisions](p7-minutes-decisions-plan.md) · [P6a meeting IA](p6a-meeting-ia-plan.md) · [P6b notifications IA](p6b-notifications-ia-plan.md)
- [Audit slice (AC-017)](audit-slice-literal-ac017.md) · [Topic Prepare UI (D-15)](topic-prepare-ui-gap-d15.md) · [Keystone package migration](keystone-package-migration.md) · [Keystone gap remediation](keystone-migration-gap-remediation.md)
