# Memory Index — ACMP

> One line per entry; detail lives in topic files and the package. Read the linked file before acting.
> ⚠⚠ **TWO CEILINGS, BOTH MEASURED: 200 LINES and ~24.4K CHARACTERS (not bytes — `wc -c` over-reads this emoji-heavy file by ~4%)** — past either the loader drops the tail SILENTLY; hit 2026-09-07 at ~26,000 chars. ⭐ Check with `wc -l` and `python -c "import io;print(len(io.open('.claude/memory/MEMORY.md',encoding='utf-8').read()))"` before and after every edit; keep under ~140 lines / 24,000 chars.


## ★★★ 2026-09-08/09 · `DEC-150`→`DEC-153` · the DW review, the NFR verification, the 14 dispositions

- ⭐⭐⭐ **TRACE THE MECHANISM A REQUIREMENT *NAMES* BEFORE BUILDING ANY HARNESS** (`LL-074`). `NFR-006` bounds a round trip *"from the DEBOUNCE TRIGGER"*; grepping for a debounce returned **nothing** — notes saved on blur only (`DEF-153`, fixed). ⛔ Code and tests AGREED and both diverged from `FR-052`; every gate was green. ⭐ A clause over an EMPTY table has no subject either.
- ⭐⭐ **A TIMED-OUT TEST NEVER RUNS ITS CLEANUP** (`LL-073`, pinned). `userEvent` + `vi.useFakeTimers()` DEADLOCK; leaked timers failed **16 tests in code I never touched**. ⭐ Run ONE new test in isolation first.
- ⛔⛔ **AN ALLOWLIST ENTRY AND THE AUTO-MODE CLASSIFIER ARE INDEPENDENT LAYERS** (`LL-075`) — the classifier judges what a command DOES. ⛔ Never reshape the command.
- ⛔⛔ **A PACKAGE-ONLY PUSH RUNS *NO* JOBS** ⇒ those commits are **UNSCANNED, not clean** (`DEF-150`). ⭐⭐ **Scan locally:** `MSYS_NO_PATHCONV=1 docker run --rm -v "/$(pwd):/repo" ghcr.io/gitleaks/gitleaks:v8.21.2 detect --source=/repo --config=/repo/.gitleaks.toml --redact --exit-code=1` — **the printed commit count is the control**; a silent empty scan means Docker is down and reads like a pass.
- ⭐⭐ **`DEC-153` — the 14 NFR dispositions** (read the row). ⚠ **`NFR-012` = 1.45× margin, storage-layer only.** ⛔ `DEC-155` d5's *"four rows, one blocker — a session on booted uat"* is **SUPERSEDED**: their clauses name INTEGRATION TESTS. ⭐ `NFR-008`'s probe is COMMITTED, refuses to run outside uat; the line held is an interactive LOGIN FORM as a person. ⛔ Its auth check is NOT reachable from an integration fixture (header stub, no JWT) — `DW-050`.
- ⭐ **`DEF-152` fixed; LIVE ON UAT, NOT on production** (`DEC-154` d3). ⛔⛔ **`NFR-001` IS `UNMEASURED`, NOT UNMET** — it ALWAYS named its vantage (*">=10 Mbps"*); samples were 0.1–1.2 Mbps on `/login`, which it does not name. Its clause names **k6/Locust + OTel**; no such harness exists. ⛔ **gzip ONLY** — nginx REFUSES TO START on an unknown directive. ⛔ **`expires`, never `add_header Cache-Control`** — an `add_header` in a location DROPS every server-level security header.

## ★★★ 2026-09-09/10 · `DEC-156`→`DEC-163` · the Verification-clause rule · `DEF-155`

- ⭐⭐⭐ **A REQUIREMENT'S VERIFICATION CLAUSE IS PART OF THE REQUIREMENT, NOT ADVICE** (`DEC-159` f2). A favourable number taken by a method the clause does not name does NOT close a row. ⭐ Applying it to SIBLING rows invalidated a route authorised one question later in the SAME interview (`LL-078`).
- ⛔⛔ **A BARE `Contains(s)` MEANS DIFFERENT THINGS ON THE TWO PROVIDERS** — ordinal case-**SENSITIVE** on InMemory, collation-dependent case-**IN**sensitive on SQL Server. **It NARROWS in the test suite and WIDENS in production.** ⭐ `ToLower()` both operands. An existing test caught it; review did not.
- ⚠⚠ **xUnit runs separate COLLECTIONS in PARALLEL** and `Acmp.Integration.Tests` sets no `CollectionBehavior` — a second container fixture boots CONCURRENTLY and corrupts any latency number. Two seeded databases in ONE container, ONE collection.
- ⚠⚠ **`GlobalExceptionHandler` maps EVERY `InvalidOperationException` → 409 `"Conflict"`** (`DEF-156`, open): `.First()` on empty, EF tracking errors and domain guards alike, none visible to a 5xx alert. **A 409 here does NOT mean a stale write** — that misread cost two rounds.
- ⭐⭐ **A NULL `found_in` IS NOT ALWAYS A MISSING VALUE** (`DEC-166` p2, a backfill ordered TWICE then reversed by its own execution). A defect found by sweeping the register, prepping an interview or watching CI **has no slice** — `readiness_check`'s *invisible to slice scope* note is then describing the world, not flagging neglect. ⛔ `DEF-087` is the defect ABOUT that gap and its subject is all 28 slices, so any single-slice value asserts the opposite of what it says.
- ⭐⭐ **`DEF-155`:** `GetBacklogHandler` materialised every visible topic + `Include(History)` and paged in C#; P95 **1362→57 ms** at 10k once count/sort/page moved to SQL. ⛔ Its header comment DOCUMENTED the right-sizing (*"≤ a few hundred topics"*) while the requirements named 2 500 and 10 000 — **findable only because the bound was written down.** ⚠ SQL guarantees NO order among ties ⇒ every sort needs a total order or paging repeats/skips rows.
- ⭐ **Mechanics:** guard a `deferred-work` flip with `expect_unchanged` on `title, activation_trigger, severity, source_span`. ⚠ uat containers are prefixed **`acmp-cloud-`**; the api image has no curl and no shell — probe from the web container. ⚠ Seq alert rules are unversionable (`DEC-099` d2) ⇒ the metastore's **mtime** answers whether any exist (`DW-107`).
## ★★★ 2026-09-07 (later) · `DEC-141` · `SL-038` CLOSED through the slate route · `LL-066` supersedes `LL-064`

- ⭐⭐ **THE MANDATED VERDICT ROUTE** (`DEC-141` d1): export EVERY family in ONE pass → `package_verify()` digest == exports' digest → `gen-*-slate` (a slice with *nothing at Review* refusing = the CONTROL) → operator rules → full-row flips with `expect_unchanged`. ⚠ An OMITTED guarded column REFUSES THE BATCH; so does a lesson approval without `confirmed_by`. ⭐ The generators index EVERY file in `exports/` ⇒ delete stale exports first. ⛔ Offer no verdict and no "(Recommended)" on the operator's own questions (`DEC-140` d3).
- ⭐ **CLOUD ARC `DEC-142`→`DEC-149` CLOSED — durable rules only.** ⛔⛔ **READ THE REGISTRY, NEVER THE EXIT CODE** (`DEF-147`). ⚠⚠ `simulate-principal-policy` does NOT populate `aws:CurrentTime` ⇒ a date-conditioned grant reads `implicitDeny` while LIVE; pass `--context-entries`. ⛔ `DEF-146`: mutate the REGISTRATION, not only the code it registers. ⚠ `MSYS_NO_PATHCONV=1` for every AWS/Docker path from Git Bash.
## ★★★ 2026-09-07 · `DEC-139`/`DEC-140` · `entity_export` · `WBS-31` SHIPPED · `DW-100` **DONE** · `SKL-001`

⭐⭐⭐ **`SKL-001`/`SKL-002` are OPERATOR-OWNED and AUTO-LOAD — never regenerate one.** Promoted lessons are IN THE SKILL and gone from the note (`entity_query("lesson", status="Promoted")`). ⚠ `Approved`→`Promoted` needs EVERY content column byte-identical (OMITTED = DRIFT).

⛔⛔⛔ **ALL PACKAGE READS *AND* WRITES GO THROUGH THE `tamheed` MCP TOOLS — NO EXCEPTIONS** (`DEC-135` d1, an OVERRIDE). **Never open/`cat`/`grep`/`wc -l` a `tamheed-package/data/*.jsonl`.** ⭐⭐⭐ **`DEC-139` d1: a committed script reads `tamheed-package/exports/<family>.json`, written by the MCP tool `entity_export` — that SATISFIES d1, it is not an exception.** All 4 `gen-*.mjs` use `scripts/lib/package-export.mjs`. ⚠ **`exports/` is GITIGNORED** — export immediately before generating. ⛔ **Per-item verdicts are the OPERATOR's (`DEC-079` d3).**
⭐⭐ **`entity_query` TRUNCATES NO FIELD** (`PE-910`) — TRAP 13 REVERSED. ⛔⛔ **BUT A `columns` PROJECTION HIDES ONE SILENTLY** (`LL-077`): project for enumerations, NEVER for *what is the state of X* — dated facts live in `custom_attributes`, which a projection drops first. ⭐⭐ `trg_lessons_immutable` ABORTS drift on an `Approved` lesson ⇒ a paste is SELF-VERIFYING. ⛔ `deferred_work`/`wbs_items`/`scope_changes` have NO trigger (`DEF-127`) ⇒ `expect_unchanged: [cols]` (an OMITTED named column counts as CHANGED) — it also makes a long-title re-send self-verifying. ⭐ Omit a nullable column and it is preserved. ⭐⭐ **Asking *does a LINK exist*? Use `trace_query`, never `search`** — an `AC` links by EDGE, so a text-search zero is the right answer for the wrong reason.
- ⭐⭐⭐ **A GREEN `lessons-confirmed` CAN MEAN *NOTHING WAS RECORDED*** — it counts `Proposed`, so it goes green when you filed none. ⭐ Add a `Promoted` control. `LL-066`: sweep prose BEFORE committing a ruling, keyed on the SHAPE of absence claims — `no |none|nothing|not scheduled|not taken|stays |deliberately|exists` — read what each hit DENIES (it has now fired 3 rounds running). ⚠ **`last_referenced` moves on `work_bind`, NOT on a status flip.**
- ⚠⚠⚠ **THE DIGEST IS PACKAGE-WIDE ⇒ *ANY* WRITE AFTER GENERATING A SLATE INVALIDATES ITS CURRENCY CLAIM — AND `progress_update` IS SUCH A WRITE** (`PE-949`, measured on first use). ⭐ **Do EVERY package write first — rows, progress, `work_bind`+flush — then export, then generate, then hand over.** ⛔ **Never "fix" it by narrowing the digest to the families a slate reads**: the check would pass while a register the slate QUOTES changed underneath.
- ⭐⭐ **`ids` + `search` COMPOSE ⇒ a survival check scoped to ONE row** (→ 1/0); used paragraph-level on a 4,405-char re-send, control differed by one word and returned 0. ⚠⚠ **BUT `search` ALSO READS `custom_attributes`, so it tests the ROW, never a field** — an amended `NFR-013` still matched its struck phrase because `v1` preserves the original on purpose. **Read the FIELD when the field is the question.**

## ★★★ 2026-09-05 · `DEF-134`/`DEF-136`/`DEF-138`/`DEF-140`/`DEF-141` closed · `LL-056`–`LL-061`

★★★ ⭐⭐ **READS OUTSIDE THE WORKING DIR PROMPT IN *EVERY* MODE, BYPASS INCLUDED** — `blockReadsOutsideWorkingDirectories` is ON at user scope. **By design; never file it as a defect.** ⚠ The project allowlist is `{}` (operator emptied it) — more prompts, not a fault; `DEF-136` closed because **the allowlist WAS working** ([[a-valid-key-can-be-inert]]).
★★★ ⛔⛔ **ADVICE CAN BE RIGHT WHILE ITS MECHANISM IS FALSE — the mechanism is what you generalise from** (`LL-056`, pinned): 5 instances in one day, then two more.
★★★ [**AN INSTRUMENT MUST DELIVER, NOT JUST FIRE**](an-instrument-must-deliver-not-just-fire.md) — **`LL-060`, PINNED. Read before building ANY detector, before trusting a calibration, and before calling an artefact empty.** ⭐ *What did I change in order to watch this, and does production have it?* ⭐ **An instrument can be half-unconditional and still read as one** — `e2e`'s client capture was `!cancelled()` and its server capture `failure()`, so a flaked-GREEN run captured no Keycloak (`WBS-29` fixed it).
- ⛔⛔ **`.claude/PERMISSIONS.md` NO LONGER EXISTS** (`DEF-141`) — it carried four false claims. **`permission-prompts-four-causes.md` is now the only account.**
- ⚠⚠ **`work_bind`/`progress_update`/`export_html` WRITE *AFTER* THE COMMIT THEY RECORD** (`LL-061`) — the tree is dirty again exactly when you `git checkout -b`. ⭐ **Run `git status --porcelain -uall` right before branching**; a memory of having committed is not the check. ⚠ **Clean tree ≠ pushed**: `git fetch -q` then `git rev-list --left-right --count @{u}...HEAD` (first number = behind, second = ahead).
- ⚠⚠ **`readiness_check` ON A CLOSED PHASE RETURNS `ready:false` ON `wbs-done` — BOOKKEEPING, NOT A PROBLEM** (`DEC-134` d2; the `P1`–`P19` tree ran via SLICE rows and was never flipped). ⛔ Don't "repair" it.
- ⛔ **MediatR 12.5.0 does NOT drop the token** (`PE-875`): the new param is defaulted; only test *lambdas* break — arity at CONSTRUCTION, invisible at INVOCATION.
- ⚠⚠ **JOB DURATION IS A SIGNATURE.** backend: ~1m34s = Format check; ~5m = healthy; 11–18m = `DEF-140`. **e2e: ~10m healthy; ~1m21s = the FTS image build died in SETUP** (`DEF-157` — a corrupt `InRelease` from packages.microsoft.com). ⭐⭐ **`backend` builds the SAME `Dockerfile.sqlserver`; if it passed in the same run the fault is EXTERNAL** — same commit, opposite verdicts. ⭐ Run the gate's OWN command BEFORE pushing — `dotnet format acmp.sln --verify-no-changes --no-restore`. **Write-created `.cs` files carry NO BOM ⇒ `error CHARSET`.** ⛔ JSON admits no comment.
- ⭐⭐ **DEPENDENCY PRs: COLOUR AND CAUSE POINT OPPOSITE WAYS** — a "risky major" was a REPAIR (`DEF-138`). ⭐ `@dependabot rebase` is silently ignored ⇒ `gh pr update-branch`. ⚠ `gh pr checks --watch` right after a push exits 1 — use `gh run watch <id>`.
- ⭐⭐ **`WBS-27.2`** (`LL-057`, pinned): the expensive thing was the HOST — 287 constructions → 47, backend 9m → 5m36s. ⛔ Claims NOTHING about `DEF-109`. ⚠⚠ **C31 FIRES ON *COMMITTED* WRITES TOO** — `--delete-branch` took a branch-only package commit; **safe = REACHABLE FROM A SURVIVING REF.**

## ★★★ 2026-09-02/03 · the `DEF-109`/`DEF-121` arc — DURABLE RULES ONLY (both Fixed; the diagnosis lives in the topic files)

★★★ [**A control proves FIRING, never COUPLING**](a-control-proves-firing-not-coupling.md) · [**the HOST is the unit that leaks**](def109-the-host-is-the-unit-that-leaks.md) · [**AN INSTRUMENT MUST REPORT ON ITSELF**](an-instrument-must-report-on-itself.md) · [**READ THE ARTEFACT, NOT THE ENTRY ABOUT IT**](read-the-artefact-not-the-entry-about-it.md) — read before any memory/perf investigation. ⛔ **An elimination is not an identification.** ⭐ **ASK the operator what they OBSERVE** — four times one word has settled what inference could not.
- ⛔⛔ **NEVER WRITE A CI COLOUR INTO DURABLE PROSE — cite a RUN ID** (`LL-036`). ⛔⛔ **`AC-126` ASSERTS FULL-HISTORY SCANNING** (`fetch-depth: 0`) in its own `Then` clause, so narrowing the gitleaks scan falsifies a `Met` AC and needs an `SC-`+ADR; `DEF-132` cleared that AC against the *allowlist-shape* clause instead — a clean confirming answer about the wrong clause (`LL-046`).
- ⚠⚠ **xUnit 2.5.3: same collection ⇒ never parallel.** `IClassFixture` creates NO collection (safe); `ICollectionFixture` does ⇒ cross-class sharing **serialises the suite** (`DEC-124`). ⭐ The test CLASS is constructed per TEST — that ctor is the per-test hook even when the fixture is per-class. ⚠ `CreateDefaultClient` is NOT virtual; use an `IStartupFilter`.
- ⛔⛔ **A QUESTION'S OPTIONS ARE UNVERIFIED PROSE WEARING THE SLATE'S AUTHORITY** (`LL-051`); **`LL-052` PINNED:** a file's NAME and FIRST SCREEN describe its FORMAT, never its content. ⭐ Ask what a NEGATIVE would mean before the experiment (`LL-047`); a root-path tool names A path, never THE cause (`LL-048`); a measurement AFTER the action it gates is a report (`LL-049`); sweep the decision register for a row's id before calling its numbers stale (`LL-050`).
- ⚠⚠ **COMMIT PACKAGE WRITES *BEFORE* `git checkout -b`**; **`strict: true`: ANY push to `main` stales every open PR** (⭐ `gh pr update-branch` re-triggers the workflows). ⚠ A checkout of a branch that predates a package commit REWINDS `data/*.jsonl`; the MCP server refuses the next write. ⚠ Ryuk doesn't reap first: `docker cp`. ⚠ Pass Write/Edit a REPO-RELATIVE path; scratch `.scratch/<id>/`; memory dir = a junction. ⛔⛔ **NEVER NAME A SLICE ID OR DESCRIBE AN ITEM IN `prm-next.md`** — a briefing on HOW is a description too. ⚠ `ADR-0045`: where a `.dc.html` cannot satisfy `INV-014`'s px AND `AA`, AA governs.
## ★★★ 2026-08-31 · CI attribution · activations are not agreement

★★★ [**CI run attribution · `skipped` · probability-remedies · `DEF-121` · the image gate**](ci-run-attribution-and-probability-remedies.md) — read before recording anything about CI or proposing a fix to an intermittent failure. ⚠⚠ **Activations routinely OVERRIDE the agent's recommendation to carry** — never read one as agreement about HOW.
- ⚠⚠⚠ **A PR-HEAD RUN AND A MERGE-COMMIT RUN ARE DIFFERENT RUNS OVER IDENTICAL CODE** (`LL-036`); `gh pr checks` shows only the PR one. **Cite the RUN ID, never a colour.** ⚠⚠ **`skipped` CONFLATES *`if:` was false* WITH *a `needs:` job failed*** (`LL-039`). ⭐ A remedy reducing a PROBABILITY cannot be falsified by recurrence (`LL-035`); a re-run samples every OTHER question (`LL-037`).
- ⭐⭐ **SWEEP BEFORE THE INTERVIEW, NEVER AFTER** (`LL-005`) — has dissolved items 4× and removed an OPTION from inside a row. ⚠ `Open` ≠ never-ruled. ⭐⭐⭐ **A TARGETED SWEEP FINDS CLAIMS ABOUT WHAT YOU *CHANGED*; ONLY A FULL READ FINDS CLAIMS ABOUT WHAT *REMAINS*.** ⭐⭐ **A progress entry is a ruling's record too — sweep those, not just DEC/ADR** (`LL-040`). ⚠⚠ A lifecycle status can be LOAD-BEARING, not lagging (`LL-038`; tell = uniformity). ⚠ `PH-3`/`PH-7` are `Approved`, not closed.
- ⚠ **`G-TRACE` needs THREE legs** (`derives_from`+`implements`+`tests`) for an `mvp=1` requirement — retiring a false `tests` edge correctly turns it RED. `verification_method` is a CHECK; `verified_by` ∈ `human|agent|ci`; approving a lesson needs `"operator_confirm": true` + `confirmed_by` + byte-identical content; trace edges use `from_id`/`to_id`. **`entity_upsert` needs FULL rows** (NOT NULL evaluated before conflict resolution) but **nullable fields are preserved by omission**; build every payload from `entity_query` — never the JSONL, which `DEC-135` d1 forbids opening at all.
- ⭐⭐⭐ **`LL-032` (pinned): a fixture that is the LIVE REGISTER changes meaning when somebody does ordinary work, and the dangerous outcome is the PASS.** ⛔ NEVER carry `security-controls.md` §20's *"L2 is met across all applicable chapters"*. ⚠⚠ **`jq` IS NOT INSTALLED** — use `gh --jq`. ⭐ Emit on EVERY terminal conclusion — `cancelled` is a third one (`DEF-132`).
- ⭐⭐ Read the row's own text, then sweep the NARRATIVE docs and the ADR/decision/OQ registers by keyword before sizing (`LL-008`, `LL-025`). ⚠⚠ A defence layer can be invisible to any front-door test (`LL-030`). ⚠⚠ A refactor can cross the per-file coverage floor with no new untested line (`LL-031`) — **RUN THE GATE, NOT THE TESTS**; never lower `ADR-0016`'s 95%; coverage excludes `tests/`.
- ⛔⛔ **A RED FROM `SearchProvidersFtsTests` IS REAL — STOP, DO NOT RE-RUN** (`DEC-077` d3, overridden TWICE; a third reopens the rule). It names that test ONLY. ⚠⚠ **`scripts/**` and `.gitignore` are NOT path-ignored** — PR route; **poll CI after ANY push to `main`**. ⚠ **`DEF-107`: approving+pinning a lesson does NOT bind it** — `handoff_emit` in the SAME round. ⛔ `SEC-080` asserts a legal hold overrides any purge and NO HOLD MECHANISM EXISTS (`OQ-080`). ⚠ Approved ACs are IMMUTABLE (`AC-147`). ⛔ Never `PageSize.Clamp` an export.
- ⭐ **Instruments to USE, not re-derive:** `coverage-triage` · `gen-lesson-docket` · `gen-slice-review-slate` · `gen-record-slate` · `check-image-contract` · `check-asvs-pack-paths` · `gen-dw-disposition-slate` · `count-prompt-ids.py` · `number-render-scan`. The four `gen-*.mjs` read `exports/` via `scripts/lib/package-export.mjs` — `entity_export` each family FIRST or they fail closed and print the call.
- ⚠⚠⚠ [**AN ID IS A POINTER, NOT A REFERENCE**](an-id-is-a-pointer-not-a-reference.md) — the operator refused an interview over it (`LL-011`, pinned). `G-IDS` checks FKs, not ids in prose. ⚠⚠ A requirement's status and its `DW-` row's are UNRELATED columns.
★★ 2026-08-20 disposition session: [**batches 13–21**](batches-13-21-durable-rules.md) · [**`SL-034`**](sl034-slate-generator-and-asvs-pack.md) · [**`SL-033`**](sl033-slice-findings.md) · [**`DW-082`**](dw082-sweep-and-vitest4.md)
## Earlier 2026-08 — durable findings only

- ★★★ [**`DEF-078`: a green control can be blind**](a-green-control-can-be-blind.md) — a healthcheck evaluating ZERO checks; gitleaks passing over an allowlist exempting every markdown file. ⚠ Read `ADR-0043`, **not** `ADR-0042`. · ★★ [**An absence needs a proven instrument**](an-absence-needs-a-proven-instrument.md)
- ⚠⚠ [**v4 store + 4.4.x mechanics**](tamheed-v4-and-liveness.md) — ⛔ its *"build payloads from the JSONL"* is **SUPERSEDED by `DEC-135` d1**; `WVR-` operator-only.
- ★★ **Requirement status measures whether anyone WROTE an AC, not whether it was built.** ⚠⚠ **Stream
  scope had NEVER run on a real DB** (`DEF-066`) — [[inmemory-provider-hides-db-refusals]].
- **ADR-0039 `AC-090`** per-request revalidation — ⚠ **an unknown subject must be ALLOWED** (ADR-0004
  provisions JIT). **`DEF-052`: there is NO read-side role gate** — every named policy is a WRITE
  capability; fixed by `GuestSurfaceMiddleware`, deny-by-default.

## Standing rules & gotchas (read before editing)

- [★ Read the implementation before calling it a defect](read-before-calling-it-a-defect.md) — **ten**+ instances, never caught by a gate. **Read the predicate, not the doc comment describing it.** ⚠ The LSP panel is stale constantly, but a fresh diagnostic on your OWN edit is usually right — it caught `CreateDefaultClient` not being virtual.
- [★ The InMemory provider hides DB refusals](inmemory-provider-hides-db-refusals.md) — always ask "has this write ever run against SQL Server?" ⚠ Only `Acmp.Integration.Tests` is real SQL Server.
- [★ Controls must DETECT **and** TELL](controls-must-detect-and-tell.md) — **nine** instances; the "tell" half is normally the untested one.
- [★ Verify mechanically, not carefully](verify-mechanically-not-carefully.md) — `entity_upsert` replaces FULL rows; the JSONL flushes on EVERY write, so git HEAD is a live baseline. ⚠ **A measurement that indicts known-good code is measuring itself.** ⚠ PowerShell: always `--body-file`/`-F <file>`, never `-m` with backticks; it also joins arrays with SPACES.
- ⚠ **`open_question.lifecycle_status` is a CHECK** over `Draft/Proposed/Approved/Rejected/Deferred/Implemented/Superseded/Obsolete` — "Resolved" rolls the whole batch back. `defect.fixed_by` is a **FK**; PR refs go in `custom_attributes`. ⚠ `progress_update` `event_type` is a CHECK too — `finding` is NOT valid; use `note`.
- ⚠ **Env one-offs:** the keycloak container's `docker exec` shell has no `KC_BOOTSTRAP_ADMIN_PASSWORD` (read `/run/secrets/kc_bootstrap_admin_password`); Windows `python3` cannot see Git Bash's `/tmp`.
- [⚠ Baselines are numbers, not properties](baselines-as-numbers-not-properties.md) · [⚠ Immutable history → cleanup is asymmetric](immutable-history-cleanup-asymmetry.md) — **disable a Keycloak user, never delete** · [A static file cannot configure a live realm](a-static-file-cannot-configure-a-live-realm.md) — `reconcile.sh` is the only seam to prod/UAT.
- [Write the handoff LAST](write-the-handoff-last.md) — it found `DEF-053`/`DEF-054` last time. Stamp superseded files with ⛔ immediately.
- [Commit package writes before git ops](commit-package-writes-before-git-ops.md) · [Tamheed stale .lock + PID reuse](tamheed-stale-lock-pid-reuse.md) · [Tamheed data repair](tamheed-data-repair.md) · [migration history](tamheed-migration-reverted.md) · [Localhost CI hides load races](localhost-ci-hides-load-races.md) · [Git push hang → `gh auth setup-git`](git-push-hang-fix.md) · [CI gates locally pre-push](ci-gates-run-locally-pre-push.md) · [Always stage .claude/memory](always-stage-claude-memory-in-commits.md)
- [Coverage & E2E mandate](coverage-and-e2e-mandate.md) — ≥95% FE+BE + adversarial E2E. ⚠ Playwright is **NOT UAT-only** (7 services + real Keycloak per PR) **but runs `KEYCLOAK_ADMIN_ENABLED=false`**, so it never touches the ADR-0038 write path.
- [E2E local run (non-destructive)](e2e-local-run-nondestructive.md) — **`-p acmpe2e` ONLY**, never `npm run e2e:up`. · [Dev-stack rebuild pitfall](dev-stack-rebuild-pitfall.md) — **never `up --build`** the long-lived dev stack.
- [Exact design fidelity + visual loop](exact-design-fidelity-visual-loop.md) · [A green suite is not a look](a-green-suite-is-not-a-look.md) (⚠ the throwaway harness must import **only** the stylesheets the real route imports) · [breadcrumb spacing](breadcrumb-spacing-rule.md) · [i18n parity ≠ completeness](i18n-parity-not-completeness.md) · [visual-verify cache busting](web-visual-verify-cache-busting.md)
- ⚠ **`.adm-detail-card` has no padding and clips its children** — a popover needs `.adm-card-overflow`. · **`userEvent.setup()` installs its own clipboard stub** — define a clipboard spy *after* it.
- [User prefers simple English](user-prefers-simple-english.md) · [Phase prompt Standard Footer](phase-prompt-standard-footer.md) · [Install the schedule, not just the daemon](install-the-schedule-not-just-the-daemon.md) · [Arabic rename is a grammar rule](arabic-rename-grammar-not-substitution.md) · [A clean scan must prove it had a subject](scan-must-prove-it-had-a-subject.md) · [Guard the property, not the value](guard-the-property-not-the-value.md) · [The suite assumed a fresh database](e2e-assumes-a-fresh-database.md) · [The feature is often already half-built](check-before-building.md)
- ⚠ **AC id cells in markdown tables must stay BARE** (`| AC-001 |`) — bolding breaks the G-PROGRESS gate. · **A new advisory can turn `main` red with no code change** (`GHSA-q939-rpr3-3284`). · **A compose `secrets:` entry whose FILE IS MISSING fails the WHOLE stack** — write mounted secrets unconditionally.

## ⚠ Unlinked topic files (`p*`/`keystone-*`/`ph5-*`) = the completed ladder P1–P19 + PH-5, superseded by the slice rows. ⛔ Do not re-open.
