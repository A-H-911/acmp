# Memory Index — ACMP

- [P6a meeting-IA plan](p6a-meeting-ia-plan.md) — DONE & MERGED to main (PR #53, squash ef074a8); next is P6b notifications IA.
- [P6b notifications-IA plan](p6b-notifications-ia-plan.md) — DONE & shipped (branch feat/p6b-notifications-ia, PR opened/unmerged). Popover+inbox reconciled to ACMP.dc.html; read-all audited; type=Category/key=derived (no migration). Next = Decisions/Minutes.
- [P8 Actions plan](p8-actions-plan.md) — GO-gated slices. **P8 (Actions) module COMPLETE: P8a/P8b/P8b2a/P8b2b/P8c-1/P8c-2 MERGED to main (latest #72 `c3db667`); P8d (AC-029 decision-link gate, OQ-045) MERGED to main (squash `83249eb`, PR #73, all 4 CI checks green, 2026-07-02).** P8d = HARD gate (Fork 1), backend-only (Fork 2), supersession exempt (ASM-014); new `IActionLinkDirectory` Shared seam; gate in `IssueDecisionHandler` not the domain. AC-029→Met. Next phase = **P9 Voting / P10 Risks+Dependencies+Traceability** (P10 widens AC-029 predicate to typed edges).
- [P7 Minutes & Decisions plan](p7-minutes-decisions-plan.md) — **P7 COMPLETE: P7a/b/c/d ALL MERGED to main (#58/#59/#60/#61/#62; P7d squash `9de17e4`, merged 2026-07-01 via operator override).** main is green + deployable. Next slice = **P8 Actions** (plan-first, GO-gated). Locked: MoM homed IN Meetings module (not new); version-preserving supersede (same MIN key, Version++) unlike Decisions; Approve+Publish = two transitions (notify on Publish); SoD-2 soft (sole-author=CreatedBy); 5-state vs design 3-toggle=blessed; Content→single markdown Summary; bilingual MIRRORED (en===ar); FE i18n namespace=`meetings.mom` (NOT `meetings.minutes` = the "{{count}} min" scalar).

- [P12 Dashboards & Reports plan](p12-dashboards-reports-plan.md) — 3 GO-gated PRs. **PR1 backend reads MERGED** (PR #93, squash `20a451b`): decisions/votes/minutes registers + `Topic.TimesDeferred`; ADR-0022 (no columnstore/chart-lib; resolves OQ-022). **PR2 role dashboards BUILT + all FE gates green** (branch `feat/P12-pr2-role-dashboards`): `features/dashboard/` (pure `dashboardAgg` + cards + `RoleDashboard` 3 variants, role-exclusive Chairman>Secretary>Committee per F-19); **AC-064/065/066 → Met** (traced to dashboardAgg.test + RoleDashboard.test); deleted orphaned `ui/Card.tsx`; 802 vitest/cov 99.80%/i18n 1364/oxlint/build green. **Committed `eeebc41`, PR #94 all 4 CI green (incl. e2e); live `.dc.html` pixel-VR PASS** (`e2e/p12-dashboard-vr.spec.ts`, 3 variants × EN-light+AR-dark, pixel-faithful, stale-bundle quirk did NOT bite). **PR2 MERGED** (squash `4648b19`, PR #94). **PR3 Reports shell BUILT + all FE gates green** (branch `feat/P12-pr3-reports-shell`): full 6-view-tab IA (`reportViews.ts` + `ReportsPage.tsx` rewrite), ~16 real snapshot cards + honest-empty trend/seam, killed DATA-tabs+Period-filter (advisor), real Stream filter + CSV; +columns/stack renderers; 815 vitest/cov 100% new files/i18n 1456; live VR PASS (executive+committee, EN-light+AR-dark, all 5 renderers). All 3 PRs MERGED (PR1 `20a451b`/PR2 `4648b19`/PR3 `6252134`). **★ P12 COMPLETE. ★** Then **adversarial audit → remediation** (branch `fix/p12-audit-remediation`): fidelity + data-honesty fixes, NO AC flips; the MAJOR = Reports Stream filter now scopes decisions/actions via linked Topic; per-series colour/en-dash/KPI/drill fixes; design-update-owed logged (dashboards diverge by AC design). See [[p12-dashboards-reports-plan]].
- [P13 Webex integration plan](p13-webex-integration-plan.md) — **PR-A (join-URL/ar-cards/schedule-UX) + PR-C-3b (ngrok Keycloak) + 3a (deep-link login) + PR-B (auto-register recordings webhook, R1 spike=200 so webhook not polling) ALL BUILT+committed on `feat/p13-webex-integration` (3a `9269160`, coverage-config `3c5e55a`, PR-B next); only operator LIVE validation + token rotation remain.** Phase-2 Webex adapter; app/adapter surface + **WS0 worker-container split** + governance DONE & CI-green on `feat/p13-webex-integration` (WS0–WS3b + WS5/6, ADR-0023 + ADR-0024**→Accepted**, AC-067–072, validator OK). WS0 = `Acmp.Bootstrap` shared composition root + `Acmp.Worker` Generic Host (Hangfire server split off API, API enqueue-only) + `Dockerfile.worker`/compose `worker`+opt-in `ngrok`; SystemCurrentUser SKIPPED (YAGNI, jobs self-attribute); 4 CompositionRootTests guard drift. Only operator **live sandbox** + coverage-run + FR-flip doc-polish remain.
- [Phase prompt Standard Footer](phase-prompt-standard-footer.md) — every pasted phase prompt also carries the phase-prompts.md DoD footer (tests, AC, authz, audit, EN/AR+RTL, progress docs, conventional commits, ADR).
- [Exact design fidelity + visual loop](exact-design-fidelity-visual-loop.md) — "from <file>.dc.html" = pixel-exact to that file, verified by screenshot-compare; not nearest-token, not pixels-deferred.
- [User prefers simple English](user-prefers-simple-english.md) — non-native speaker; explain choices in plain short sentences with a clear recommendation.
- [Breadcrumb spacing rule](breadcrumb-spacing-rule.md) — every screen: 12px gap below the breadcrumb, owned globally on shared `.breadcrumb`; don't re-add per page.
- [i18n parity ≠ completeness](i18n-parity-not-completeness.md) — check-i18n only checks EN/AR key parity; add every enum value to both locales by hand or the UI silently renders raw English.
- [Web visual-verify cache busting](web-visual-verify-cache-busting.md) — live :8088 can serve a stale bundle after web edits; force-recreate the container + clear browser cache + `?cb=` and confirm the loaded JS hash before trusting screenshots.
- [E2E local run (non-destructive)](e2e-local-run-nondestructive.md) — the Playwright e2e needs a fresh `.env.example` stack (local KC authority); it CANNOT run against the dev stack (ngrok issuer). Run via isolated `-p acmpe2e` project after `stop`ping dev, then `down -v` e2e + restart dev on `deploy/.env`. ⚠ never `up --build --env-file .env.example` on the dev project (SQL/KC password-vs-volume mismatch → unhealthy).
- [Coverage & E2E mandate](coverage-and-e2e-mandate.md) — standing goal: ≥95% coverage FE+BE + adversarial E2E every flow/screen; no committed Playwright suite yet, vitest/coverlet need config — baseline first, GO-gated slices.
- [P9 Voting plan](p9-voting-plan.md) — **P9 COMPLETE: P9a backend (`3000b79`, #74) + P9b UI (squash `28d7960`, #75) both MERGED to main, 2026-07-02.** Vote aggregate IN Decisions module; quorum=live-attendance seam; SoD-3=Option A. P9b = FE-only `/votes/:key` screen + Call-vote configure dialog wired to `/api/votes` (Forks 1/2/3 reconciled; issue-from-vote deferred). Next = **P10 Risks+Dependencies+Traceability**.
- [P10 Risks/Deps/Traceability plan](p10-risks-deps-traceability-plan.md) — 7 GO-gated slices; **P10a Risks backend MERGED** (#77, `24d87df`). **P10b Risks UI MERGED** (#78, squash `c424324`, 4 CI checks green): FE-only `/risks` register + `/risks/:key` detail + create dialog vs `/api/risks`; 582 vitest green, per-file cov 100% on 5 new files; Linked-topic REQUIRED (Option A), Mitigation-plan=prose-card, no immutability warning — all flagged design→behavior reconciliations; live `.dc.html` screenshot-compare (heat grid+matrix, RTL+dark) pending a running-stack pass. Operator chose to BUILD the SVG impact graph (P10f) + risk/dep dashboards (P10g) in P10, not defer. **P10c/d/e MERGED. P10f is FULL-STACK (operator overrode FE-only → backend graph endpoint + FR-095 now): PR1 backend MERGED (squash `64930f1`, PR #82, 4 CI green) — read-time composition via 2 new Acmp.Shared ports (NOT a cross-schema CTE), ADR-0020 + OQ-047. PR2 FE (branch `feat/P10f-graph-ui`, commit `4faf1ad`, PR #83): `/traceability/:type/:key` SVG page + group-by-type aside + list-tree fallback; 684 vitest green (+53), per-file cov 100% on 5 new FE files; a11y role→roving-tabindex; RTL scaleX(-1) edges + logical nodes; status-chip→type-chip reconciliation (no far-node status, ADR-0001); cross-stream toggle wired but Topic-scope partial; warm-path "Open graph" panel button. **LIVE VR PASS DONE → FR-096 Met**: `e2e/p10f-graph-vr.spec.ts` (real login + seed via self-describing edge APIs + warm-path open) captured EN-light+AR-dark+list, pixel-faithful to `.dc.html`, RTL edge-flip meets nodes (top risk resolved). All 4 CI checks green on PR #83. **KC-password correction: a fresh `npm run e2e:up` bootstraps Keycloak admin with `.env.example`'s `ChangeMe_KC#2026` (export `KC_BOOTSTRAP_ADMIN_PASSWORD=ChangeMe_KC#2026` for playwright); the `Acmp_KC-4cc52977c342#26` value only applies to a persisted KC-db volume, NOT a fresh boot.** Next = operator merge GO (squash #83).** **P10g (LAST slice) DONE pre-merge (branch `feat/P10g-risk-dep-reports`): FE-only `/reports` Risk & Dependency analytics — no-reference composition reusing the Dashboards&Reports.dc.html card renderers (matrix/stat/bars), rest of Reports IA deferred to P12. 6 cards client-composed from 3 existing reads (risks+deps+topics, no backend); severity-coloured 3×3 count matrix (active-scoped); operator "Fuller" override → 2 by-stream cards join to Topic.streams (includeClosed fetch; multi-stream double-count so Σbars≥distinct KPI; stream CODE shown). OQ-047 scoped partial-advance (inherit-from-topic for reporting agg only). 702 vitest (+18), per-file cov 100% both new files, i18n +37 reports.* keys. FR-138→Partial, FR-095 stays Partial Topic-scope, FR-135/136/137 role dashboards (AC-064/065/066) stay Pending→P12. **MERGED squash `9eb6941` PR #84, all 4 CI green + live matrix VR pass (EN-light+AR-dark RTL pixel-faithful to Dashboards&Reports.dc.html), 2026-07-04. ★ P10 COMPLETE ★ — all 7 slices merged, main green+deployable. Next = P11+ (Governance/Research/Knowledge/Diagrams) or P12 Reporting (reuses P10g's features/reports/* card renderers + reportAgg for the full Reports shell + role dashboards).**
- [P11 ADRs & Invariants plan](p11-adrs-invariants-plan.md) — Governance module. **P11a/P11b/P11c ALL MERGED (#87/#88/#89; P11c squash `878fae6`, 2026-07-04). P11c = Invariant backend: Kind DROPPED (OQ-048 — design form+FR-106 omit it, docs/domain/standards-and-best-practices.md §A = SSoT makes Invariant a sibling not parent), Category+Scope only; SoD-soft `AuthorApprovedSelf` off `CreatedBy` not client Owner (advisor-caught); 1039 BE tests, cov 99.79%. Next = P11d Invariant UI — FOLD IN the OQ-048 doc fix (strike "+ kind" from docs/domain/entity-lifecycles.md §9). Then P11e Decision→ADR promotion (FR-068).** operator: defer violations (FR-108/109 OQ), soft SoD, build promotion (P11e). **P11d MERGED (#90 squash `8901760`, all 4 CI green incl. e2e): Invariant UI (register+no-ref detail+create dialog, FE) + a latent P11a backend hotfix (`GovernanceDbContext` was missing from `MigrationRunner` → Governance schema never provisioned on the deployed stack → ADRs+Invariants 500'd; one-line fix repairs both). Live `.dc.html` VR PASS. Next = P11e (Decision→ADR promotion, FR-068).** **P11e MERGED (#91 squash `918dbff`, all 4 CI green incl. e2e): FR-068 full-stack — POST /api/adrs/from-decision (Chairman-only, Issued-only, one-ADR-per-decision→409); new `IDecisionReader` + `ITraceabilityWriter` (first cross-module WRITE seam, ADR-0021) Shared.Contracts seams; wired the Decision detail's Convert-to-ADR button + confirm dialog; born-Draft ADR pre-filled (Context←Rationale/Decision←Statement/Drivers←Alternatives) + RecordedAs bidirectional edge. Live VR PASS. ★ P11 COMPLETE (a/b/c/d/e). ★ Only queued Governance follow-up = the deferred lifecycle-buttons slice (propose/approve/supersede/deprecate on ADR + Invariant details, both currently read-only).** **P11 AUDIT REMEDIATION (2026-07-05, branch `fix/p11-audit-remediation`, 30 files): adversarial audit → 26 fidelity+robustness fixes, NO AC flips (superseded-body dimming + false-comment kill, Superseded chip warn→neutral, shieldPlus/book/filterLines/checklist glyphs, shared Invariant H1, nav+aria-current tabs, Convert primary+preview, supersede left-arrow/badge, metrics+sticky, dead-code purge; ★BE1 filtered-unique-index migration `Governance_AdrSourceDecisionUnique`, re-promote edge-heal, ADR-0021 wording fix, supersede successor audit). Gates green (BE 1059/cov 99.80%/format/ArchUnit 40/40; FE 772/cov/parity 1310/build). Live pixel-VR SKIPPED — local web container served stale bundle after --build AND --no-cache rebuild (env quirk not code); operator trusted gates. Committed on GO. See [[p11-adrs-invariants-plan]] remediation entry.**
- [Webex coverage-gate async exclusion](webex-coverage-gate-async-exclusion.md) — RESOLVED: removed `CompilerGeneratedAttribute` from coverlet.runsettings (it dropped async method-body coverage → Webex read ~40%); exposed 19 real gaps, all tested to green (global 99.64%). Gotchas: record decl lines need `with {}` not `new`; MigrationRunner needed a `RunAsync`/`AddWebexIfPresent` testability seam.
- [Run CI gates locally pre-push](ci-gates-run-locally-pre-push.md) — `dotnet test` ≠ CI green; also run `dotnet format --verify-no-changes` (BOM/CHARSET) + `node scripts/check-coverage.mjs .` (≥95% per-file). P9a burned 2 CI cycles skipping these.
- [Always stage .claude/memory in commits](always-stage-claude-memory-in-commits.md) — `.claude/memory/` is repo-tracked; every commit must include the session's memory + MEMORY.md updates (`git add -A`). P10e forgot to.
- [Keystone migration gap remediation](keystone-migration-gap-remediation.md) — ladder is P1–P19 (canonical in roadmap §Build-slice ladder + P13–P19 prompts in follow-up-prompts); 16e0577 truncation accident recovered; INV-014 mislabel fixed; README §A–G pointers restored; ~200 stale paths repaired.
- [Keystone package migration](keystone-package-migration.md) — /docs reshaped to Keystone v1.0.0 foldered format + mechanics (validator-green); ADRs moved to docs/adrs/, guardrails→INV- register, root AGENTS.md + thin CLAUDE.md; paths remapped repo-wide.
- [Topic Prepare UI gap (D-15)](topic-prepare-ui-gap-d15.md) — **DONE (Tier 3, 2026-07-09, branch `fix/d15-topic-prepare-ui`, 22 files, awaiting commit GO).** "Mark prepared" button + Prepared badge + Secretary notify + agenda empty-state reword; core-loop.spec now clicks the button (honest AC-035 close, not direct-HTTP) + TopicApiTests proves DI; D-15→Done, OQ-049 (no un-prepare). All gates green **incl. Playwright core-loop run locally GREEN** (via isolated `-p acmpe2e` compose project on .env.example, dev stack stopped+restored non-destructively — dev bakes ngrok KC authority so e2e needs local .env.example authority + fresh volumes) + BE 1217/FE 845/cov/format/i18n/build/Keystone. Design-update-owed stands.
- [P13 audit remediation](p13-audit-remediation.md) — adversarial P13 audit → 2 GO-gated slices. Slice 1 (MinioFileStore real coverage via Testcontainers + honest AC rows + recording-tab minors) MERGED (PR #101, squash `3a973c4`). Slice 2 (dormant Webex hardening: fail-closed key, refresh-degrade, refresh-audit, replay guard, per-job Retry-After, real Hangfire dead-letter test; AC-068/069→Met) DONE+all-gates-green on `fix/p13-audit-slice2`, awaiting commit GO. ★ check-coverage merges stale repo-root cobertura → phantom lines; `rm -rf coverage-out TestResults` before trusting it.
- [P13 recording upload](p13-recording-upload.md) — FR-056 manual recording upload + presigned MinIO playback + delete + source chip: DONE/gated/live-validated, committed on `feat/p13-recording-upload` + PR. ADR-0025, AC-073/074 Met. Gotchas: nginx 1MB `client_max_body_size` was the "upload not working" bug (→2100m, 2GB cap); stale bundle needs hard-refresh; dev users secretary-test/chairman-test. Remaining: D=AC-070 Webex live close, E=D-15.
- [Audit slice (literal AC-017)](audit-slice-literal-ac017.md) — closes Pending AC-017/018/019/020 (INV-005). Operator chose literal AC-017 + same-tx (NFR-042) + RBAC excludes Admin (ADR-0027 supersedes FR-153). Plan `~/.claude/plans/glittery-forging-meteor.md`. **PR0 ADRs 0026/0027 Accepted; PR1 (same-tx infra) + PR2 (74 emit sites → EmitEnrichedAsync) + PR3 (Auditor read/verify API) ALL DONE on `feat/audit-infra`, all gates green, awaiting commit GO. AC-017/018/019/020 → Met.** PR3 = inline `AuditEndpoints` (`GET /api/audit` filter+paginate DESC + `GET /api/audit/verify`) injecting `AuditDbContext` (deviation from plan's MediatR-handler wording, advisor-blessed — a read gains nothing from the pipeline); DTO normalizes v1-lean vs v2-enriched rows; RBAC proven both routes (Auditor/Chair/Sec→200, Member/**Admin**→403, no-token→401); `SeedAuditAsync` seeds both row shapes; also closed a pre-existing `RisksEndpoints.SetMitigationStatus` HTTP-coverage gap (flagged). Gates: BE Api 203, cov 99.66%, format/i18n/Keystone green. NEXT = **PR4 (/audit UI to `ACMP Lists & Registers.dc.html` + FE RBAC fix), GO-gated**. Nothing on main.
