# Memory Index — ACMP

> Compacted 2026-08-12 (2nd). One line per memory; detail lives in the topic files and in the Tamheed
> package. Read the linked file before acting on anything below. Per-PR build history was dropped —
> it lives in `handoff/RESUME.md` §3 and in `progress-entry` rows.

## ★ Active work — PRODUCTION IS LIVE ON `65e45d4`

- **★ START HERE: `handoff/RESUME.md`** — rewritten whole 2026-08-12; the single durably-named entry point. Kickoff prompt = **`handoff/prm-next.md`** (durable too — EDIT it, never add another `prm-*.md`). Every other `RESUME-*.md` / `prm-*.md` is ⛔ superseded.
- **Prod** https://acmp.anas7ammo.dev · `i-04d9717feea79204b` · **always-on**. **UAT** `i-07ac28ac2fedab921` · **stopped when idle**. Both on **`65e45d4`** with **`KEYCLOAK_ADMIN_ENABLED=true`**. They differ ON PURPOSE. 26 real members; budget $100/mo. **Always read instance ids from `describe-instances`, never from notes.**
- ★★ **The handoff said enabling in-app user management was "one variable". It was a 56-COMMIT RELEASE.** Three measurements: SSM pinned `e403e18` on both envs · `/api/session/me` 404→401 · and decisively **`git show e403e18:deploy/keycloak/reconcile.sh` had NO `ensure_admin_client`** (arrived `122f41d`/#237), so the flag alone would have booted a healthy host authenticating as a Keycloak client **that did not exist** — confirmed by the prod reconcile log reading `creating client 'acmp-admin-svc'`. **Read the deployed REVISION, not just the pin, not just the docs.** (`OQ-075`, resolved.)
- ⚠ **My own probe was right for the WRONG REASON and I recorded it before checking.** `/api/session` is the `MapGroup` **prefix**, not a route — it 404s on new code too. Valid form `/api/session/me`. Corrected in `OQ-075` rather than dropped. **Check a probe can distinguish the two states before citing it.**
- ✅ **The ADR-0038 write path has RUN FOR REAL** (`#250`, `src/Acmp.Web/uat-invite-probe.mjs`). **No CI run can do this:** `IIdentityProvider` is registered only when the flag is on and `.env.example` sets it **false**, so even the 7-service e2e stack with a real Keycloak never constructs the adapter; every backend test uses `FakeIdentityProvider`. Measured on UAT: invite 200 · roles 204 · **`AC-093`'s audit row read back OUT OF THE HASH CHAIN** (actor/subject/when + **both** before & after) · chain intact incl. that row. `AC-093` **Met**; `AC-088`/`AC-091` re-evidenced live. The probe **borrows the SPA's token** — one with its own credential proves something about the probe.
- ★ **NEXT SESSION, ITEM 1: LOG IN TO PRODUCTION.** Nobody has since the release. DEF-023 verbatim — healthy containers, valid cert, /api/ 401, and nobody could log in. This release put **two fail-closed middlewares** on prod for the first time (GuestSurfaceMiddleware, PrincipalRevalidationMiddleware) and **a member with no role claim resolves to Guest**, so any prod account lacking a role claim is now confined to the guest surface. uat-login-probe.mjs **cannot** reach prod (UAT fixture passwords) — operator action.
- ✅ **Production's ADR-0038 credential chain IS proven** (PE-281) — client_credentials with the pinned secret → **200 + access_token**; a wrong secret → **401 unauthorized_client**, so the endpoint is genuinely checking. ⚠ **ValidateOnStart only proves the option is POPULATED**, and UAT proved the wiring with a *different* generated secret — prod's was unexercised until this check. ⚠ My first attempt returned 400 invalid_request and that was **my method**, not the credential (PowerShell piping into `curl --data @-`); the tell was the code — 400 is a malformed REQUEST, a bad credential is 401.
- ⚠ **`AC-004`: the idle timeout CANNOT fire for an open tab** (`OQ-076`, needs an operator decision). Live realm `ssoSessionIdleTimeout=1800`, but `automaticSilentRenew: true` and **zero app-side inactivity detection** — each renew resets the SSO idle clock, so only the **10-hour max lifespan** ends an open session. Same shape as `AC-090`. Recorded as **analysis, not measurement**. Also **`AC-090`'s text says "60-minute idle timeout"; the realm says 30.**
- ✅ **`DEF-053` / `DEF-054` Fixed** (#248, #249) — the two gaps the previous handoff exposed. `/session` carries `RequireRole`; `up.sh` asserts `keycloak-config` converged via new `deploy/scripts/assert-oneshot.sh`, **extracted because an inline check could not be TESTED** (`up.sh` runs `gen-secrets.sh`, which clobbers live dev secrets). Each fix **verified to FAIL without the change**: revert `App.tsx` → exactly the 5 denied roles fail; neuter the guard → exactly the 2 dependent cases fail.
- ✅ **The minimum Keycloak grant is ONE role: `manage-users`** — and it is now proven on **two deployed realms**, not just CI (`grant is exactly: manage-users` in both reconcile logs). ⚠ My candidate (`+ view-realm`) was **wider than the truth**. Re-runnable: `scripts/probe-keycloak-grant.mjs`. ⚠ **Sufficiency ≠ minimality** — the CI gate has two legs, and leg 2 strips the grant and REQUIRES the 403.
- ⚠ **Still open for the operator:** `OQ-076` (idle timeout) · `OQ-074` (whose view do Chairman/Secretary "preview"? — ⚠ new: `navModel.ts` ACCESS grants `session` to **guest only**, so they are permitted on a page with **no nav link**) · `OQ-062` is **stricter in code than in the decision** (a *permanent* UAT Webex ban vs "off **until** a UAT space exists" — the exit condition can never be met).
- **★ NEXT:** `OQ-076` decision · then **the 11 Partials as ONE campaign** — approach already agreed: **CI E2E leg now, re-evidence on UAT later**. All Partial for the same reason: `Acmp.Api.Tests` uses a synthetic `TestAuthHandler`, so **no test anywhere drives a real Keycloak token through the role matrix**. `AC-048` (`beforeunload`) is probably unprovable — Partial-with-a-reason is legitimate. Then `DEF-038` residue, `Streams.NameAr`, `DEF-055`.
- ✅ **Partials campaign Tranche A DONE** (#251) — e2e/role-matrix.spec.ts carries the deny matrix through a **real Keycloak token** for the first time. AC-005/AC-007 **Met**. The shared gap was precise: PermissionMatrixTests never crosses HTTP, Acmp.Api.Tests crosses it with a **synthetic TestAuthHandler**, RealJwtAuthTests only covers 401s. **No fixtures needed** — AuthorizationBehavior runs before Validation/Transaction, so a random guid reaches a real role decision. **Every denial is controlled by an allowed role on the SAME route**, or a 403 could just mean the path was wrong.
- ⚠⚠ **DEF-056 — A REFUSED MUTATION IS NOT AUDITED, found by that spec's FIRST run.** Every write endpoint carries a per-endpoint `RequireAuthorization(Policies.X)`, so ASP.NET 403s **before** MediatR and AuthorizationBehavior:39 — the only emitter of Authorization.Forbidden — never runs. SqlAuditSink is innocent (Add + SaveChanges immediately). So AC-006's audit clause is **unmet in the product**, and AV-006's evidence never asserted an audit row, so it had never been checked by anything. Pinned as **	est.fail(), not a skip** — it passes while broken and **goes red the day it is fixed**. Fix = an IAuthorizationMiddlewareResultHandler (one seam, covers endpoints added later).
- ⚠ **The e2e specs were typechecked by NOTHING** — `tsc -b --listFiles` listed **zero** files under e2e/; Playwright transpiles without checking. Closed by 	sconfig.e2e.json (34 files, and the directory already passed with zero errors, **measured first**). ⚠ The plain incremental `tsc -b` reported exit 0 **and zero files** even after the fix — only --force showed the truth. **A green exit from a build that checked nothing.**
- ⚠ **DEF-045 was FIXED and its row said otherwise, and I repeated the row to the operator.** Causes 3 and 4 were already done (8dee3b1; a loud 	est.skip on the sweep cron). I used the stale claim to justify skipping a UAT regression. **The read-the-implementation rule applies to REGISTER ROWS too** — a row is the kind of authority that feels pre-checked.
- **Final state 2026-08-13:** gates 7/7 · **83 Met / 10 Partial / 0 Pending** over 93 ACs · **139 evidenced** · open defects **6 of 56** (`DEF-012` `DEF-038` `DEF-039` `DEF-041` `DEF-055` `DEF-056`) · open questions **`OQ-074` + `OQ-076`**. ⚠ **Newest sha with ECR images = `65e45d4`**; later commits are `.md`/`tamheed-package/` only.
- ⚠ **`Streams.NameAr` on prod is NOT done.** Real table is **`membership.streams`.`name_ar`** — the C# names don't exist in SQL and every module owns a schema.
- ⚠ **`AC-085` leg 1 is evidenced** (`AV-130`) by the **message body**, not a count — on a shared SNS topic no count can discriminate (`AV-118`).

## Shipped, reference only (detail in the package / RESUME §3)

- **ADR-0038 in-app user management** — #234 backend · #235 invite UI · #236 role UI · #237 deploy plumbing · #238 reconcile guard · #246 `/members` area. `FR-156/157/158` done.
- **ADR-0039 `AC-090`** (#239) — was **unsatisfiable by construction**: authorization is token-driven, so a removed role survived a full access-token lifetime (**300s, measured**) and forced sign-out cannot revoke a token in flight. Now per-request revalidation in **middleware**, fail-closed, proven by a **refused request**. ⚠ **An unknown subject must be ALLOWED** — ADR-0004 provisions just-in-time, so failing closed there refuses **every first login**. Real seam = `IPrincipalRevalidator` → `PrincipalVerdict {Allowed,Stale,Expired,Disabled}`; window column = `AccessExpiresAt`. ⚠ **`RolesChangedAt` and `GuestAccess.Grace` are NOT real symbols** — an older note paraphrased them.
- **ADR-0040 `FR-159` guest presenters** (#241 writer + #242 `/session`, `DEC-040`) — window = `ScheduledEnd + 24h`; the operator widened it from the ADR's no-grace recommendation because refusal is per-request and immediate, so no grace 401s a presenter **mid-presentation**. The invite is a **Meetings** use case over ONE Membership port (`IGuestProvisioner`). **`ADR-0021` had already fixed that pattern — read it before designing any cross-module seam.**
- **`DEF-052`: there was NO read-side role gate anywhere** — 14 content groups `RequireAuthorization()` with no policy; every named policy is a WRITE capability. Fixed by `GuestSurfaceMiddleware`: **deny-by-default, not a policy per group**, because an opt-in list silently exempts every route added later. `SC-007` narrowed `AC-059` to exclude Guest — caught by an existing `[InlineData("Guest")]` citing the AC, **not by any gate**.
- **Guest-expiry sweep** (#240) hourly. `IIdentityProvider` injected as `IEnumerable` (demanding it makes the worker unbootable); the member is **still disabled locally** without it. ⚠ Its predicate is `AccessExpiresAt != null && < now` (`ExpireGuestAccess.cs:59`), so **an invited member — who resolves to role `Guest` with a null window — is NOT swept.**
- **`DW-025`'s premise was FALSE and I wrote it: ACMP HAS NO RESCHEDULE.** If one is ever built it MUST call `IGuestWindowWriter`. What shipped is the exposure half: cancel / item removal / slot reassignment close the window.
- **`SC-005`: an ADR named a seam its own tests could not reach.** `ADR-0039` specified `JwtBearerEvents.OnTokenValidated`; the API host authenticates with a **non-JWT scheme**, so that seam is unreachable from any API test. **Check the harness can reach a named seam before approving the ADR.**
- **`DEC-041`'s four items** — `DEF-050` Webex secrets (#243) · `DW-025` (#244) · `OQ-071` CI grant proof (#245) · `OQ-069` Members area (#246). **Two were built against my recommendation** — read `DEC-041` before re-opening either.

## Standing rules & gotchas (read before editing)

- [★ Read the implementation before calling it a defect](read-before-calling-it-a-defect.md) — **nine** instances; never caught by a gate. It has made defects smaller, made one **disappear**, and this session **killed one in three minutes** (the sweep-vs-invitee scare). **Read the predicate, not the doc comment describing it.**
- [★ Controls must DETECT **and** TELL](controls-must-detect-and-tell.md) — now **seven** (DEF-023/030/031/032, OQ-068, DEF-051, DEF-054). The "tell" half is always asserted in a comment rather than tested.
- [★ Verify mechanically, not carefully](verify-mechanically-not-carefully.md) — `entity_upsert` replaces FULL rows; the JSONL flushes on EVERY write, so git HEAD is a live baseline. ⚠ **Also now: a measurement that indicts known-good code is measuring itself** (`grep -c $'\r'` degraded to `grep -c ''`), and `/acmp/*` env params are **LF**, not CRLF.
- ⚠ **Do not accept ONE tool's negative as proof of absence** — the `Grep` tool returned "No files found" for `AccessExpires`, which is in the tree; PowerShell found it instantly. Pairs with [absence needs an untruncated search](absence-claims-need-untruncated-search.md).
- ⚠ **PowerShell joins arrays with SPACES.** `aws ... --output text` returns an **array of lines**; `[IO.File]::WriteAllText(path,$array)` writes one space-joined line and would have **destroyed the SSM env file**. Use `($v -join "`n")` and verify the line count before publishing.
- ⚠ **`gh pr create --body` and `git commit -m` with backticks/quotes break under PowerShell** — always `--body-file` / `-F <file>`.
- ⚠ **`open_question.lifecycle_status` is a CHECK** over `Draft/Proposed/Approved/Rejected/Deferred/Implemented/Superseded/Obsolete` — "Resolved" rolls the whole batch back. `defect.fixed_by` is a **FK**; PR refs go in `custom_attributes`.
- ⚠ **The keycloak container's `docker exec` shell has no `KC_BOOTSTRAP_ADMIN_PASSWORD`** — the entrypoint exports it for its own process only. Read `/run/secrets/kc_bootstrap_admin_password`.
- ⚠ **Windows `python3` cannot see Git Bash's `/tmp`** — pass Windows-style absolute paths when building SSM `--parameters file://` payloads.
- [⚠ Baselines are numbers, not properties](baselines-as-numbers-not-properties.md) — a count-based test on a shared topic can never discriminate.
- [⚠ Immutable history → cleanup is asymmetric](immutable-history-cleanup-asymmetry.md) — deleting a Keycloak user ORPHANS its member rows forever. **Disable, never delete.**
- [A static file cannot configure a live realm](a-static-file-cannot-configure-a-live-realm.md) — `realm-export.json` reaches **fresh stacks only**; `reconcile.sh` is the only seam to prod/UAT.
- [Write the handoff LAST](write-the-handoff-last.md) — it found `DEF-053`/`DEF-054` last time. Stamp superseded files with ⛔ immediately.
- [Commit package writes before git ops](commit-package-writes-before-git-ops.md) · [Tamheed stale .lock + PID reuse](tamheed-stale-lock-pid-reuse.md) · [Tamheed data repair](tamheed-data-repair.md) · [migration history](tamheed-migration-reverted.md)
- [Localhost CI hides load races](localhost-ci-hides-load-races.md) · [Git push hang → `gh auth setup-git`](git-push-hang-fix.md) · [Run CI gates locally pre-push](ci-gates-run-locally-pre-push.md) · [Always stage .claude/memory in commits](always-stage-claude-memory-in-commits.md)
- [Coverage & E2E mandate](coverage-and-e2e-mandate.md) — ≥95% FE+BE + adversarial E2E. ⚠ **The Playwright suite is NOT UAT-only** (`e2e.yml` runs 7 services with a real Keycloak per PR) **but runs with `KEYCLOAK_ADMIN_ENABLED=false`**, so it never touches the ADR-0038 write path.
- [E2E local run (non-destructive)](e2e-local-run-nondestructive.md) — **`-p acmpe2e` ONLY**, never `npm run e2e:up`. · [Dev-stack rebuild pitfall](dev-stack-rebuild-pitfall.md) — **never `up --build`** the long-lived dev stack.
- [Exact design fidelity + visual loop](exact-design-fidelity-visual-loop.md) · [A green suite is not a look](a-green-suite-is-not-a-look.md) — ⚠ the throwaway harness must import **only** the stylesheets the real route imports.
- [Design: breadcrumb spacing](breadcrumb-spacing-rule.md) · [i18n parity ≠ completeness](i18n-parity-not-completeness.md) · [Web visual-verify cache busting](web-visual-verify-cache-busting.md)
- ⚠ **`.adm-detail-card` has no padding and clips its children** — anything opening a popover needs `.adm-card-overflow`. · **`userEvent.setup()` installs its own clipboard stub** — define a clipboard spy *after* it.
- [User prefers simple English](user-prefers-simple-english.md) · [Phase prompt Standard Footer](phase-prompt-standard-footer.md) · [Install the schedule, not just the daemon](install-the-schedule-not-just-the-daemon.md) · [Arabic rename is a grammar rule](arabic-rename-grammar-not-substitution.md) · [A clean scan must prove it had a subject](scan-must-prove-it-had-a-subject.md) · [Guard the property, not the value](guard-the-property-not-the-value.md) · [The suite assumed a fresh database](e2e-assumes-a-fresh-database.md) · [The feature is often already half-built](check-before-building.md)
- ⚠ **AC id cells in markdown tables must stay BARE** (`| AC-001 |`, never bolded) — bolding breaks the Keystone G-PROGRESS gate.
- ⚠ **A new advisory can turn `main` red with no code change** — `GHSA-q939-rpr3-3284` (SSH.NET) blocked every merge mid-session. "It's only tests" is how a blocking gate becomes advisory.
- ⚠ **A compose `secrets:` entry whose FILE IS MISSING fails the WHOLE stack** — any mounted secret must be written **unconditionally** by `gen-secrets`.

## Completed ladder P1–P19 + PH-5 (reference only — do not re-open)

- [PH-5 / SL-025 — UAT is live and LOGIN WORKS](ph5-sl025-uat-live.md) · [PH-5 AWS deployment](ph5-aws-deployment.md) · [P19 release readiness + D-23](p19-release-readiness.md) · [P18 deployment](p18-deployment.md) · [P17b decision-issuance UI](p17a-test-hygiene.md) · [P17/P18/P19 slice notes](next-p17-p18-p19.md)
- [P16 hardening B2b/B3/B4](p16-hardening-b2b-b3-b4.md) · [P16b CI security gates](p16b-ci-security-gates.md) · [P16a audit & vote crypto](p16a-audit-vote-crypto.md)
- [P15 Research & Knowledge](p15-research-knowledge-plan.md) · [P15 audit remediation](p15-audit-remediation.md) · [P15f/g search](p15f-search-progress.md)
- [P13 Webex](p13-webex-integration-plan.md) · [P13 audit remediation](p13-audit-remediation.md) · [P13 recording upload](p13-recording-upload.md) · [Webex coverage-gate exclusion](webex-coverage-gate-async-exclusion.md)
- [P12 Dashboards & Reports](p12-dashboards-reports-plan.md) · [P11 ADRs & Invariants](p11-adrs-invariants-plan.md) · [P10 Risks/Deps/Traceability](p10-risks-deps-traceability-plan.md)
- [P9 Voting](p9-voting-plan.md) · [P8 Actions](p8-actions-plan.md) · [P7 Minutes & Decisions](p7-minutes-decisions-plan.md) · [P6a meeting IA](p6a-meeting-ia-plan.md) · [P6b notifications IA](p6b-notifications-ia-plan.md)
- [Audit slice (AC-017)](audit-slice-literal-ac017.md) · [Topic Prepare UI (D-15)](topic-prepare-ui-gap-d15.md) · [Keystone package migration](keystone-package-migration.md) · [Keystone gap remediation](keystone-migration-gap-remediation.md)
