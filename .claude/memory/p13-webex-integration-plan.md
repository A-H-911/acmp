---
name: p13-webex-integration-plan
description: P13 Webex integration (Phase 2) — app/adapter surface + WS0 worker-container split + governance COMPLETE & CI-green on branch feat/p13-webex-integration; only operator live sandbox remains.
metadata:
  type: project
---

**P13 Webex integration (Phase 2)** — branch `feat/p13-webex-integration` (committed, not yet merged). Plan file: `~/.claude/plans/peaceful-knitting-nest.md`. Governed by ADR-0023 (accepted) + ADR-0024 (proposed, worker).

**DONE + tested + validator-green (WS1/WS2/WS3/WS3b/WS5-6):**
- **WS1** `INotificationSink` + `NotificationDispatcher` (single `INotificationChannel`, fans out to sinks) — InApp became a sink; **zero changes to the ~15 callers**.
- **WS2** new module `src/Modules/Integrations/Webex` (`Acmp.Modules.Integrations.Webex`, depends ONLY on `Acmp.Shared`, ArchUnit-enforced): typed `WebexApiClient`, `AdaptiveCardBuilder` (v1.3, ≤80KB, EN/AR), `WebexNotificationSink` (Enabled-gated, **committee-wide events only** — MeetingScheduled/AgendaPublished/MinutesPublished/DecisionIssued; per-recipient fan-out collapses to ONE space post), `WebexSendJob` (429→`Schedule(Retry-After)`), testable `IWebexJobScheduler` seam (generic; wraps Hangfire `IBackgroundJobClient`).
- **WS3** first **anonymous** endpoint `POST /api/webex/webhook` + `WebexSignatureFilter` (**HMAC-SHA1** default per Webex docs — NOT SHA256; SHA256/512 configurable; raw body + 5-min replay); `IMeetingWebexWriter` (ADR-0021 write seam) + `Meeting` Webex fields (`WebexMeetingId` indexed) + migration `Meetings_AddWebexFields`; recording processor (`GET /recordings/{id}`→attach, idempotent).
- **WS3b** OAuth (`/api/webex/oauth/start|callback`, Admin-gated, **single-use state cookie** — security-review fix); `WebexToken` store (AES-GCM, own `webex` schema, migration `Webex_TokenStore`, added to `MigrationRunner` conditionally) + transparent refresh; `ScheduleMeeting` enqueues create for online meetings via Shared `IWebexMeetingProvisioner` (Meetings registers a no-op default; adapter overrides when enabled). **Dropped speculative `IMeetingIntegration` seam (YAGNI).**

**Key decisions:** Hangfire-native retry (no outbox); **space-only delivery** (`CommitteeRecipient` has NO email → per-user DM/invitations/email-attendance deferred = **D-14**); adapter **not registered at all when `Webex:Enabled=false`** (clean AC-071, dodges Hangfire-off-in-Testing DI landmine); AC-067–072 added (criteria + audit, all **Met**).

**Verification:** 710 Application (+39 Webex unit) / 143 API (+3 webhook integration AC-069/071) / 188 Domain / 41 ArchUnit (Webex boundary) — green. Keystone validator all-7 PASS. `dotnet format` applied. 2 migrations.

**WS0 DONE + CI-green (2026-07-07):** `Acmp.Bootstrap` library = single composition root (`AddAcmpModules` + `AddAcmpHangfireStorage`) BOTH hosts call → no DI drift. New `Acmp.Worker` Generic Host runs `AddHangfireServer()` + the recurring action-reminder sweep (moved off API); **API now enqueue-only** (server removed, `IBackgroundJobClient` kept). API owns EF migrations; worker waits (`compose depends_on: api healthy`). `Dockerfile.worker` + compose `worker` service + opt-in (`--profile ngrok`) ngrok ingress (reserved `NGROK_DOMAIN` optional); Webex env in `.env.example` for both. **`SystemCurrentUser` deliberately SKIPPED (YAGNI, advisor-grade evidence):** every worker job either isn't an `IAuthorizedRequest` (sweep) or hardcodes its `system:*` audit actor (`system:action-reminders`/`system:webex`), and `CurrentUserService` is null-safe headless → a SystemCurrentUser changes zero behaviour. `Acmp.Worker`/`Acmp.Bootstrap` are hosts/wiring → ArchUnit enumerates assemblies by explicit type-ref, so auto-exempt (no ArchUnit edit). `CompositionRootTests` (4) make composition drift a build-time failure. ADR-0024 → **Accepted**. 722 App (+4 smoke) / 143 API / 41 Arch / 188 Domain green; format clean; validator all-7.

**OAuth fix + ngrok wired (2026-07-07, sandbox prep):** OAuth consent endpoints `/api/webex/oauth/start|callback` were JwtBearer-`AdminConfig`-gated = un-completable (browser redirect can't carry a bearer; ACMP is bearer-only, no session cookie) → Webex callback always 401'd. Made **anonymous + single-use `state` cookie** (already present) + **token-link audit** (INV-005); ADR-0023 updated; regression test (callback reachable w/o auth → 400 not 401). Hardening path (deferred): SPA-mediated `/start` (authed XHR → authorize URL) once an admin "Connect Webex" screen exists. ngrok: compose tunnels `web:80` (nginx serves SPA + proxies `/api`) via reserved `${NGROK_URL}` (dev `acmp.ngrok.dev`/prod `acmp.ngrok.app`); bot+space+OAuth creds+authtoken in git-ignored `deploy/.env` (WEBEX_ENABLED still false until operator flips). **ROTATE the pasted bot token + ngrok authtoken (in chat history).**

**LIVE SANDBOX RUN (2026-07-07, `acmp.ngrok.dev`):** PROVEN live — worker container (Hangfire server+dispatchers+dequeue/retry = definitive ADR-0024 proof), AC-069 webhook (HMAC 200/401 over ngrok, worker dequeued), OAuth setup-key gate (`/start` no-key→404), bot→space post 200 (after operator added bot `acmp-dev@webex.bot`). **Real bug caught (mocks missed): recording fetch used the BOT token → Webex 403 "missing scopes" (recordings are user-scoped). FIXED:** `IWebexApiClient.GetRecordingAsync(accessToken, recordingId, ct)` now takes the OAuth host token; `WebexWebhookJob` resolves it via `IWebexTokenService.GetValidAccessTokenAsync` (graceful null → drop, no 403 storm). 43 Webex app tests green. **Live stack STILL RUNNING old worker image — redeploy (rebuild worker+api) to run the fix live.** Direct-access-grants OFF in realm (can't password-grant a token to trigger a card programmatically → AC-067 card needs SPA login → schedule meeting).

**REMAINING (open):** operator-run **live Webex sandbox** validation (`docker compose --profile ngrok up` + ngrok URL + Webex creds) → confirm space card renders + recording webhook attaches → flip AC-067/072 "live pass" notes; a coverage-collection run (`check-coverage.mjs` ≥95% — not run, cost); WS5/6 doc polish (`functional.md` FR-056/057→Met, status-report regen) as P13 formally closes. See [[keystone-migration-gap-remediation]], [[ci-gates-run-locally-pre-push]], [[always-stage-claude-memory-in-commits]].

---
**LIVE VALIDATION + FOLLOW-UP BACKLOG (2026-07-07, latest commit `83fd995`, branch `feat/p13-webex-integration`, NOT pushed).** Live stack ran on `acmp.ngrok.dev`. **Proven live:** worker container (ADR-0024), AC-067 (card in space — operator saw it), AC-069 (webhook HMAC 200/401), AC-072 (meeting auto-create MTG-2026-002 + WebexMeetingId/JoinUrl write-back). **By test only:** AC-068 (429 reschedule), AC-071 (disabled→in-app). **AC-070 NOT closed** — root cause: **no Webex webhook is registered** (`GET /webhooks` count:0); the app never self-registers, and recordings are host-owned so the webhook must be created with the **secretary OAuth token** (not the bot token); webhooks aren't retroactive + Webex recording processing is delayed. **3 real bugs the live run caught that mocks couldn't:** (1) recording fetch used bot token→403 (fixed `fbd2559`: OAuth token); (2) meeting-create date `.ToString("o")` 7-digit fraction→Webex 400 (fixed `37873e6`: `yyyy-MM-ddTHH:mm:ssZ`); (3) bot not in space→404 (operator added bot). Dual-language cards done (`83fd995`, EN+AR, order by DefaultLanguage) — **not yet redeployed live**. Keycloak: registered ngrok URLs on `acmp-web` client (running + realm-export); set `acmp-admin`/`AcmpAdmin#2026`. Config all in git-ignored `deploy/.env` (bot/room/OAuth/token-key/setup-key=`8ba39603061c5b3338ead6f6da6a8970eab05e9b016dfa49`/ngrok authtoken). SPA login works on `localhost:8088` (http); ngrok SPA login blocked by https-page→http-keycloak mixed content (the likely card→login cause, unproven).

**NEXT-SESSION PLANNING BACKLOG (plan-only, operator-approved list):**
1. Show the Webex meeting JoinUrl in the meeting-view screen (meeting detail DTO likely already has it; SPA `features/meetings` detail must render a "Join Webex" link). Ties to #5.
2. Dual-language cards **Arabic-first** then English (already order-configurable via `Webex:DefaultLanguage`; set `ar` as default OR change the code default — confirm hardcode vs config).
3. **ngrok as the ONLY URL** (no localhost) + retest card→login. BIG infra: serve Keycloak over https on the ngrok domain — nginx proxies `/auth/*`→keycloak, `KC_HOSTNAME=https://acmp.ngrok.dev/auth` + proxy headers, rebuild web with `VITE_OIDC_AUTHORITY=https://acmp.ngrok.dev/auth/realms/acmp`, match API authority, update nginx CSP. Files: `deploy/nginx/default.conf.template`, `deploy/docker-compose.yml`, `deploy/keycloak/realm-export.json`, web build args.
4. **Auto-register the `recordings/created` webhook** (option 1, the AC-070 gap): on OAuth callback (after StoreFromExchange) register via the **OAuth token** — POST /webhooks {resource:recordings,event:created,targetUrl:WebhookPublicUrl+/api/webex/webhook,secret:WebhookSecret}; idempotent (list-then-upsert); also register on startup if a token exists; consider FR-057 polling fallback for air-gapped. New registrar in the Webex module + a POST /webhooks method on the API client (OAuth token).
5. Better **Join URL field UX** on `/meetings/new` (related to #1): Webex-enabled+online → read-only "auto-generated by Webex" (show URL post-create on the meeting view); Webex-off or non-Webex → manual entry (Zoom/Teams); in-person → hide. Maybe a "Generate Webex meeting vs use my own link" toggle. FE form redesign + maybe a backend flag; note the create job currently OVERWRITES a manual JoinUrl.
6. Document all CURRENT Webex use-case flows (card-on-event; online-meeting auto-create; recording attach; OAuth consent) and BRAINSTORM enrichments (meeting-start reminder to space; update/cancel meeting→update Webex meeting; recording link surfaced in minutes/decision; vote-open/decision cards; interactive card actions RSVP/ack; per-user DM + attendance auto-populate — both email-gated/D-14; Webex-Assistant transcript→AI summary Phase 3; Webex bot slash-commands).

---
**PLAN REFINED + PR-A BUILT (2026-07-07, still on `feat/p13-webex-integration`, NOT committed).** Devil's-advocate review corrected the first draft: (a) **card→login is an APP bug not mixed-content** — `authConfig.ts` uses sessionStorage (per-tab) + `onSigninCallback`→`/` discards the deep link; a card opens a NEW tab (empty session) → login → lands home. Operator chose the **minimal** fix (preserve deep-link route; keep sessionStorage). (b) `/auth` proxy would collide with the SPA `/auth/callback` → use **`/kc/`** for Keycloak; KC26 needs `KC_HTTP_RELATIVE_PATH`. (c) point-5 status widened to `{enabled,canAutoCreate}`. Plan split into **PR-A (UX: pts 1/2/5/6), PR-B (pt4 webhook auto-register, closes AC-070), PR-C (pt3a app card→login fix + pt3b ngrok-only KC-https for DEV)**. Full plan: `~/.claude/plans/peaceful-knitting-nest.md`.

**PR-A DONE (code + BE-tested, NOT committed):** pt2 = `WebexOptions.DefaultLanguage` default `en`→**`ar`** (one-liner; sink already threads it; +ordering test). pt1 = `MeetingOverview.tsx` renders an https-guarded "Join Webex" link from `meeting.joinUrl` (already on DTO/TS) + `.mt-join` CSS + test. pt5 = new **`GET /api/webex/status`→{enabled,canAutoCreate}** (`WebexEndpoints.cs`; canAutoCreate uses existing `IWebexTokenService.HasTokenAsync`; resolves token svc ONLY when enabled per AC-071) + `api/webex.ts` `useWebexStatus` hook + `SchedulePage.tsx` join-field UX (InPerson=hide / online+auto=read-only note / online+manual=input; submit sends manual joinUrl only when online && !auto) + tests. pt6 = new `docs/domain/webex-use-cases.md`. **BE gates green: solution builds 0 errors; AdaptiveCardBuilder 5/5, WebexStatusApiTests 4/4.** FE vitest/build NOT run here (node_modules partial — no local vitest/@vitejs); run in CI/operator env. i18n keys added to BOTH en/ar (`meetings.overview.joinWebex`, `meetings.schedule.joinAutoNote`). **Pending: FE gates + full `dotnet test` + `dotnet format` + coverage, progress-log/acceptance-audit update, commit — all on operator GO. PR-B + PR-C not started (need operator live env).**
