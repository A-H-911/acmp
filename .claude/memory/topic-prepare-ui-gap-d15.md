---
name: topic-prepare-ui-gap-d15
description: "HIGH-PRIORITY defect — Topic Prepare (Accepted→Prepared) has no SPA UI, so the agenda pool is always empty and the intake→agenda core loop is broken in-product. Tracked as D-15."
metadata: 
  node_type: memory
  type: project
  originSessionId: c8e659d5-b6c4-496f-b58d-bc1e02886d1e
---

**Operator-reported bug (2026-07-07): agenda-builder "Search topics" shows nothing.** Root cause (proven, not inferred): the agenda pool is `GET /api/topics?status=Prepared`, but **the SPA never calls `POST /api/topics/{id}/prepare`** — only `useAcceptTopic`/`useReturnTopic` are wired (in the kanban). `Topic.MarkPrepared` requires status `Accepted`; with no UI trigger, **no topic ever reaches `Prepared`**, the pool is empty **by construction**, and the search filters an empty list. It's a missing-workflow (W4 "Prepare a topic"), not a search bug. Backend endpoint EXISTS (`TopicEndpoints.cs`, AC-035/BL-036) — shipped backend-only.

**Why AC-035 read "Met":** its live leg in E2E `core-loop.spec` does *"UI accept → **direct-HTTP prepare**"* — the test calls the endpoint directly, masking the absent UI. Transition + audit + eligibility are Met **at the API**; the **UI affordance is missing** → arguably Partial. I added a dated caveat to the AC-035 note in `docs/validation/acceptance-audit.md` (flagged for operator; didn't unilaterally re-flip).

**Design↔domain tension** (operator decided: KEEP the Prepared gate): the **domain package makes Prepare mandatory** (entity-lifecycles §1, W4; owner completes prep materials/affected-systems/risks before the committee's time; publish hard-guards `RequireStatus(Prepared)`). The **design files OMIT it** — kanban has 5 buckets (no `Prepared`; Accepted & Prepared both map to `accepted` in `topicMeta.ts`), timeline jumps Accepted→Scheduled, agenda pool labeled "Ready to schedule". Behavior SoT = package → keep the gate; design-update-owed logged.

**Tracked as D-15 (Tech-debt, Open, HIGH PRIORITY)** in `docs/execution/deferred-work-register.md` (v1.2.0). **Recommended fix (frontend-only, NO ADR), advisor-vetted:**
- **Tier 1 (core):** `usePrepareTopic` + a "Mark prepared" button on an `Accepted` topic detail (`TopicDetail.tsx` `.dt-actions` — already has disabled Add-to-agenda/Edit slots). **Gating = show-and-enforce** (render for any Accepted topic; backend 403s — the Owner is often a plain Member, so a role gate would hide it from the right person). **Invalidate THREE keys:** backlog + `['topics','prepared']` + `['topics','detail',key]`. Reword agenda empty-state (`meetings.poolEmpty`) to point to the action. i18n en/ar + tests.
- **Tier 2 (coherence):** a "Prepared" badge on kanban cards (Accepted & Prepared share the `accepted` bucket → otherwise invisible). NOT a 2nd action, NOT a bucket split (design = 5 fixed).
- **Tier 3 (optional):** notify the Secretary on prepare (`PrepareTopicHandler` only audits; `INotificationChannel` seam exists via ScheduleMeeting).
- **Flags (raise OQ if pursued):** no "un-prepare"/defer-from-Prepared exists (mis-prepared topic stuck until scheduled); design-update-owed.

**★ OPERATOR PICKED TIER 3 (2026-07-09, after P13 merged via PR #99).** Finalized full-stack build plan (all recon done, ready to execute — NO ADR needed):

*Backend (Secretary notification, W4):*
1. NEW `src/Modules/Topics/Topics.Application/Internal/TopicNotifications.cs` — mirror `DecisionNotifications`: `const string CategoryTopicPrepared = "TopicPrepared";` + `public static Func<string,NotificationMessage> TopicPrepared(string topicKey)` (bilingual EN/AR title+body, DeepLink `/topics/{key}`).
2. `PrepareTopic.cs` handler — inject `ICommitteeDirectory` + `INotificationChannel`. After the audit: `var secs = await _directory.GetActiveMembersInRoleAsync(AcmpRoles.Secretary, ct);` then publish `TopicNotifications.TopicPrepared(topic.Key)(s.UserId)` per secretary, **skipping the actor `sub`** (no self-noise, mirrors CreateAction W13). `GetActiveMembersInRoleAsync` is the PROVEN headless "who is the Secretary" seam (ICommitteeDirectory.cs:15). `NotificationMessage` = single `RecipientUserId` (no role broadcast) → fan out per-recipient like `DecisionNotifications.FanOutAsync`.
3. Handler test: mock ICommitteeDirectory (2 secretaries) + INotificationChannel → assert 1 publish/secretary + skip-self when a secretary prepares their own.

*Frontend:*
4. `api/topics.ts` — `usePrepareTopic()` mirroring `useAcceptTopic` (topics.ts:187): POST `/topics/${topicId}/prepare`, invalidate `['topics','backlog']`+`['topics','prepared']`+`['topics','detail',key]`.
5. `TopicDetail.tsx` `.dt-actions` (line 131, has 2 disabled "coming soon" Buttons) — render "Mark prepared" `Button` when `topic.status === 'Accepted'` (show-and-enforce; 403-handled inline).
6. `topicMeta.ts`/`Kanban.tsx` — "Prepared" badge on cards with status Prepared (they share the `accepted` bucket, topicMeta:42).
7. `AgendaBuilder.tsx` — reword the pool empty-state to point at "Mark prepared".
8. i18n EN/AR: `topics.prepare.*` + notification category `TopicPrepared` label + kanban Prepared badge + agenda empty-state ([[i18n-parity-not-completeness]] — add category to BOTH locales).
9. FE tests: topics.test.ts (usePrepareTopic), TopicDetail.test, Kanban.test (badge).

*OQ + docs:* new `OQ-###` (missing un-prepare/defer-from-Prepared → mis-prepared topic stuck until scheduled; applied default = defer); flip D-15 → Done; acceptance-audit AC-035 caveat resolved (UI affordance now exists); progress-log + status-report entries; Keystone validator on docs. Gates: BE `dotnet test`+format+check-coverage (≥95%/file — cover the new handler branch + TopicNotifications); FE vitest+i18n+build. Live VR = no-reference composition (design omits Prepare — design-update-owed).

**★ BUILT & GATED — DONE (2026-07-09, Tier 3, branch `fix/d15-topic-prepare-ui`, 22 files, awaiting commit GO).** Shipped exactly the plan above: FE `usePrepareTopic` (invalidates backlog+`['topics','prepared']`+detail — the pool key is confirmed at `api/meetings.ts:198`, the line that unblocks the agenda builder) + "Mark prepared" button in `TopicDetail.tsx` DetailHeader (`status==='Accepted'`, show-and-enforce, inline 403 via `.dt-prepare-err`) + Prepared `Tag tone="info"` badge in `Kanban.tsx` Card + `notifPresentation` `TopicPrepared` type (icon `checkCircle`, verified valid) + reworded `meetings.poolEmpty.body` + i18n `topics.prepare.*`/`notif.type.topicPrepared` EN+AR. BE new `Internal/TopicNotifications.cs` + `PrepareTopicHandler` injects `ICommitteeDirectory`+`INotificationChannel`, fans out to `GetActiveMembersInRoleAsync(Secretary)` skip-self. **Honest AC-035 close (advisor point):** `core-loop.spec` prepare leg switched from direct-HTTP to **clicking the button** (removed `captureBearer`/`prepareTopic`/`request`/`bearer`), and new `TopicApiTests` `POST /{id}/prepare→204` proves the DI seams resolve (a mock can't). Tests: handler fan-out/skip-self, API prepare, FE hook (3 invalidations)/button(show/hide/403)/badge/notifType. Docs: **D-15→Done**, **OQ-049** (no un-prepare; `DeferTopic` excludes Prepared so a mis-prepared topic waits until scheduled), AC-035 caveat resolved (verdict Met on API transition+audit+locally-tested affordance), progress-log + status-report v1.4.0. **Gates ALL green incl. e2e:** BE 1217 tests/cov 99.69%/format; FE 845 tests/per-file cov ≥95%/i18n 1480/build; Keystone OK; **Playwright `core-loop.spec` run locally against the live stack — GREEN** (button click drives prepare 204 → topic in agenda pool → published). **How the e2e ran (operator-instructed, non-destructive):** the dev stack bakes `KEYCLOAK_AUTHORITY` = ngrok URL (`deploy/.env`) so the e2e login can't run against it; the e2e needs `.env.example`'s local `keycloak.localhost:8085` authority + fresh volumes. Ran it via an **isolated compose project** (`docker compose -p acmpe2e --env-file deploy/.env.example up --build --wait`) after `docker compose stop` on the dev stack (freed ports, kept volumes), then `-p acmpe2e down -v` + restarted dev on `deploy/.env`. Operator volumes untouched. **⚠ persisted-volume trap re-learned:** never `up --build` the dev stack with `--env-file .env.example` — it recreates SQL/KC against mismatched passwords (volume was init'd with `deploy/.env`) → SA login-failed/unhealthy; restore with `--env-file deploy/.env`. Design-update-owed stands (design omits Prepare). Live VR skipped = no-reference composition.

Discovered mid-session while finishing [[p13-webex-integration-plan]] PR-A. See [[exact-design-fidelity-visual-loop]] (INV-014), [[ci-gates-run-locally-pre-push]], [[p13-recording-upload]] (P13 now closed+merged).
