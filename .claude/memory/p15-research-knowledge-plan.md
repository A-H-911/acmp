---
name: p15-research-knowledge-plan
description: P15 Research & Knowledge — GO-gated 8-slice plan; Keystone import DEFERRED (D-05); global search + convert IN scope per operator; OQ-034/INV-002 live for search.
metadata: 
  node_type: memory
  type: project
  originSessionId: a67f04de-cda2-4d8f-aca8-f9541bc16a18
---

P15 (PH-2 remainder) = two new modules **Research** (schema `research`) + **Knowledge** (schema `knowledge`) + a **global search** surface. PH-2 = **AC-less**, traces to FRs. Prompt locked in [[keystone-package-migration]]'s follow-up-prompts.md P15 section. Plan-first, GO-gated (same discipline as P10/P11).

**Entities.** Research: `ResearchMission` (RMS-, aggregate) → child `Finding` (FND-), `Recommendation` (REC-). Knowledge: `Document` (DOC-, wiki, Draft→Published→Archived, **immutable version snapshot per save**), `Template` (TPL-, TargetType Topic/ADR/MoM/Document/Action, Active→Deprecated, versioned). Permissions: `Document.Manage` (AiO=Owner), `Template.Manage` (Sec/Chair/Admin).

**Operator scope decisions (2026-07-12):**
1. **Keystone import (FR-112) DEFERRED out of P15 → future** (D-05, no ADR — DEC-016/ADR-0007 optional-companion decision intact; committed PR #106). FR-111 keeps the package-ref *field* only; NO `IResearchImporter`. FR-114 delivered **partial** (package-ref + manually-linked artifacts; NO import-status/imported-items widget).
2. **Global search built NOW** (operator override of my index-only rec) → **AC-060 + AC-061 enter P15 scope** (were Pending). ⚠ **OQ-034 (Arabic word-breaker recall spike) + INV-002 become live**: default = SQL Server FTS (ADR-0011); if recall <80% the fallback is app-owned OpenSearch behind `ISearchIndex` — but **OpenSearch = 2nd datastore → INV-002 needs an ADR**. Run the spike at P15f; STOP + raise ADR before adding any datastore.
3. **Manual Finding/Recommendation entry** = yes (create dialogs are a no-reference composition, guardrail #14 — design frames them as imported).
4. **Convert flows IN P15** (operator override of my defer rec): Mission→Topic + Recommendation→Topic/Decision (W16), cross-module write seam (reuse P11e `ITraceabilityWriter` precedent).
5. **Versioning (FR-117)** = immutable `DocumentVersion` snapshot table + client-side markdown diff.

**Slice ladder (8):** P15a Research backend (convert-capable aggregates, CRUD, audit, topic-link, RBAC) → P15b Research UI (register+detail+manual create dialogs, `isResearch` design) → P15c Research convert (W16) → P15d Knowledge backend (Document versioned + Template) → P15e Knowledge UI (wiki list + versioned page + diff + Template mgmt, `isWiki`+template design) → P15f Search backend (SQL FTS `ISearchIndex`, OQ-034 spike FIRST, AC-061) → P15g Search UI (global grouped results, AC-060) → P15h Template wiring (picker→pre-fill at create-time, FR-120).

**FR coverage:** FR-111/113/114(partial)/115 (Research) · FR-116/117/118/119/120 (Knowledge) · AC-060/061 (search). FR-112 deferred.

**Reuse (ponytail):** module scaffold (Governance/Dependencies pattern = Domain/Application/Infrastructure/Api), register+detail+create-dialog FE (P10b Risks/P11d Invariants), `MarkdownEditor` (DV-04), `TraceabilityPanel`+`ITraceabilityWriter` (P10e/P11e), version-preserving pattern (Minutes/ADR), audit sink, RBAC policy pattern.

**Design (INV-014):** `/ACMP product context/ACMP Research & Knowledge.dc.html` (576 lines; `isResearch` missions register+detail, `isWiki` list+serif page, template). Read directly at each UI slice.

**Status:** plan GO'd by operator; **P15a in progress** on branch `feat/P15a-research-backend`. Docs-refresh + P15-defer landed as PR #106. See [[p11-adrs-invariants-plan]] for the module-slice precedent.

---

**P15c COMPLETE (2026-07-13).** P15c-1 backend MERGED (#113 `bab2adc`, target-owns/ADR-0021, no new ADR). **P15c-2 frontend BUILT + all gates + live pixel-VR PASS** (branch `feat/p15c2-research-convert-frontend`, plan `~/.claude/plans/nothing-clear-for-me-steady-cerf.md`, awaiting commit GO). FE-only. `ConvertToTopicDialog` (pre-filled topic form, no-ref guardrail #14) → `POST /api/topics/from-research` → navigate to new `TOP-`; header Convert button **Completed-only** (backend 409s otherwise; design shows Active → behaviour wins, INV-014 flag); per-rec Convert on Accepted. `TraceabilityPanel` mounted on the mission in a two-col `1fr 320px` grid (FR-114). Header `TOP-` xref from the incoming edge with a **plain-indicator fallback** for older missions (FR-115; create-time edge only, backfill still deferred). **Edge-driven "Converted → TOP-" chip** (reads the rec's own outgoing Informs edge → self-heals a failed best-effort mark-converted, which is fire-and-forget/non-fatal). `ResearchMission` added to `GRAPH_FOCUS_TYPES` + `useMission` cold-resolver in `ImpactGraphPage` → graph opens on a mission incl. refresh (FR-113). `TokenInput` extracted `SubmitTopic`→`components/ui`. 939 FE tests/cov 99.64%/i18n 1640/oxlint/build green; `e2e/p15c-convert-vr.spec.ts` EN-light+AR-dark pixel-faithful. **FR-113/114/115 → Met.** Fast-follows: FR-115 backfill + UpdateMissionDraft edge; D-19 flaky DecisionPage test. Next = **P15d/e Knowledge** → f/g search (OQ-034/INV-002) → h. ⚠ live VR = safe `-p acmpe2e` dance only (stop dev → `up --build --wait -p acmpe2e` → run → `down -v -p acmpe2e` → `start -p acmp`); the dev `ngrok` container needs `up -d ngrok` to recreate after (stale network on plain `start`).

---

**P15d-1 Knowledge backend (Document) — BUILT + all gates green, awaiting commit GO (2026-07-14).** Branch `feat/p15d1-knowledge-document`, plan `~/.claude/plans/nothing-clear-for-me-steady-cerf.md` (operator-approved after devil's-advocate; I inverted requirement-vs-design priority on TargetType in the first draft — corrected). First of TWO P15d PRs (Template = P15d-2). New `knowledge`-schema module = 3 projects mirroring Research exactly. **Document** (DOC-, Draft→Published→Archived, `byte[] RowVersion`→409): FR-117 = owned immutable `DocumentVersion` snapshot (Version/Title/Body/SavedAt/SavedBy) appended per content save (Create/Edit→Version++); Publish/Archive = status-only (no version). `Category` free-text (FR-116 requires it; **domain-model §391-397 OMITS it** — added per requirement, doc-gap logged). Title/Body bilingual LocalizedString (en===ar). `OwnerUserId` = Keycloak-sub **attribution only** — Document isn't topic-scoped so CapabilityHandler AiO can't resolve (verified `CapabilityHandler.cs:40` fires only for `ITopicScopedResource`) → Chair/Sec effective (OQ-050 precedent, like ADRs). CQRS + `/api/knowledge/documents/*` (mutations `Policies.DocumentManage` — **already existed + registered**, verified `Policies.cs:33-34`/`AuthorizationRegistration.cs:46-47`, no auth wiring). ★ registrations: `KnowledgeDbContext` in MigrationRunner (P11a gate), `AddKnowledgeModule`+MediatR assembly in AcmpCompositionRoot, MapKnowledgeEndpoints, ArchUnit boundary + `Knowledge_should_not_depend` fact, InMemory swap in AcmpWebApplicationFactory; migration `Knowledge_Init` (documents+document_versions+key_counters). **Independently re-verified (not sub-agent trust):** build clean, Domain 233/Arch 49/Api 219/Application 845 pass, format clean, coverage no-Knowledge-file-<95% (the 3 flagged = pre-existing shared infra `AmbientTransaction*`/`MinioFileStore` covered by the Docker-gated Integration suite, green on CI). Built by a general-purpose subagent from the plan + Research template; deviations resolved: string OwnerUserId (Keycloak sub, not Guid — else API-test subs 500), API 409 via terminal-state conflict (InMemory can't enforce rowversion — Research precedent), KnowledgeRoles=Chair/Sec only. **OQ-051 logged: FR-119 `{Topic,ADR,MoM,ResearchMission}` vs domain-model §403 `{Topic,ADR,MoM,Document,Action}` TargetType — decide in P15d-2 (Template).** Next = P15d-2 Template (reuses this scaffold; key-counter already per-prefix so `NextTemplateKeyAsync` needs no schema change) → P15e Knowledge UI (`isWiki` design).

---

**P15d-1 MERGED (PR #115 `424e03e`).** **P15d-2 Knowledge backend (Template, FR-119) — BUILT + all gates green, awaiting commit GO (2026-07-14).** Branch `feat/p15d2-knowledge-template`, same plan (operator-approved; devil's-advocate + advisor pass). A **mirror** onto the P15d-1 scaffold — Template rides the existing `KnowledgeDbContext` + `Knowledge.Application` assembly, so **NO** MigrationRunner/composition-root/ArchUnit/factory change (ArchUnit 49→49; verified by the unchanged test count). **Template** (TPL-, Active→Deprecated, `byte[] RowVersion`→409): `Name` bilingual LocalizedString (en===ar); **`Body` = single Markdown string** (only Name bilingual — I first over-mirrored Document's LocalizedString Body; **advisor caught it**, corrected to plain string per domain-model §403 + en===ar makes 2nd body locale pointless); `TargetType` enum; **`Version` = plain counter, NO snapshot table** (FR-117 versioning is Document-only; FR-119 asks only for edit). Ops: `Create`→Active v1, `Edit`→Version++ (Name+Body only, **TargetType immutable**), `Deprecate`→terminal soft-delete (permanent retention). **OQ-051 → Approved:** `TargetType` = FR-119's `{Topic, Adr, MinutesOfMeeting, ResearchMission}`, NOT §403 sketch `{…,Document,Action}` — requirement is the contract (LLM01), §403 self-contradictory (§404 omitted Action, neither listed ResearchMission), Document/Action YAGNI + string-enum-addable-with-no-migration; spelling mirrors Traceability `ArtifactType`; domain-model §402-404 reconciled in-PR. CQRS: Create/Edit/Deprecate/GetTemplateByKey/GetTemplatesRegister (+ **`targetType` filter = the P15h seam**) → `/api/knowledge/templates/*`; mutations `Policies.TemplateManage` (**already registered** — Chair/Sec/**Admin**). ★ **RBAC backstop `KnowledgeRoles.TemplateManage` INCLUDES Administrator** (unlike DocumentManage={Chair,Sec}) — matches its matrix row `AuthorizationRegistration.cs:46`; else a valid Admin passing the endpoint policy is wrongly 403'd at the MediatR boundary (devil's-advocate catch). Migration `Knowledge_AddTemplate` (one `templates` table; TPL- rides per-prefix `knowledge_key_counters`, no counter-schema change). **Independently re-verified:** build clean, Domain 241(+8)/Arch 49/Api 226(+7)/Application 854(+9) pass, format clean (BOM + file-scoped-ns on the generated migration — the CI-only gotcha), coverage no-Knowledge/Template-file-<95% (the 6 flagged = pre-existing Docker-gated shared infra, green on CI), global 98.69%. Admin-can-create proven at API (the one behavioural diff from documents); 409 via terminal-state conflict (edit/deprecate a Deprecated template). **FR-119 → Met (backend).** Next = **P15e Knowledge UI** (`isWiki` list+serif page + Template mgmt, INV-014 live pixel-VR) → P15f/g search (OQ-034 spike FIRST + INV-002) → P15h template pre-fill (reads targetType seam).

---

**P15e Knowledge UI (wiki + templates, FR-116/117/119) — BUILT + all gates green + live pixel-VR PASS, awaiting commit GO (2026-07-14).** Branch `feat/p15e-knowledge-ui`, plan `~/.claude/plans/elegant-imagining-tome.md`. One combined slice (operator chose combined over split). **4 operator forks:** combined slice · wiki=tree-with-Category-as-space · markdown=**marked+dompurify** (vetted lib) · templates=**standalone /templates** (not the design's admin-only home — Chair/Sec are managers). **Adversarial re-verify caught sub-agent errors** (untrusted): flat route `/templates` not `/knowledge/templates`; Admin ALREADY had a honest-empty `templates` ComingDataTab (removed it); `@types/dompurify` is deprecated (v3 self-typed — don't add); author name RESOLVABLE via `useMembers()` join on `Member.keycloakUserId` (not a gap); `TraceabilityPanel` supports `traceType="Document"` (confirmed); no AC maps to FR-116/117/119 (traceability-matrix→ADR-0002/TEST-041). New `components/ui/MarkdownView.tsx` = `DOMPurify.sanitize(marked.parse(md))` allowlist + rel=noopener hook; 7 tests incl cure53 XSS-strip (built + verified by lead). `api/wiki.ts`+`api/templates.ts` (lead). The ~25 UI files built by a **general-purpose subagent** (delegate-clone-I-verify, operator-chosen; subagent hit a transient ENOTFOUND mid-run, resumed via SendMessage), then **every gate independently re-run by lead**. Wiki: `Category`-grouped tree (Published→all, Draft→managers dot-marked, Archived excluded = OQ-052) + serif reading view (author via keycloakUserId join, read-time, tags, MarkdownView body, TraceabilityPanel links) + split md/preview editor (Save=PUT edit, mirror to both locales) + version-history panel (from `Versions[]`, diff→P14) + create dialog; no-ref lifecycle (New/Publish/Archive/status badge, manager-only). Templates: `.gTpl` table (real enums) + no-ref create/edit form (TargetType Select disabled on edit) + Deprecate row action; removed Admin templates tab. Fixed a nit: template Name Input maxLength 512→256 (backend cap). **Gates (lead-verified):** build clean, oxlint clean, **1016 FE tests/132 files/per-file ≥95%**, i18n 1736 keys. **Live VR PASS:** `e2e/p15e-knowledge-vr.spec.ts` (5 screens × EN-light+AR-dark, isolated `-p acmpe2e`, KC bootstrap `ChangeMe_KC#2026`) — wiki reading/editor/history + template table/form pixel-faithful to `ACMP Research & Knowledge.dc.html` + `ACMP Administration.dc.html`. ⚠ VR seed uses PUT for doc edit (not POST — only publish/archive are POST); seed accumulates across runs (down -v for a clean tree). **FR-116/117/119 → Met (UI).** Flags: History+status manager-only (could relax to all readers); TraceabilityPanel substitutes the design's compact linked card. Next = **P15f/g global search** (OQ-034 Arabic word-breaker spike FIRST + INV-002 live — STOP+ADR before any 2nd datastore) → **P15h** template pre-fill (reads targetType seam).
