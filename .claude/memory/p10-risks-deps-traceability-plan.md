---
name: p10-risks-deps-traceability-plan
description: "P10 (Risks + Dependencies + Traceability) GO-gated slice plan, operator scope decisions, and progress."
metadata: 
  node_type: memory
  type: project
  originSessionId: af139742-8b3a-46f5-b25e-be819e747112
---

**P10 = Risks + Dependencies + generic Relationship trace edges + dashboards.** It is really 2–3 phases compressed — the design's own `ACMP Usage Map.dc.html` splits it as **P8** (Actions **+ Risks**) and **P11** (Dependencies & Traceability). Same `.dc.html` files govern regardless of phase label (guardrail #14). Pre-code reviewed by the `ecc:architect` subagent + advisor (both GO); the advisor tool was briefly unavailable mid-session then came back.

**GO-gated slice plan (mirrors P8a–d / P9a–b cadence):**
- **P10a — Risks backend** — DONE, branch `feat/P10a-risks-backend`, green (849 tests). `Risk` aggregate + owned `Mitigation` in a new `Risks` module (copied the Actions pattern). See progress-log P10a.
- **P10b — Risks register + detail UI** — DONE & **MERGED** to main (squash `c424324`, PR #78, all 4 CI checks green, 2026-07-03). FE-only clone of the P8b `features/actions/*` template consuming the merged `/api/risks`. `/risks` 8-col `gRsk` register (status+exposure filters, key/exposure/status server sorts, 3×3 7px heat mini-grid + critical badge), routed `/risks/:key` detail (facts + LARGE 3×3 matrix + legend + prose Mitigation-plan card), `CreateRiskDialog` (Title/Likelihood/Impact/Owner/**Linked-topic REQUIRED**/Mitigation-plan → POST). Gates green: tsc/oxlint clean, 582 vitest (37 new, axe-clean), i18n parity 991, per-file cov 100% on 5 new files. **Reconciliations flagged (design→update):** Linked-topic required (Option A, no System sentinel); no immutability warning (risks mutable); Likelihood/Impact default Medium/High; Accepted→info/Escalated→danger tones no-reference; "Mitigation plan" = prose card NOT a table; saved-view dropped (actions precedent). **One open item:** live screenshot-compare vs the `.dc.html` (heat grid + matrix, RTL+dark) pending a running-stack pass — flagged, not claimed. See progress-log P10b.
- **P10c — Relationship (traceability) backend + panel API + AC-029 widening** — **DONE (branch `feat/P10c-relationships-backend`, pre-merge, local gates green).** New **`Traceability` module** (docs/30's Search&Traceability home) = self-describing `Relationship` edge `(Source/Target Type,Id,Key,Title)`+RelType+`IsActive` soft-delete, schema `traceability`, migration `Traceability_Init`. **No Artifact registry → ADR-0019 amends ADR-0008** (edges self-describe; matches docs/30 §2.1 FK-less SQL). Features: `CreateRelationship` (RBAC new `Policies.TraceabilityLink`=Chair/Sec; self-loop guard; audit `Relationship.Created`), `DeactivateRelationship` (soft-delete, audit `Relationship.Deactivated`), `GetArtifactRelationships` (panel by `(ArtifactType,PublicId)`→{outgoing,incoming} 1-hop active; returns `relType`+`direction` enum names, NO English inverseLabel = i18n on FE). **AC-029 widened**: new Shared seam `ITraceabilityLinks.DecisionHasDownstreamEdgeAsync` OR'd with `IActionLinkDirectory` in `IssueDecisionHandler`; **CURATED downstream set** (decision=source of recorded-as/resolves OR target of implements; upstream/lineage decided-by/derived-from/supersedes EXCLUDED so a decision can't self-satisfy via its own topic — ASM-P10c-2; gate query ≠ panel query). Superset → **AC-029 stays Met**. **Title snapshotted on edge** (AC-062 needs title display; key-only would push a resolution seam into P10e) — accepted staleness. **Owner-AiO create deferred** (RBAC-only; no ABAC handler for arbitrary artifact types). **Panel = explicit edges only**; projecting existing soft-refs deferred → later slice, **read-time composition not write-through**. Gates: build+all tests green (20 new App + 6 new Api + 1 DecisionsApi; 8 IssueDecision ctor sites threaded `ITraceabilityLinks` substitute), format clean (fixed migration CRLF), per-file cov 99.73% (13 new files measured). **AC-062/063 stay Pending → P10e** (assert FE panel display). Docs: ADR-0019, docs/10 row 32, ASM-015, progress-log P10c, acceptance-audit. Reviewed by architect+advisor (both GO-WITH-FIXES, all folded; the AC-029 curated-set was the advisor's sharpen over both my outgoing-only and the architect's source-OR-target — both leaked). **Advisor's final catch (docs-only 2nd commit dd89298):** the create validator does NOT check the docs/30 §2.2 per-RelType endpoint-type matrix (enum+self-loop only) → a malformed edge (e.g. Decision--RecordedAs-->Meeting) could satisfy the AC-029 gate; trusted-actor input, full 16-type matrix DEFERRED → ASM-015 point 6. **CI: all 4 checks green on PR #79** (e2e flaked once on the unrelated S6b agenda-DnD dispatchEvent timeout — re-ran green, NOT a P10c regression; stack booted + PKCE login worked so migration/DI fine). **MERGED to main — squash `b4ba206`, PR #79, all 4 CI checks green (2026-07-03).** Next = P10d Dependencies BE.
- **P10d — Dependencies backend** — **DONE & MERGED to main (squash `edaf912`, PR #80, all 4 CI checks green incl. e2e no-flake, 2026-07-03).** New **`Dependencies` module** (docs/README core BC), schema `dependencies`, migration `Dependencies_Init`. `Dependency : AuditableEntity` = self-describing directed edge `(From/To Type,Id,Key,Title)`+`Kind`+`Status`+`Note?`+**RowVersion** (ADR-0018); `Status{Open→Resolved|Removed}` carries soft-delete (no separate IsActive). Module-local enums `DependencyEndpointType{Topic,Action,System,Decision}` / `DependencyKind{DependsOn,BlockedBy,Blocks,RelatesTo}` / `DependencyStatus`. Features: CreateDependency (RBAC `Policies.DependencyCreate` — pre-existed P4, no new wiring; DPN per-module key counter mirror of RiskKeyGenerator; self-loop guard; audit `Dependency.Created`), Resolve/Remove (by PublicId; Open-only domain guard→409; 404 unknown; audit), GetDependencyByKey, GetDependenciesRegister (paged; Kind/Status/BlockedOnly filters; **Removed excluded by default**; DB-translatable), **GetDependenciesForArtifact `{outbound,inbound}`** = the read-time-composition seam for P10e. `IsBlocker=Kind∈{BlockedBy,Blocks}&&Open` computed in DTO, not stored. **OQ-046 = the crux: operator asked "why 2 tables — unify?"; both reviewers said KEEP-SEPARATE + read-time composition (store DPN once, NO write-through mirror, NO unify).** Unify rejected (sparse nullable cols for ~12 non-governed RelTypes; folds core BC into cross-cutting Traceability or makes Decisions depend on Deps for AC-029; diff lifecycle⇒diff aggregate; docs/11 §A.3 settled). Recorded: OQ-046 (docs/42), ASM-016 (docs/41, 7 points), fixed the **stale docs/30 §1.1 "maintained in sync" sentence** (was the source of the "duplication?" instinct) + edited docs/11 §Dependency (Kind 4-set drop Impacts, Note→string, IsCrossStream derived-read-time). **P10f MUST UNION the Dependencies table** (recursive-CTE traversal won't see dep edges via Relationship). **STREAM FACT CORRECTION (verified P10f, 2026-07-03):** the earlier "Topic.AffectedStreamIds Guid[]" note is WRONG — the real field is `Topic.AffectedStreams` = `IReadOnlyCollection<string>` (stream CODE strings); `Topic : IStreamScopedResource`. The master `Stream {Code, LocalizedString Name}` list lives in the **Membership** module (streams scope member WRITE-ABAC, docs/10 §E.1); members are assigned via `MemberStreamAssignment`. **ONLY Topic carries streams** — Decision/Action/Risk/System/Dependency carry NONE. ⇒ FR-095 cross-stream is only computable for Topic↔Topic edges from real data; every edge touching a non-Topic endpoint has no stream to compare (the graph node's stream is unknown). Reconciliations (ASM-016): IsCrossStream NOT modelled (FR-095 = P10e read-time from stream SETS), Note=plain string, endpoint types not matrix-validated, Member/Reviewer AiO dormant (no FE caller yet — topic-sourced create CAN satisfy `ITopicCapabilityResolver` once P10e supplies `ITopicScopedResource`; only action→action resolver-blocked), resolve/remove reuse Dependency.Create policy. Gates (hand-verified not just build agent): build clean; **950 tests green** (Domain 168/App 608/Api 121/Arch 36/Integration 17; +10 Domain +22 App +8 Api +4 Arch new); cov exit-0 global 99.75% 0-files-<95%; format clean. **No AC flips** (backend; FR-094/095/098 data stood up; AC-062/063 Pending→P10e; live→P17). Next = **P10e** Deps register + Traceability panels UI. See progress-log P10d.
- **~~P10d — Dependencies backend (superseded plan)~~**: (`Dependency` governed edge DPN; mirrors a `depends-on` Relationship per docs/30 §1.34; blocked-work + cross-stream).
- **P10e — Deps register + Traceability panels UI** — **DONE (branch `feat/P10e-deps-traceability-ui`, pre-merge, local gates green, awaiting merge GO).** FE-only, mirrors P10b/P8b. Operator scope calls (2026-07-03): **panel = detail-page ASIDE only** (standalone group-by-type Relationships page + SVG graph both → P10f); **register "New dependency" ships limited+flagged** (Topic/Action pickable). Delivered: `/dependencies` register (From·Relation·To·Blocked·Status; Relation+Blocked-work filters; key/status server sorts; global link/blocked counts via count fan-out; "New dependency" Chair/Sec + disabled "Open graph" stub), routed `/dependencies/:key` **edge** detail (NO panel — it's an edge not an artifact, architect ruling; replaced the PlaceholderPage + added breadcrumb leaf), shared **`TraceabilityPanel`** on Topic/Decision/Action/Risk detail = **AC-062** (MERGES `/api/traceability` + `/api/dependencies/artifact` read-time into Upstream/Downstream/Related; navigable links via `otherType→route` map, routeless types = plain text; deps fetched only for the 4 DependencyEndpointType artifacts → Risk shows rel edges only), + Chair/Sec create dialogs = **AC-063** (`CreateDependencyDialog` composed to design `dependency` form; `CreateRelationshipDialog` NO-REFERENCE; one shared `ArtifactPicker` over Topic/Action/Risk = only 3 of 16 types have FE list hooks; contextual-first From/Source pre-seed). **Reconciliations (flagged):** Cross-stream col+filter OMITTED (FR-095 deferred, not em-dash theater — architect corrected my "render —" lean); NO far-artifact status chip (ADR-0001) but dep "Blocked" pill (IsBlocker, ADR-0019); direction axis = curated FE heuristic in `traceMeta` (design only specs 4 dep kinds + 7 far-types, not 16 relTypes); Decision/System not pickable (no list hook). **Architect corrections folded:** RelationshipDirection is structural Outgoing/Incoming (relMeta→dir map needed regardless → group-by-type is LESS code but we ship group-by-DIRECTION aside per operator "aside only" call, which is legal); dep edges DO carry IsBlocker (surface it). Gates: tsc/oxlint/vite build clean, **638 vitest (+56)**, axe-clean, i18n parity 1090 keys (deps.*+trace.* both locales, all enum values by hand), per-file cov ≥95% (100% api/meta/panel/dialogs). **AC-062/063 → Partial** (live real-stack → P17 per G-TRACE). **Visual-verify DONE** (unlike P10b) — drove the LIVE stack (real Keycloak PKCE + SQL; deps seeded via `POST /api/dependencies` which self-describe per ADR-0019, no topics needed) + screenshot-compared register/edge-detail/create-dialog vs `.dc.html` in EN-light + AR-RTL-dark: all match, RTL mirrored + dark clean, Cross-stream absent. NOT the AC-062/063 Met evidence (full create-via-UI→both-panels round-trip = P17). VR harness pattern = `e2e/vr-sweep.spec.ts` (loginAs + captureBearer + setMode + shot to `vr-out/`; KC admin pass in `deploy/.env` NOT `.env.example`). Files: `src/Acmp.Web/src/api/{dependencies,traceability}.ts`, `features/dependencies/*` (register/page/dialog/depMeta), `features/traceability/*` (panel/picker/dialog/traceMeta), 4 detail-page mounts, App.tsx routes, nav/breadcrumbs.ts, i18n en/ar. Next = **P10f** (SVG impact graph — MUST UNION both edge tables + the standalone group-by-type panel).
- **P10f — Impact graph (transitive) — SPLIT INTO 2 PRs (architect call).** Operator chose to BUILD NOW. **Operator OVERRODE both reviewers' FE-only lean → FULL-STACK:** (1) **backend subgraph endpoint** (server-side BFS), (2) **build FR-095 cross-stream NOW**; one-page IA + register-graph-deferred stayed as blessed. Third sub-decision put to operator = **stream model = (a) Topic-scope** (OQ-047).
  - **PR1 (backend) = DONE & MERGED to main (squash `64930f1`, PR #82, all 4 CI checks green — backend/compose/frontend/e2e, no flake, 2026-07-03).** `GET /api/traceability/graph/{type}/{id}?depth=1..3` → `{focusType,focusId,depth,nodes[],edges[],partial}`, read-all. `GetImpactGraph` q+handler in Traceability module; pure `ImpactGraphComposer` (BFS) wired to real reads. **READ-TIME composition, NOT a cross-schema CTE** (ADR-0020 clarifies ADR-0019's "recursive CTE" phrase — 2-schema CTE breaks ADR-0001). **2 new `Acmp.Shared` ports (primitive DTOs, enums don't leak):** `IDependencyArtifactReader` (Dependencies.Infra impl reuses existing `GetDependenciesForArtifact` via ISender) + `ITopicStreamReader` (Topics.Infra impl, `Topic.AffectedStreams` codes). **#1 CI trap AVOIDED:** seam DTOs are Shared-owned (reusing `Dependencies.Application.DependencyEdgeDto` would turn `Traceability_should_not_depend_on_other_modules` ArchUnit RED) — all 36 arch tests green, no boundary rule changed (P8d `IActionLinkDirectory` precedent). **#2 trap:** visited-set keyed on type-NAME string+id (ArtifactType vs DependencyEndpointType overlap by name only). Guards: `MaxNodes=60` (OQ-018)→partial, depth clamp 1–3, per-node try/catch→leaf+partial (never blanks). **System** (dep-only, ∉16 ArtifactTypes) skips rel read + dead-ends; unmapped type skips dep read. `blocked`=node touches active blocker dep edge (IsBlocker); far-node lifecycle status NOT returned (ADR-0001). **FR-095 = Topic-scope (partial):** isCrossStream true only both-ends-Topic + disjoint non-empty stream codes. Gates: **972 tests green (+24 new**: 15 composer via handler+faked ports, 2 dep-reader, 2 topic-stream, 3 graph API incl. real Topic→Decision→Action walk), format clean (fixed UTF-8-BOM CHARSET on every new file), **per-file cov 100% on all 7 new files, global 99.75%**. Docs: **ADR-0020**, **OQ-047** (inherit model (b) stays OPEN on multi/zero-topic semantic), docs/30 §4.1+§6.2, progress-log+acceptance-audit. **AC:** FR-096 data stood up (verdict lands w/ PR2 FE); FR-095→Partial(Topic-scope); no flips. Reviewed by architect (2 rounds, GO-WITH-FIXES all folded) + advisor (caught focus-identity gap).
  - **PR2 (FE) = NEXT.** Bespoke in-app SVG graph page `/traceability/:type/:key` (depth 1–3, blocked toggle, **roving-tabindex** keyboard nav not role=application, List-tree fallback, group-by-type aside) consuming the endpoint via `useTraceGraph` (single TanStack query — the FE need NOT re-BFS, the backend returns the whole graph); **focus-identity:** warm=panel passes {id,title} via router state, cold=per-type detail fetch by key. Reconciliations to flag: role→roving-tabindex, status chip omitted, cross-stream badge shows CODE not localized name, reduced-motion gate on edge-dash. Register/panel "Open graph" wiring. EN/AR+RTL+dark+axe + visual-verify vs `.dc.html` (RTL edge/node alignment `scaleX(-1)` edges vs logical `inset-inline-start` nodes = top visual risk). The design's graph is bespoke in-app SVG (NOT Tarseem — Tarseem stays Phase-2 alt).
- **P10g — Risk-exposure + dependency dashboards** — **DONE (branch `feat/P10g-risk-dep-reports`, pre-merge, local gates green). THE LAST P10 SLICE.** FE-only. Replaces the `/reports` PlaceholderPage with a focused **Risk & Dependency reports** page that REUSES the card renderers/tokens of `ACMP Dashboards & Reports.dc.html` (matrix/stat/bars). Full Reports IA (Dashboard/Reports toggle, role tabs, 6 view-tabs, Export, filters row, other-domain cards) **deferred to P12** — page = **no-reference composition** (guardrail #14, the design's Reports view has only 1 buildable risk card + 0 from-wire dep cards). **Operator chose "Fuller": BUILD by-stream now** (override of both reviewers' defer-lean, like P10f). Six cards, ALL numbers composed **client-side** from 3 existing reads (`useRisksRegister`+`useDependenciesRegister`+`useBacklog`, each pageSize=500, NO server clamp verified — no new backend): risk-exposure 3×3 **count matrix** (coloured by **severity** L×I ≤2/3-4/≥6 = success/warn/danger, verified to reproduce the design's 9 authored `mcell` zones EXACTLY; active-scoped=exclude Closed+Accepted), risk stat, dep stat (Blocked=isBlocker), deps-by-relation bars, **Risk by stream** + **Cross-stream/blocked deps by stream** (join risk.subjectId/dep.{from,to}Id → linked **Topic.streams**; **`includeClosed=true` topics fetch** = the trap catch, an active risk can hang off a closed topic; multi-stream topic counts under EACH stream so **Σbars ≥ distinct KPI** which is never the bar sum; non-Topic/empty-stream excluded; stream **CODE** shown, no localized name on wire — `StreamRef` rides only on Member). Reads are **authenticated-only** ("any authenticated committee member", no role policy) so Administrator with `reports:view` won't 403. **OQ-047 scoped partial-advance:** by-stream adopts inherit-from-topic model (b) for **reporting aggregation only**; P10f's per-edge Topic↔Topic semantic unchanged (record as addendum NOT closure). Files: `features/reports/{reportAgg.ts,ReportsPage.tsx,reports.css}` + 2 tests; App.tsx route (PlaceholderPage→ReportsPage); en/ar +37 `reports.*` keys. Gates: tsc/oxlint clean, **702 vitest (+18** — 11 reducer incl. money-path by-stream props, 7 component: all 4 states + retry-refetches-3 + axe), **per-file cov 100% on both new files**, i18n parity 1167 each. **AC:** no Must flips — **FR-138** (per-stream risk/dep report, Should/Ph2) → **Partial**; **FR-095** advanced in reporting, stays Partial Topic-scope; **FR-135/136/137 role dashboards (AC-064/065/066) stay Pending → P12** (this is the Reports view NOT the role Dashboard tab). **LIVE VR PASS** (`e2e/p10g-reports-vr.spec.ts`, mirrors p10f-graph-vr): real KC login + seed 6 risks reproducing the design matrix + a blocking dep → open `/reports` → EN-light + AR-dark screenshot-compared **pixel-faithful to `ACMP Dashboards & Reports.dc.html`** (3×3 matrix layout + severity-zone tinting all 9 cells + stat tiles + bars + P12 note; **RTL Probability axis mirrors, danger cells track the axis** = top visual risk resolved; dark tokens + AR labels clean). Counts read >6 only because both VR tests share the DB (harness artifact, spec asserts nothing = no CI interference; no committed spec counts risks/deps). **KC gotcha CONFIRMED AGAIN: fresh `e2e:up` → run playwright with `KC_BOOTSTRAP_ADMIN_PASSWORD=ChangeMe_KC#2026`. Risk create REQUIRES an owner (`ownerUserId`=member `publicId`, `ownerName`=fullName; ApiMember has NO keycloakUserId — pick `members.find(m=>m.role==='Secretary')`).** Reviewed architect (2 rounds) + advisor, all GO-WITH-FIXES folded. CI on PR #84 was 4/4 green pre-VR-spec; re-push adds the VR spec (2 more e2e captures). **MERGED to main — squash `9eb6941`, PR #84, all 4 CI checks green (backend/compose/frontend/e2e incl. the P10g VR spec), 2026-07-04.** **★ P10 COMPLETE ★** — all 7 slices (P10a Risks BE, P10b Risks UI, P10c Traceability BE, P10d Deps BE, P10e Deps+Trace UI, P10f impact graph BE+UI, P10g risk/dep reports) MERGED to main; main green + deployable. Next phase per the roadmap = **P11+ (Governance ADRs/Invariants · Research · Knowledge · Diagrams) or P12 Reporting** (which will build the full `/reports` shell: Dashboard/Reports toggle, role dashboards FR-135/136/137=AC-064/065/066, the 6 view-tabs, Export, filters — P10g's `features/reports/*` card renderers + `reportAgg` reducers are the reusable seed).

**P10-review (2026-07-04, branch `feat/P10-review`):** post-merge adversarial audit of all 7 slices → GO on correctness/architecture, NO-GO on fidelity-reconciled. Remediation slice fixed 7 MAJOR (Risks filter order Status→Owner→Exposure; Deps blocked-toggle accent+34px `.fchip` not red/30px + relation-arrow horizontal `arrowRight`/`.dep-rel-back` not vertical; Traceability aside `openGraph` key + matched-node `hit` border `.ig-node--hitBlocked/hitCross`; **ADR-0019/0020 indexed in adr/README.md**) + MINOR cluster incl. **Arabic-gender matrix impact-axis** (new masculine `reports.impactLevel.*` منخفض/متوسط/عالٍ vs feminine probability `reports.level.*`). Backend: impact-graph BFS `partial` no longer halts sibling branches (+regression test); Decisions ArchUnit forbidden-list += Traceability+Actions. New icons `ban`+`arrowUpRight`. **Accepted/deferred (guardrail 11):** AiO dead-path B4 (fails closed → future authz slice, don't touch 248-matrix), app-wide "Showing X of Y"+register copy, reports KPI headline R3→P12, dep-detail Impact prose D16 (don't invent). Gates green: 972 BE (+1) / 702 FE / format / per-file cov ≥95% / i18n 1172 / ArchUnit 36. No AC flips. Live 4-mode VR re-capture recommended pre-merge (static parity done). Next = merge GO → P11.

**Operator scope decisions (2026-07-03):** start P10a ✓; **build the full SVG graph now** (not deferred); **build risk/dep dashboards in P10** (not folded into P12). No permission-denied `.dc.html` state exists for risks/deps/traceability — do NOT invent one.

**Key P10a facts to carry forward:** `RiskExposureScale` (Severity=L×I 1–9 → band ≤2/≤4/≤6/9) is the single source — P10b's heat grid consumes the projected `Exposure` band, never re-derives. New `Risk.Accept` policy (Chairman/Secretary, no AiO) is distinct from `Risk.Manage`. Escalate fans out to Secretary+Chairman (BL-135). Design→behavior reconciliations flagged for the design update: Description optional (form omits it), "Mitigation plan" seeds the first Mitigation (Type=Reduce), band named "Exposure" not "Severity". Related: [[p9-voting-plan]], [[ci-gates-run-locally-pre-push]], [[exact-design-fidelity-visual-loop]].
