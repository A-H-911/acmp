---
name: p13-audit-remediation
description: P13 (Webex + recording) adversarial audit → 2 GO-gated remediation slices. Slice 1 (recording tab + MinioFileStore real coverage + honest AC rows) DONE + gated on branch fix/p13-audit-slice1, awaiting commit GO. Slice 2 (dormant Webex hardening) planned, not started.
metadata:
  type: project
---

**Adversarial audit of the merged P13 slice** (Webex + meeting recording, main `b6bb185`) found — no CRITICAL, but: 3 MAJORs + minors. Verdict = **conditional GO** (v1 ships with `WEBEX_ENABLED=false` — the Webex fixes are dormant; the adapter isn't even registered when disabled). Remediation split into **two GO-gated slices** (advisor-driven): Slice 1 = Webex-independent + live-validatable now; Slice 2 = dormant Webex hardening (unit-tested; live Webex validation owed to operator). Plan file: `~/.claude/plans/peaceful-knitting-nest.md`.

**Findings.** M1 `WebexTokenProtector` derives its AES key `SHA256(TokenEncryptionKey ?? "")` with a shipped `CHANGE_ME` placeholder + **no fail-closed validation** (enable Webex w/ placeholder → tokens encrypted under a public key). M2 `WebexOAuthClient.PostAsync` throws `WebexApiException` on any non-2xx, so `WebexTokenService.GetValidAccessTokenAsync` **throws** on a revoked refresh token (400) instead of returning null → jobs dead-letter instead of no-op (contradicts its own AC-072 contract). M3 (test-integrity) the **production** `MinioFileStore` (behind AC-073/074) sat behind a coverlet exclusion whose "not wired into any v1 flow" comment was **false** → zero automated coverage. M4 acceptance-audit over-claimed **AC-068** (dead-letter/monitor half untested — only a Hangfire `[AutomaticRetry(3)]` attribute) + **AC-069** ("idempotent" only age-based replay tested) + **AC-073** (real adapter faked+excluded). Minors: m5 token-refresh not audited (INV-005 literal); m6 `.mt-rec-webex` no `:focus-visible`; m7 webhook replay guard bypassed when `Created` null; m8 HMAC over re-encoded string not raw bytes; m9 429 Retry-After ignored by webhook/create jobs (only WebexSendJob honors it); m10 upload `accept="video/*"` broader than server allowlist; m11 dead i18n keys `recording.uploading`+`recording.loading.body`; m12 `.mt-rec-foot` divider not in design. NOTE: `deploy/.env` holds real secrets on disk — **gitignored, never committed** (verified) — rotate before go-live.

**Operator decisions:** two slices · M3 = **Testcontainers-MinIO** (not the lazy honest-comment) · M4 = correct rows **+ add the missing tests** · AC-068 = **real in-memory Hangfire dead-letter test**.

**SLICE 1 DONE + all gates green (branch `fix/p13-audit-slice1`, awaiting commit GO):** M3 — removed `**/MinioFileStore.cs` from `coverlet.runsettings` `ExcludeByFile` + rewrote the comment; new `tests/Acmp.Integration.Tests/MinioFileStoreTests.cs` (Testcontainers.Minio 3.10.0, mirrors `SqlBackstopFixture`; one container, `IAsyncLifetime`, covers bucket create+skip / presign / exists found+object-missing+bucket-missing / delete — passed against a real container). M4-docs — AC-073 evidence upgraded, **AC-068 → Partial** (plain, NOT `**bold**` — Keystone G-PROGRESS rejects bolded verdicts), AC-069 wording. m6/m10/m11/m12 recording-tab fixes. Gates: BE 339 files/99.69%/per-file ≥95% (MinioFileStore now counted), format clean, Integration 18; FE vitest 845 (one `DecisionPage` timeout **flaked under concurrent-container load** — green in isolation), i18n 1478, build clean; Keystone OK.

**SLICE 2 DONE + all gates green (branch `fix/p13-audit-slice2`, awaiting commit GO):** M1 fail-closed key = new `WebexOptionsValidator` (`IValidateOptions`) wired via `AddOptions().Bind().ValidateOnStart()` (plain Configure never validated) — Enabled needs a real ≥16-char non-`CHANGE_ME` key; **broke 3 enabled-Webex test hosts (WebexWebhook/OAuth/StatusApiTests) which didn't set a key → added `Webex:TokenEncryptionKey` to each** (the correct fail-closed consequence; CompositionRootTests already had one). M2 `catch (WebexApiException ex) when (ex.StatusCode is 400 or 401) → null`, 5xx/429 bubble. m5 injected `IAuditSink` → `Webex.OAuthTokenRefreshed` on refresh only. m7 drop null-`Created`. m8 **deferred w/ honest comment** (UTF-8 decode↔encode is lossless for real Webex payloads; don't perturb the security path). m9 mirrored WebexSendJob 429-reschedule in both jobs (injected `IWebexJobScheduler` → updated both job-test helper ctors). **AC-068 → Met**: `WebexJobDeadLetterTests` (real `Hangfire.InMemory` server, `[AutomaticRetry(1, DelaysInSeconds=[0])]` throwing job → Failed state via `IMonitoringApi`; `[CollectionDefinition(DisableParallelization=true)]` — JobStorage.Current is global). **AC-069 → Met**: reprocess-idempotency + no-timestamp-drop tests. m13 **gated off** (deleting `WebexRecordingTests` would drop `WebexRecording` coverage — `DtoContractCoverageTests` doesn't cover it; OAuth-test naming cosmetic). Gates: BE build clean; Domain 188/Arch 41/App 796/Integration 18/API 188 green; coverage 99.62% per-file ≥95% (`WebexEndpoints` 82/85=96.5%, residual 3 = unreachable disabled branch + 2 record-decl lines); format clean; Keystone OK. **★ Gotcha: `check-coverage.mjs` merges EVERY `coverage.cobertura.xml` under repo root — stale `./coverage-out/` + root `./TestResults/` (line numbers from a prior run) create PHANTOM uncovered lines after any edit that shifts line numbers → false per-file failure. `rm -rf coverage-out TestResults` (both gitignored/untracked, CI fresh-checkout is immune) before trusting the gate.** FE untouched this slice. Only operator secret rotation + live Webex validation remain.

**Operator residual:** rotate Webex bot token + OAuth client secret + `WEBEX_WEBHOOK_SECRET` + `TokenEncryptionKey` + ngrok authtoken in `deploy/.env`. See [[p13-webex-integration-plan]], [[p13-recording-upload]], [[ci-gates-run-locally-pre-push]], [[always-stage-claude-memory-in-commits]].
