---
name: p8-actions-plan
description: "P8 Actions module — 4-slice plan, GO'd forks, and current progress (P8a+P8b+P8b2a+P8b2b done & merged; next P8c Hangfire)."
metadata: 
  node_type: memory
  type: project
  originSessionId: 5d6ffc09-c171-48a2-9586-476c992f4111
---

**P8 = Actions module**, plan-first + GO-gated, sliced like P7. Design SoT = `ACMP Lists & Registers.dc.html` `isActions` register + detail. Canon: docs/11 (aggregate), docs/12 §7 (lifecycle), W13/W14/W22, docs/10 rows 14–15.

**4 slices:** P8a Actions backend · P8b register+detail UI · P8c Hangfire reminders/escalation + Admin job dashboard · P8d AC-029 decision-link gate.

**P8a — DONE & MERGED to main (squash `26dd245`, PR #63).** New Actions bounded context mirroring Decisions. Aggregate is **`ActionItem`** (NOT `Action` — avoids the `System.Action` BCL clash); DbSet/table/key stay Actions/`ACT-YYYY-###`. 6-state lifecycle Open→InProgress↔Blocked→Completed→Verified + Cancelled; **overdue derived** never stored. **SoD-1 (AC-012/013)** enforced in `VerifyActionHandler` (verifier≠owner≠completer → audit denial + `ForbiddenAccessException` 403; the P4 `SegregationOfDuties.CanVerifyAction` predicate already existed). People fields = Keycloak sub strings + name snapshots (direct SoD compare). 709 tests green, coverage 99.62%. AC-012/013 stay **Partial** (Met→P17 live real-stack, matching Decisions AC-027/028 discipline).

**GO'd forks (locked):** (1) AC-029 gate applies ONLY to follow-up-bearing outcomes (Approved/ConditionallyApproved/EnhancementsRequired/DesignChangesRequired/ResearchRequired) — not Rejected/Deferred/etc. (2) Hangfire Admin dashboard **wired in P8c** (Admin-only), flips AC-056. (3) Action detail = **routed `/actions/:key`** (blessed deviation from the design's drawer, so notifications deep-link).

**Carried into later slices:** Hangfire is NOT wired anywhere yet — P8c stands it up fresh (app-owned, ACMP's SQL, ADR-0014; injectable service Hangfire only cron-triggers; thresholds→appsettings; storage bootstraps its own tables, not EF). **ASM (P8d):** AC-029 proxies the downstream link via `ActionItem.SourceId` `(SourceType,SourceId)` query until the P10 typed-edge Traceability module (ADR-0008). `SourceId` is trusted input (no cross-module existence check). Member-AiO create/verify untested (no AC) → verify in P8b UI. YAGNI-deferred: Assignees[], ProgressUpdate history table, file attachments.

**P8b — DONE & MERGED to main (squash `d1b09a6`, PR #64).** Read-first split (operator GO): P8b = the `/actions` register + routed `/actions/:key` detail (READ-ONLY); writes → **P8b2**. Built to `ACMP Lists & Registers.dc.html` `isActions`. Files: `api/actions.ts` (useActionsRegister paged + server status/overdue filters + due/progress/status sorts; useAction by-key; **useActionsCounts** = two `pageSize=1` count queries for the GLOBAL filter-independent header — the paged list can't carry those facets), `features/actions/{ActionsRegister,ActionPage,actionMeta,actions.css}` + i18n `actions.*` EN+AR (every status incl. Cancelled by hand). **Server-side filtering** (operator GO, not the design's client toggle — correct under paging). Only due/progress/status sortable (server-backed); Owner filter disabled stub; New-action + Saved-view disabled stubs. Visual-verified via throwaway dev-stub harness (register+detail × EN-light + AR-RTL-dark) — matches the ref, no drift. 470 FE tests green, new files 100% lines, axe AA, i18n parity 764.

**P8b2 split into P8b2a (lifecycle+verify) + P8b2b (create).**

**P8b2a — DONE & MERGED to main (squash `666b8c8`, PR #67).** The action detail is now actionable, **no backend change**. `api/actions.ts` +7 W14 mutation hooks by Guid `id` (start/unblock/verify no-body; block/cancel mirrored bilingual `reason`; progress `progressPct`; complete optional note) — each invalidates the whole `['actions']` family (detail+register+counts). `actionMeta.ALLOWED_TRANSITIONS` mirrors the ActionItem domain guards. `ActionActions.tsx` (new) = gated button row + one transition dialog; Chairman/Secretary manage any, Member manages only owned; **Verify separated (SoD-1): owner/completer never see it**, API stays the gate. **Fork C DONE:** `AcmpAuth.userId` now exposed from `profile.sub` (FE-only; dev stub = `dev-user`). NO-REFERENCE COMPOSITION flagged (the Lists&Registers Actions drawer draws no buttons; composed from shared Button + the Confirmation/Destructive Dialog patterns in `ACMP Create Flows & Dialogs.dc.html`). 491 FE tests green (was 470), actions files 100% lines, i18n parity 796, axe AA, visual-verified EN-light+AR-RTL-dark. AC-012/013 stay Partial (client SoD-1 hide + 403 handling proven; Met→P17). Exercises FE handling of a **403 denial**, NOT enforcement (backend, P8a); Member **create** SoD path still untested → P8b2b.

**Operator forks resolved (this session):** **A = EXPOSE** `CommitteeMember.KeycloakUserId` on `MemberDto`/`api/members` so the create Owner select can send `{OwnerUserId: sub, OwnerName}` (mild info-exposure flagged: any authed member sees everyone's subs — low risk ≤20 on-prem). **B = NO standalone action — ALWAYS from a context (FINAL).** Domain requires a source (`ActionSourceType` = Topic/Meeting/Decision/Condition/Risk, NO Manual; `SourceId` required NotEmpty); so create launches from a source page with source pre-filled — **first context = the Decision detail `/decisions/:key`**, Meeting next. **C = expose `userId`** (done). 

**P8b2b — DONE & MERGED to main (squash `5b219da`, PR #69).** Contextual create, always from a source (call B). **Fork A shipped:** `MemberDto` + `GetMembersHandler` expose `KeycloakUserId` (committee-wide readable; mild info-exposure flagged); `api/members.keycloakUserId`. `api/actions` +`useCreateAction` + `ActionPriority`/`ActionSourceType` types. `CreateActionDialog.tsx` (new, composed to `ACMP Create Flows & Dialogs.dc.html` `action` — REAL ref, not no-reference): Title·locked Linked-to·**Owner member-select** (keyed to sub, active only)·Due·Priority seg (Low/Medium/High → wire Low/**Normal**/High, default Normal — enum middle is `Normal`, UI labels "Medium")·Description; mirrored en===ar; routes to `/actions/:key`. `DecisionPage` = role-gated "Create follow-up action" on ACTIVE decisions, source pre-filled. **Retired the register's "New action" stub** (create is always contextual). Backend build clean, Membership app 27 + API 10 pass; FE 496 tests (was 491), changed files 100% lines, i18n parity 820; visual-verified EN-light+AR-RTL-dark. Exercises the Member create path; SoD on create API-enforced. AC-012/013 unchanged (verify → P17).

**Next = P8c** (plan-first, GO-gated): Hangfire reminders/escalation + Admin job dashboard (AC-054/055/056) — stands Hangfire up FRESH (app-owned, ACMP's SQL, ADR-0014; injectable service only cron-triggers; thresholds→appsettings; storage bootstraps own tables, not EF; Admin-only dashboard flips AC-056). Then **P8d** (AC-029 IssueDecision→action downstream-link gate, OQ-045; proxies via `(SourceType,SourceId)` until P10 typed-edge Traceability). See [[p7-minutes-decisions-plan]].
