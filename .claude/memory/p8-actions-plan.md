---
name: p8-actions-plan
description: "P8 Actions module — 4-slice plan, GO'd forks, and current progress (P8a+P8b+P8b2a+P8b2b done & merged; next P8c Hangfire)."
metadata: 
  node_type: memory
  type: project
  originSessionId: 5d6ffc09-c171-48a2-9586-476c992f4111
---

**P8 = Actions module**, plan-first + GO-gated, sliced like P7. Design SoT = `ACMP Lists & Registers.dc.html` `isActions` register + detail. Canon: docs/11 (aggregate), docs/12 §7 (lifecycle), W13/W14/W22, docs/10 rows 14–15.

**4 slices:** P8a Actions backend · P8b register+detail UI · P8c Hangfire reminders/escalation + Admin job dashboard · P8d AC-029 decision-link gate.

**P8a — DONE & MERGED to main (squash `26dd245`, PR #63).** New Actions bounded context mirroring Decisions. Aggregate is **`ActionItem`** (NOT `Action` — avoids the `System.Action` BCL clash); DbSet/table/key stay Actions/`ACT-YYYY-###`. 6-state lifecycle Open→InProgress↔Blocked→Completed→Verified + Cancelled; **overdue derived** never stored. **SoD-1 (AC-012/013)** enforced in `VerifyActionHandler` (verifier≠owner≠completer → audit denial + `ForbiddenAccessException` 403; the P4 `SegregationOfDuties.CanVerifyAction` predicate already existed). People fields = Keycloak sub strings + name snapshots (direct SoD compare). 709 tests green, coverage 99.62%. AC-012/013 stay **Partial** (Met→P17 live real-stack, matching Decisions AC-027/028 discipline).

**GO'd forks (locked):** (1) AC-029 gate applies ONLY to follow-up-bearing outcomes (Approved/ConditionallyApproved/EnhancementsRequired/DesignChangesRequired/ResearchRequired) — not Rejected/Deferred/etc. (2) Hangfire Admin dashboard **wired in P8c** (Admin-only), flips AC-056. (3) Action detail = **routed `/actions/:key`** (blessed deviation from the design's drawer, so notifications deep-link).

**Carried into later slices:** Hangfire is NOT wired anywhere yet — P8c stands it up fresh (app-owned, ACMP's SQL, ADR-0014; injectable service Hangfire only cron-triggers; thresholds→appsettings; storage bootstraps its own tables, not EF). **ASM (P8d):** AC-029 proxies the downstream link via `ActionItem.SourceId` `(SourceType,SourceId)` query until the P10 typed-edge Traceability module (ADR-0008). `SourceId` is trusted input (no cross-module existence check). Member-AiO create/verify untested (no AC) → verify in P8b UI. YAGNI-deferred: Assignees[], ProgressUpdate history table, file attachments.

**P8b — DONE & MERGED to main (squash `d1b09a6`, PR #64).** Read-first split (operator GO): P8b = the `/actions` register + routed `/actions/:key` detail (READ-ONLY); writes → **P8b2**. Built to `ACMP Lists & Registers.dc.html` `isActions`. Files: `api/actions.ts` (useActionsRegister paged + server status/overdue filters + due/progress/status sorts; useAction by-key; **useActionsCounts** = two `pageSize=1` count queries for the GLOBAL filter-independent header — the paged list can't carry those facets), `features/actions/{ActionsRegister,ActionPage,actionMeta,actions.css}` + i18n `actions.*` EN+AR (every status incl. Cancelled by hand). **Server-side filtering** (operator GO, not the design's client toggle — correct under paging). Only due/progress/status sortable (server-backed); Owner filter disabled stub; New-action + Saved-view disabled stubs. Visual-verified via throwaway dev-stub harness (register+detail × EN-light + AR-RTL-dark) — matches the ref, no drift. 470 FE tests green, new files 100% lines, axe AA, i18n parity 764.

**P8b2 split into P8b2a (lifecycle+verify) + P8b2b (create).**

**P8b2a — DONE & MERGED to main (squash `666b8c8`, PR #67).** The action detail is now actionable, **no backend change**. `api/actions.ts` +7 W14 mutation hooks by Guid `id` (start/unblock/verify no-body; block/cancel mirrored bilingual `reason`; progress `progressPct`; complete optional note) — each invalidates the whole `['actions']` family (detail+register+counts). `actionMeta.ALLOWED_TRANSITIONS` mirrors the ActionItem domain guards. `ActionActions.tsx` (new) = gated button row + one transition dialog; Chairman/Secretary manage any, Member manages only owned; **Verify separated (SoD-1): owner/completer never see it**, API stays the gate. **Fork C DONE:** `AcmpAuth.userId` now exposed from `profile.sub` (FE-only; dev stub = `dev-user`). NO-REFERENCE COMPOSITION flagged (the Lists&Registers Actions drawer draws no buttons; composed from shared Button + the Confirmation/Destructive Dialog patterns in `ACMP Create Flows & Dialogs.dc.html`). 491 FE tests green (was 470), actions files 100% lines, i18n parity 796, axe AA, visual-verified EN-light+AR-RTL-dark. AC-012/013 stay Partial (client SoD-1 hide + 403 handling proven; Met→P17). Exercises FE handling of a **403 denial**, NOT enforcement (backend, P8a); Member **create** SoD path still untested → P8b2b.

**Operator forks resolved (this session):** **A = EXPOSE** `CommitteeMember.KeycloakUserId` on `MemberDto`/`api/members` so the create Owner select can send `{OwnerUserId: sub, OwnerName}` (mild info-exposure flagged: any authed member sees everyone's subs — low risk ≤20 on-prem). **B = NO standalone action — ALWAYS from a context (FINAL).** Domain requires a source (`ActionSourceType` = Topic/Meeting/Decision/Condition/Risk, NO Manual; `SourceId` required NotEmpty); so create launches from a source page with source pre-filled — **first context = the Decision detail `/decisions/:key`**, Meeting next. **C = expose `userId`** (done). 

**P8b2b — DONE & MERGED to main (squash `5b219da`, PR #69).** Contextual create, always from a source (call B). **Fork A shipped:** `MemberDto` + `GetMembersHandler` expose `KeycloakUserId` (committee-wide readable; mild info-exposure flagged); `api/members.keycloakUserId`. `api/actions` +`useCreateAction` + `ActionPriority`/`ActionSourceType` types. `CreateActionDialog.tsx` (new, composed to `ACMP Create Flows & Dialogs.dc.html` `action` — REAL ref, not no-reference): Title·locked Linked-to·**Owner member-select** (keyed to sub, active only)·Due·Priority seg (Low/Medium/High → wire Low/**Normal**/High, default Normal — enum middle is `Normal`, UI labels "Medium")·Description; mirrored en===ar; routes to `/actions/:key`. `DecisionPage` = role-gated "Create follow-up action" on ACTIVE decisions, source pre-filled. **Retired the register's "New action" stub** (create is always contextual). Backend build clean, Membership app 27 + API 10 pass; FE 496 tests (was 491), changed files 100% lines, i18n parity 820; visual-verified EN-light+AR-RTL-dark. Exercises the Member create path; SoD on create API-enforced. AC-012/013 unchanged (verify → P17).

**P8c split into P8c-1 (jobs/backend) + P8c-2 (Admin dashboard).**

**P8c-1 — DONE & MERGED to main (squash `466a1db`, PR #71).** Hangfire reminder/escalation sweep (AC-054/055),
backend-only. **Operator forks resolved:** dashboard flavour = **B (designed Administration → Job Monitor tab)** → P8c-2; overdue rhythm = **system config** (`ActionReminders:OverdueMode`), **default DailyWhileOverdue**. **Cadence + recipients are DOC-SETTLED (docs/29 §3.4), not invented:** due reminder T-3→owner; overdue→owner; escalation >7d→+Secretary, >14d→+Chairman. **Hangfire stood up FRESH** (`Hangfire.AspNetCore`+`Hangfire.SqlServer` 1.8.14) on ACMP's OWN SQL, own `HangFire` schema, storage bootstraps its own tables (NOT EF migrations); wired in `Program.cs` (coverage-excluded) and **gated off under the `Testing` host** (`!env.IsEnvironment("Testing")`) so the InMemory Api factory never opens SQL. `SweepActionRemindersHandler` (`Actions.Application/Reminders`, MediatR `SweepActionRemindersCommand`, NOT IAuthorizedRequest — headless, audit actor `system:action-reminders`) holds ALL logic (unit-tested on a fake clock); the recurring job just sends it. Calendar-day math (UTC date floor) so T-3/>7/>14 boundaries don't drift with run time. `ActionReminderOptions` (appsettings `ActionReminders`: DueReminderDaysBefore=3, EscalateToSecretaryAfterDays=7, EscalateToChairmanAfterDays=14, OverdueMode=Once|DailyWhileOverdue default DailyWhileOverdue, SweepCron="0 6 * * *"). **Idempotency = 4 nullable markers on `ActionItem`** (`DueReminderSentAt`/`OverdueNotifiedAt`/`EscalatedToSecretaryAt`/`EscalatedToChairmanAt`) + migration `Actions_ReminderMarkers` (forward-only) — **NO OUTBOX exists** (`INotificationChannel.PublishAsync` writes synchronously), so the markers ARE the dedup; **save-before-send = at-most-once (favour no-spam)**. Escalation recipients via **new `ICommitteeDirectory.GetActiveMembersInRoleAsync(role)`** (Membership-owned, reads the login-refreshed `CommitteeMember.Role` cache) — Actions never reads Membership tables (ArchUnit green). 3 new bilingual `ActionNotifications` (DueReminder/Overdue/Escalation, EN+AR by hand). **KEY GOTCHA (cost me an e2e-red cycle):** the static `RecurringJob.AddOrUpdate` reads `JobStorage.Current`, uninitialized until the Hangfire hosted server starts (after `app.Build()`) → **threw at startup → API container exit 139 → e2e red**; fix = resolve **`IRecurringJobManager` from DI** and call `.AddOrUpdate` (the exception prescribes it). Unit tests can't boot the real Hangfire server, so this only surfaced in e2e — **for future Hangfire/real-stack wiring, boot the API against a real SQL container locally BEFORE pushing** (the InMemory factory won't catch startup-order bugs; `dotnet run` stdout is block-buffered to a file — use SIGINT/graceful or read the container log). **Deferred/flagged:** `NotificationMessage` has no urgency → catalog tiers not reflected in the center's High+Critical filter (Notifications concern); no preference model (RD-09) → due-reminder sends unconditionally; role cache login-refreshed (ASM). **AC-054/055 → Partial** (logic fake-clock-proven; live cron firing → P17 per G-TRACE). Backend 496 App + 123 Domain + 24 Arch + 74 Api green; per-file coverage 170 files @ 99.62%; format clean; all 4 CI checks green incl. e2e.

**P8c-2 — DONE (branch `feat/P8c-2-actions-jobs-admin`, pre-merge as of 2026-07-02).** The designed Administration → Job Monitor tab (operator fork B), flips **AC-056 → Met**. Backend: 2 endpoints on the existing `/api/admin` group (inherits `Policies.AdminConfig`). `GET /api/admin/jobs` resolves **`JobStorage` OPTIONALLY from DI** (`GetService<JobStorage>()`) → `Configured=false` under Testing/no-Hangfire host, else a **pure `JobsMonitorMapper`** projects `GetMonitoringApi()` (5 counts via `GetStatistics()` + recent rows merged from Failed/Processing/Scheduled/Queues().FirstJobs/Succeeded, capped 20, newest-first; name=`Job.Method.Name`, queue=`Job.Queue`, absolute UTC timestamps out). `POST /api/admin/jobs/{id}/requeue` = the design's Retry (`IBackgroundJobClient.Requeue` → bool; 204/404; 503 when off) — **audited `admin.job.requeued`** (guardrail #5). **Test seam that beat the ≥95% per-file gate (advisor call):** the mapper is a pure static fn unit-tested against a **mocked `IMonitoringApi`** (NSubstitute added to Api.Tests); the endpoint's live Map/requeue branches covered by **wired tests that substitute `JobStorage`/`IBackgroundJobClient` into the Testing host via `WithWebHostBuilder`** (`JobStorage` is abstract w/ parameterless ctor → NSubstitute-able; `Requeue` routes through `IBackgroundJobClient.ChangeState(str,IState,str)` → unconfigured=false=404, configured=true=204). FE: `api/jobs.ts` (`useAdminJobs` 30s poll + `useRequeueJob`), `JobMonitor.tsx` (composes shared **Table** like RolesReference, StatusChip tones Succeeded→success/Processing→info/Scheduled→scheduled/Enqueued→neutral/Failed→danger, Button danger retry on failed rows only), `jobFormat.ts` (relative "when" via native **`Intl.RelativeTimeFormat`** = EN+AR plurals free; duration split n+unit). Full states incl. **not-configured** panel + empty. i18n `admin.jobs.*` EN+AR by hand; `.adm-jobs-*` logical CSS. **HARD GATES PASSED:** backend 171 files @ 99.67% (`JobsMonitor.cs`+`AdminEndpoints.cs` 100%), Api 88 green, format clean; FE 514 green, new files 100% lines, i18n 843, tsc+vite+oxlint clean; **real-SQL boot** (throwaway console vs running `acmp-sqlserver`) proved `JobStorage`→`SqlServerStorage` non-null so prod = `Configured=true` NOT the fallback (the P8c-1 lesson: Testing host can't tell "correctly off" from "broken-in-prod off"); **visual-verified EN-light + AR-RTL-dark** via throwaway seeded-cache harness, deleted. **Honest-sparse** (only real jobs, not the mock's DigestEmail/DiagramRender). Blessed deviations: "2 minutes ago" (Intl) vs mock "2m ago"; retries column "—" (no per-row JobDetails call). **Next = P8d** (AC-029 gate). ~~Superseded plan below.~~

**P8d — DONE & MERGED to main (squash `83249eb`, PR #73, all 4 CI checks green incl. e2e, 2026-07-02).** AC-029 downstream-link gate, resolves OQ-045; **backend-only (Fork 2 GO)**. **Fork 1 GO = HARD gate** (AC-029+FR-067+OQ-045(a) all mandate reject-and-stay-Draft; the brief's soft lean was overridden). A follow-up-bearing decision (Approved/ConditionallyApproved/EnhancementsRequired/DesignChangesRequired/ResearchRequired — the locked 5; `DecisionOutcomeRules.RequiresDownstreamLink` pure seam in Decisions.Domain) can't be Issued until ≥1 linked ActionItem exists → `InvalidOperationException`→**409**, stays Draft; Rejected/Deferred/etc. exempt. **Cross-module seam = new `IActionLinkDirectory` (`Acmp.Shared/Contracts/Actions`, primitive `DecisionHasLinkedActionAsync(Guid)`, mirrors ICommitteeDirectory); impl `ActionLinkDirectory` in Actions.Infrastructure counts `(SourceType=Decision,SourceId)` via AnyAsync — Decisions never reads Actions tables (ArchUnit green).** **Gate lives in `IssueDecisionHandler`, NOT `Decision.Issue`** (count is cross-module, domain can't see it) → **auto-exempts supersession** (SupersedeDecisionHandler drafts+issues the successor atomically with ZERO links; gating it would deadlock every correction; not a bypass since first-issue is only reachable through the gated path). **ASM-014 (docs/41):** proxy "downstream link" as ≥1 Action until Risk/Traceability P10 (ADR-0008); supersession exempt; SourceId trusted (no existence check). **Why no FE:** no Draft→Issue affordance exists (api/decisions.ts=read+supersede; issue-UI was out of P7b scope), and under a hard gate an Issued decision ALWAYS has ≥1 link so a "missing links" indicator is dead code → issue-UI + create-on-draft = later slice. **KEY GOTCHA for future gate work:** adding a ctor dep to `IssueDecisionHandler` broke every existing test that issued an Approved (follow-up) decision with no link — the P8b2b UX creates actions AFTER issue, so the whole shipped Actions grain is post-issue; the hard pre-issue gate fights it (that's why the operator's instinct was soft). Fixed existing tests by linking an action first or using a non-follow-up outcome. Backend 173 files @ 99.67%, Domain 123/Arch 24/App 509/Integration 16/Api 90 green; format clean. **AC-029 → Met; OQ-045 resolved. P8 (Actions) module COMPLETE** — a/b/b2a/b2b/c-1/c-2/d all shipped. Next phase = P9 Voting / P10 Risks+Dependencies+Traceability (P10 widens the AC-029 predicate to typed edges per ASM-014).

**~~SUPERSEDED (P8c-2 plan, now done):~~ Next = P8c-2** (plan-first, GO-gated): the **designed Administration → Job Monitor tab** (`ACMP Administration.dc.html` `isJobs`, the disabled placeholder tab today) — a bearer-authed `/api/admin/jobs` endpoint (same pattern as the existing `/api/admin/health` in `AdminEndpoints.cs`, `Policies.AdminConfig` = Administrator-only) reading Hangfire's `JobStorage.Current.GetMonitoringApi()` (5 stat tiles Succeeded/Processing/Scheduled/Enqueued/Failed + recent-jobs table name·queue·status·when·duration·retries) + a **Requeue endpoint** (design's Retry button = `IBackgroundJobClient.Requeue`, so NOT read-only) + the FE tab in EN/AR/RTL. **Honest-sparse:** it shows only jobs that actually run (the sweep), not the mock's aspirational DigestEmail/DiagramRender list. **Flips AC-056** — and since it's an endpoint+screen it can go straight to **Met** with a small E2E (unlike 054/055). Then **P8d** (AC-029 IssueDecision→action downstream-link gate, OQ-045; proxies via `(SourceType,SourceId)` until P10 typed-edge Traceability). See [[p7-minutes-decisions-plan]].
