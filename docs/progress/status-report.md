---
status: Approved
version: 1.13.0
updated: 2026-07-19
owner: Claude Code execution agent
generation: derived
---

# Status Report — ACMP

Derived snapshot of where the build is. Regenerated each update cycle from the [progress log](progress-log.md), the [acceptance audit](../validation/acceptance-audit.md), and the registers. This is the "where you are now" pointer referenced by [AGENTS.md](../../AGENTS.md).

## Current position

- **Phase:** PH-1 (MVP Governance) **complete**; PH-2 (Governance Expansion) **substantially delivered**. **Cross-cutting hardening `P16` (Security) ★ COMPLETE ★ — all batches merged: Batch 1 (PR #124), Batch 2 (PR #126 `11c6372`), and B2b+B3+B4 (PR #141, squash `e15cfff`, 2026-07-17).** **`P14` (Tarseem diagrams) is DEFERRED INDEFINITELY and removed from the active ladder — operator decision 2026-07-17, recorded as `DEC-028` (see also D-11); re-opened only by explicit operator instruction.** With P14 out, **PH-2 is closed** and the remaining ladder is the three cross-cutting closing slices **P17 → P18 → P19**. **P17 is CLOSED (P17a hygiene #143 + P17b decision-issuance #145); P18 (Deployment) is substantially delivered — P18a hardening MERGED (PR #146 `7991f93`, 9/9 CI green) and P18b (backup/restore/promote scripts + runbook + ADR-0033) committed on `feat/P18b-backup-runbook` for PR #147. The only remaining ladder slice is `P19` (final audit & release readiness) — the last slice.** The canonical build-slice ladder (`P1…P19`) is in [planning/roadmap.md](../planning/roadmap.md) §Build-slice ladder.
- **Latest slice:** **P18 (Deployment) — P18a hardening MERGED (PR #146, squash `7991f93`, all 9 CI checks green); P18b (backup/restore/warm-standby) committed on `feat/P18b-backup-runbook`, bound for PR #147. No AC verdict flip.** P18a (three batches per [deployment.md](../domain/deployment.md)): **Docker secrets everywhere** (ADR-0032 — file-backed `/run/secrets`; .NET `AddKeyPerFile`, SQL/Keycloak entrypoint shims, MinIO/Postgres native `*_FILE`; `gen-secrets.sh` writes 0644-in-0700 with `printf` not `echo`; `up.sh` single-command bring-up, NFR-052; the two-context `Dockerfile.sqlserver` inlines the shim as an ENTRYPOINT); **prod overlay `docker-compose.prod.yml`** (additive — Keycloak `start`, Seq auth, internal-only ports via Compose `!override`, §9 resource limits, OCI labels, SQL `/backups` mount); and **least-priv `acmp_svc` runtime login** (ADR-0031/0032) which **closes the AC-017/018 "→ P18 least-priv" residual** — runtime = db_datareader+db_datawriter+EXECUTE+acmp_app (not db_owner/sysadmin) so the audit-schema DENY finally binds (the devil's-advocate review rejected `db_owner`: it can REVOKE the DENY), via a migrator/runtime split (`--migrate-only` `db-migrate` one-shot + `sqlserver-init`) and NFR-045 readiness checks (MinIO/Hangfire critical, Seq degraded-only) on `/readyz`. Durable proof: the extended `AuditImmutabilityDbPermissionTests` shows `acmp_svc` is DB-denied audit UPDATE/DELETE. **D-16 is now fully done.** P18b: `deploy/scripts/{backup,restore,promote}.sh` + `crontab.example` (SQL `.bak` + KC `pg_dump` + MinIO `mc mirror` + off-box `scp`, nightly + 4h business-day RPO ≤4h per NFR-057), ADR-0033 (backup-as-scripts), consolidated `deploy/runbooks/README.md`; **tested restore proven** (seed 3 → backup → DELETE 0 → restore → 3 on a real SQL container). **Operator/P19 residuals stay open by design** (deployment.md §3.4): TLS certs/topology, C-CRYPTO Step B + TDE + MinIO/Seq TLS/SSE + backup encryption, the OQ-040 SQL-edition security decision, AC-004 Keycloak realm idle-timeout/MFA (OQ-003). **Next = P19.** Prior: **P17b (final slice — decision-issuance UI) — COMPLETE on `feat/decision-issue-ui`, merged PR #145. AC-014/015/016 flipped Partial → Met; the F-03 core-loop leg landed.** The P17b-0 triage's one D-15-shape product gap: a committee could not issue a decision or ratify a vote through the SPA (the "Record decision" button was a disabled stub; `api/decisions.ts` had only read + supersede). Backend was already complete (SoD-3 co-attestation + vote auto-ratify), so this is FE-only: `useRecordDecision`/`useIssueDecision`, a chairman-gated `RecordDecisionDialog` launched from the in-session `MeetingWorkspace` (couples the agenda item's Closed vote via `useVotesRegister` filter — no vote-by-topic endpoint — records once holding the Draft so a failed issue never duplicates, then issues, surfacing the SoD-3 403 / AC-029 409 inline), and the `approvedBySoleAuthor` warning badge on the minutes banner (AC-014 — the flag was set server-side but never shown). **AC-015** (chair who closed the vote is denied issue → 403 surfaced inline; vote stays Closed), **AC-016** (secretary-counted vote → chair issues with override → decision Issued + vote Ratified — the F-03 "chairman ratify → decision Issued" leg), **AC-014** (sole-author badge renders) — all proven live (`p17b-decision-issue.spec.ts`, `p17b-sole-author.spec.ts`). Specs use a non-follow-up outcome (Rejected) so the SoD-3 gate is reached, not the AC-029 409 (gate order in `IssueDecision.cs`). Design reconciliation (one merged dialog vs. the design's two surfaces; design-only create-form fields dropped) recorded as **OQ-058**. F-03 itself stays ☐ (a P19 full-loop staging sign-off) but its last missing evidence now exists. Gates: FE `test:cov` 140 files/1059 tests exit 0, per-file cov OK (decisions.ts 100%); i18n parity 1786; build exit 0; oxlint clean; no C# changed. Prior: **P17a (test hygiene) — COMPLETE on `feat/p17a-test-hygiene`, merged (PR #143). No AC verdict flips (by design).** ★ **It found that `main` was shipping `NOT READY` on critical Keystone gate `G-PROGRESS` while this report claimed the validator was green.** `e15cfff` (P16 #141) bolded the 74 AC id cells in the acceptance audit as a "G-IDS fix"; `G-IDS` **already skips that file by name** (`validate_package.py:428-436`) so the bold was never needed, while **`G-PROGRESS` parses those same cells** (`:968-988`) with `strip().strip("`")` + `fullmatch` — **backticks only, never asterisks** — so all 74 ACs read "not represented (coverage gap)". Bisected: `11c6372` bare → **OK**; `e15cfff` bold → **NOT READY**; un-bolded → **OK, 7/7**. Validator unchanged since 2026-06-22 ⇒ no version excuse; the prior "green 6/6" was recorded without re-running. Ids **must stay bare**; the stale guard comment now records the verified mechanism. **D-19 fixed** — and its register diagnosis ("non-deterministic pick over the mocked decisions list") was **wrong**: there is no list; `loc` is always mounted, so `findByTestId` resolved instantly and `toHaveTextContent` evaluated once, before `onConfirm`'s post-await `navigate()` flushed. Fix = retry the assertion (`waitFor`), 8/8 green; the sibling assertions at `:168/:188/:209/:217-220` are **provably safe** (`mutateAsync` is invoked synchronously) and were left alone. **The live-leg verdict rule is now written into [definition-of-done.md](../execution/definition-of-done.md) §Acceptance Criteria** (operator: *keep the bar, fix the label*) — it had been cited everywhere as "per G-TRACE", but G-TRACE is Keystone *link-completeness* (`work-breakdown.md:331`) and says nothing about verdicts; **the standard is unchanged**. Also: `AGENTS.md` un-staled (P12 → P17/P18/P19 + P14 deferral), `security.yml`'s "all scanners report-only" header corrected (gitleaks/semgrep/trivy-fs are **gating**), and the stale `→ P14` in the P7b blockquote judged (audit-timeline actually shipped in the Audit slice #105) and **annotated rather than rewritten**. Gates: validator **7/7**; FE `test:cov` exit 0 (139 files/1051 tests); i18n 1768; build exit 0; no C# changed. **Next = P17b-0 AC triage, which blocks all spec work; the "~19 flips / 36→55" forecast is retracted pending it.** Prior: **P16 Batches B2b + B3 + B4 (the remaining security hardening) — COMPLETE, merged as `e15cfff`.** One PR (operator's full-scope decision), closing **D-21**. **★ The plan's biggest item was cancelled as unnecessary:** the ~110-site frontend refactor (+ the @dnd-kit CSSOM shim) existed only to drop `style-src 'unsafe-inline'`, but **CSP governs `<style>` elements and `style=""` attributes, not the CSSOM** — and react-dom applies `style={{}}` via CSSOM writes, never a style attribute (no SSR here). Browser-verified under `style-src 'self'`: CSSOM writes apply; `setAttribute('style')` + injected `<style>` are blocked. `@dnd-kit` turned out to be **tree-shaken out of the production bundle entirely** (only its own test imports it), so plan risk **R1** (the shim wedging the ≥95% coverage gate) was moot three times over. Operator: skip the refactor, log as hygiene → **D-22**. **Delivered:** HSTS + Permissions-Policy + strict CSP, all server-level (nginx `add_header` does not merge across levels, so a per-location one would drop the inherited set); read-only-FS containers + `cap_drop:[ALL]` + no-new-privileges on api/worker/web (sidecars: no-new-privileges only); crypto scaffold → **`Partial (Operator/P18)`, never Met** — and **corrected 2026-07-17**: the SQL-transit leg is now genuinely ON (`Encrypt=True` on all 4 runtime sites; live-verified `encrypt_option` FALSE→**TRUE**; no cert needed — SQL Server self-signs), still `Partial` because `TrustServerCertificate=True` means encrypted-but-**not-authenticated** and MinIO/Seq stay plaintext. The original "TDE is edition-gated" reasoning was **wrong for this stack** (it runs **Developer = Enterprise** features, `EngineEdition=3`, so TDE would work today); TDE is deferred for **certificate key custody**. New finding: **OQ-040 gates two P1 controls** while marked non-blocking (Express/Web lack TDE **and** backup encryption); Seq alert runbook (5 rules, each on a property the app already emits; records what is *not* alertable rather than shipping dead tripwires — no export feature exists (D-07), no `Restricted` tier (D-20)); C-NOTIF audit → **Met** (bodies carry only artifact key + deep link + day counts). **Three real bugs found by verifying, two self-inflicted by this branch:** the B2b digest-pin broke `SearchProvidersFtsTests` (Testcontainers 3.10 cannot parse a digest in `FROM`) → upgraded to 4.13 rather than un-pin a moving `2022-latest` tag; a bare tmpfs on `/etc/nginx/conf.d` inherits root:root so nginx (UID 101) could not write its config and would start with **no server block while reporting `running`** → `uid=101,gid=101,mode=0755`; and R3 was under-diagnosed — `proxy_request_buffering off` stops only *nginx* spooling, while ASP.NET's `IFormFile` independently spools >64 KB to `Path.GetTempPath()`, so with a 2 GiB cap against a 512 MB–1 GB RAM budget the api's `/tmp` had to be a **disk-backed volume**, not tmpfs. Also fixed a latent `ngrok web:80`→`web:8080` bug (profile-gated, so e2e never caught it) and the plan's "SameSite=Strict" instruction, which would have **broken** the Webex OAuth callback (`Lax` is required for the cross-site top-level redirect). **Gates:** full `dotnet test` **1451 pass / 0 fail**; coverage **416 files, 99.65%**; format clean; `check-vulns` 0 High/Critical; `trivy config deploy/` **0 misconfig** (D-21 closed; fs gate tightened CRITICAL→CRITICAL,HIGH); Keystone validator NOT READY on G-IDS but **pre-existing and identical on `main`** (74 findings, none in changed files) — **⚠ this claim is FALSE and was retracted 2026-07-17 (P17a): `main` at `11c6372` validates `RESULT: OK`. The subsequent "G-IDS 74→0 fix" bolded the 74 AC id cells in the acceptance audit and thereby broke critical gate `G-PROGRESS`, shipping `main` NOT READY from `e15cfff` until P17a un-bolded them (now OK, 7/7). See the P17a progress-log entry.**. **Live (isolated `-p acmpe2e`):** hardened read-only stack boots healthy across every container (runtime-verified `ReadonlyRootfs`/`CapDrop[ALL]`/non-root); **e2e 40/40** incl. VR sweep EN-light + AR-dark and the drag specs; **CSP: 26 screens → 0 violations** and a real kanban drag engaged with 0 violations; rate limiting 140 posts → exactly 120×200 + 20×429 with `Retry-After: 60`; a **300 MB** recording upload → HTTP 200 through read-only nginx (64m tmpfs) + read-only api; a PNG spoofed as `video/mp4` → 400; Seq: 500 real `Acmp.Api` events, **zero** unmasked PII. **No new ADR; no AC verdict change** (controls satisfy NFR-018/019/024/051/054). Prior: **P16 Batch 1 (audit & vote crypto core) MERGED (PR #124 `87dfa40`); ADR-0030 + ADR-0031 ratified → Accepted.** A verify-and-close-gaps slice closing the two deferrals the P16 prompt names in bold. **D-13 per-ballot crypto chaining (ADR-0030):** a SHA-256 chain sealed at `Vote.Close` over all `vote_ballots` rows (`BallotChain` / `Vote.VerifyBallotChain`) + a tally-vs-ballots recompute, making a direct-SQL edit/insert/delete/reorder of a closed vote's ballots detectable — the gap the vote state-change `AuditEvent` chain does not cover; legacy pre-P16 closes are unsealed/skipped (no backfill). **D-16:** the nightly integrity job (`IIntegrityVerifier` + a per-module `IIntegrityCheck` seam, ADR-0001; `Acmp.Worker` `Cron.Daily(3)`) re-verifies the audit chain **and** every sealed vote's ballot chain + tally, alerting via Serilog + a durable `AuditEvent` (C-INS-02); plus the DB-permission migration `Audit_DenyMutation` (`DENY UPDATE,DELETE ON SCHEMA::audit`, ADR-0031) — **Partial: inert until the app runs a least-priv login instead of `sa` (sysadmin bypasses DENY) → P18 operator residual**, proven now under a restricted login (Testcontainers). Security suite = **`dotnet test --filter "Category=Security"` (339 tests)**; new evidence register [`../validation/security-controls-audit.md`](../validation/security-controls-audit.md). **No AC verdict flips** — AC-017/018/019/020 were already `Met`; Batch 1 closes their "→ P16" residual notes + D-13 tech-debt. Scope forks (operator-confirmed): transit/at-rest crypto → Batch 3 scaffold + P18; **Confidentiality ABAC (C-AUTHZ-04) deferred as a feature → D-20** (verified: no `Restricted`/`Confidentiality` field exists in code); SoD-2 soft flag left per-spec (`warn+audit`). Gates green: BE build / `dotnet format` / **1429 tests** / per-file cov **99.67%**; Keystone 7/7 critical; CI (backend/compose/e2e/frontend) all green; **no FE change** (no i18n delta). Prior: **P15 design-fidelity + i18n audit remediation (INV-014/INV-009) MERGED (PR #121 `a54e169`); ADR-0029 ratified → Accepted (PR #122 `c1aff92`).** Five batches force-matched to the `.dc.html` mockups: serif title (LTR-scoped `--serif`), localized AR breadcrumb, `UpdatedAt` register column (m5 backend DTO/projection/sort), 7-icon wiki toolbar (`markdownInsert` util), draft autosave, **WK10** bespoke read-only Linked-artifacts card + `Document` made a pickable relationship target (option C), History for all readers, Arabic-Indic read-time, localized search status, P15-scoped DMY (`lib/p15Date`). **No AC flips** (fidelity/i18n only). Gates green: FE 1050 tests / i18n parity 1768 / oxlint / build / cov exit 0; BE build / `dotnet format` / per-file cov 99.67%; **live pixel-VR 8/8, signed off twice** (EN-light + AR-dark). Prior: **P15h (Template pre-fill — FR-120 → Met) MERGED (PR #119 `68d90d4`). ★ P15 COMPLETE (8/8 slices). ★** FE-only: one shared `TemplatePicker` (`features/templates/`) wired into the four create surfaces (Topic→description, ADR→Context, MoM→body, Mission→Question), read-only reuse of the P15d/P15e `?targetType` + `/{key}` seam — no backend/module/seam/migration. Active-only list, empty→hidden (INV-014), placeholders stay literal, Apply disabled while the field has content (guards a restored SubmitTopic draft). Field-mapping (operator-blessed): FR-120's singular "content field" → one primary field/artifact; ADR→Context, Mission→Question (the create command's only prose field). OQ-051 flat `Topic` = picker offers all Topic templates regardless of topic type. No-ref affordance (design-update-owed). FE gates: build/oxlint clean, i18n parity 1754, per-file coverage exit 0 (changed files 95.72–100%); no BE change. Prior: **P15f/g (Global Search — FR-143/144/145/118, AC-060/061 → Met) MERGED (PR #118 `6c6ed95`).** OQ-034 resolved by spike: SQL Server FTS (Arabic word-breaker lcid 1025, 82.9% recall) + LIKE booster — single datastore, INV-002 not triggered, no OpenSearch/ADR. `ISearchProvider` cross-module seam (ADR-0001) with 5 per-module impls (Topics/Decisions/ADRs/MoMs/Documents) + inline `/api/search` fan-out coordinator (grouped by type) + 5 guarded FTS migrations (`IsFullTextInstalled` guard, `suppressTransaction`, per-module catalog) + `deploy/Dockerfile.sqlserver`. FE `/search` page (no-ref composition). Real-stack proof `SearchProvidersFtsTests` (Docker FTS SQL Server). BE 1415 tests/cov 99.66%; FE 1023 tests; format/i18n/build green. Live pixel-VR smoke owed. Prior: **P15e (Knowledge UI) MERGED (PR #117 `59df43e`).** FE for the P15d Knowledge backend, one combined slice. New `marked`+`dompurify`-backed `MarkdownView` (allowlist-sanitized, cure53 XSS test). Wiki = a `Category`-grouped tree + serif reading view (author via `useMembers`/`keycloakUserId` join, `TraceabilityPanel` links) + split md/preview editor + version-history panel (diff→P14) + create dialog; lifecycle affordances (New/Publish/Archive/status) added as no-ref compositions, manager-only; tree shows Published→all, Draft→managers, Archived excluded (OQ-052). Templates = standalone `/templates` (Knowledge nav, not the admin-only design home) `.gTpl` table with real enums + no-ref create/edit form (TargetType immutable on edit) + Deprecate action; removed the honest-empty Admin templates tab. Independently re-verified: build clean, oxlint clean, **1016 FE tests / per-file ≥95%**, i18n 1736 keys; live VR captured EN-light + AR-dark against `ACMP Research & Knowledge.dc.html` + `ACMP Administration.dc.html` — the original "pixel-faithful" wording was an **overclaim** (VR specs are capture-only, no baseline diff): a later fidelity audit found INV-014/INV-009 gaps (serif title fell to sans, Arabic breadcrumb showed English "Governance", register showed "Created" not "Updated", etc.), **closed 2026-07-15 by the P15 audit-remediation** (merged **PR #121 `a54e169`**; five batches + **ADR-0029 Accepted #122**; see progress-log). **FR-116/117/119 → Met (UI).** Next = **P15f/g global search** (OQ-034 + INV-002) → **P15h** template pre-fill. Prior: **P15d-2 (Knowledge backend — Template, FR-119) MERGED (PR #116 `50e6ffd`).** A **mirror** onto the P15d-1 scaffold — no new module wiring (Template rides the existing `KnowledgeDbContext` + `Knowledge.Application` assembly; ArchUnit unchanged 49→49). `Template` (TPL-, Active→Deprecated, RowVersion): `Name` bilingual (en===ar), `Body` a **single Markdown string** (only Name bilingual — advisor-caught over-mirror correction), `TargetType` enum, `Version` a **plain counter** (no snapshot — FR-117 is Document-only). `Create`/`Edit`(TargetType immutable)/`Deprecate` (soft delete). CQRS + `/api/knowledge/templates/*` (**`targetType` filter = the P15h seam**) + enriched audit; RBAC backstop `TemplateManage` **includes Administrator** (matches its matrix row, unlike DocumentManage). Migration `Knowledge_AddTemplate` (one `templates` table, same DbContext). Independently re-verified: build clean, Domain 241/Arch 49/Api 226/Application 854 pass, format clean, no Knowledge/Template file <95% cov (global 98.69%). **OQ-051 → Approved:** `TargetType` = FR-119 `{Topic, Adr, MinutesOfMeeting, ResearchMission}` (requirement wins over §403 sketch; Document/Action YAGNI, addable later with no migration); domain-model §402-404 reconciled in-PR. **FR-119 → Met (backend).** Next = **P15e Knowledge UI** (wiki + template mgmt, INV-014 live pixel-VR) → P15f/g global search → P15h template pre-fill. Prior: **P15d-1 (Document, FR-116/117) MERGED (PR #115 `424e03e`).** New `knowledge`-schema module (3 projects mirroring Research); `Document` (DOC-, Draft→Published→Archived, RowVersion) with FR-117 immutable owned `DocumentVersion` snapshots per content save; `Category` free-text (FR-116; domain-model gap logged), bilingual Title/Body (en===ar), owner = attribution only (Chair/Sec effective, OQ-050 precedent). ★ `KnowledgeDbContext` in MigrationRunner + composition-root + ArchUnit wired; migration `Knowledge_Init`. Prior: **P15c COMPLETE — P15c-1 backend MERGED (PR #113 `bab2adc`); P15c-2 frontend MERGED (PR #114 `c660749`, live pixel-VR PASS).** P15c-2 is FE-only: `ConvertToTopicDialog` (pre-filled topic form → `POST /api/topics/from-research` → navigate to the new `TOP-`), header **Convert** button (Completed-only, INV-014 flag) + per-recommendation Convert (Accepted), the shared `TraceabilityPanel` mounted on the mission in a two-column grid (FR-114), a navigable `TOP-` header xref with a plain-indicator fallback for older missions (FR-115), an **edge-driven** "Converted → `TOP-`" chip that self-heals a failed best-effort status-mark, and `ResearchMission` added to `GRAPH_FOCUS_TYPES` + a cold-resolver so the impact graph opens on a mission incl. refresh (FR-113). `TokenInput` extracted to `components/ui`. 939 FE tests / cov 99.64% / i18n 1640 / oxlint / build green; live VR EN-light + AR-dark RTL pixel-faithful to `ACMP Research & Knowledge.dc.html` (`e2e/p15c-convert-vr.spec.ts`). **FR-113/114/115 → Met.** Prior in P15: **P15a backend + P15b UI MERGED to `main` (PR #107 `7d82aba`, PR #108 `c06dc85`, 2026-07-13); P15b live pixel-VR MERGED (PR #110 `abb84a9`).** New **Research** module (schema `research`): `ResearchMission` (RMS-) + `Finding` (FND-) + `Recommendation` (REC-) with manual CRUD/lifecycle, INV-005 enriched audit, `Research.Manage` RBAC, migration `Research_Init` (P15a); the `/research` register + mission detail + create/manage dialogs — **role + status gated** (Chair/Sec; mission must be `Active` for child mutations), built to `ACMP Research & Knowledge.dc.html` with design-only sections omitted (guardrail #14) (P15b). Keystone import deferred (FR-112 → D-05); traceability graph edges (FR-113/115) + convert (W16) deferred → **P15c**. Also merged **PR #106 (`186f24c`)** — governance-register refresh + Keystone-import deferral + **OQ-050** (non-topic-scoped governance artifacts are A-only) + **D-17** (owned-child audit enrichment deferred). P15b live pixel-VR pending (post-merge). Prior slice: **Audit module (literal AC-017) — MERGED to `main` (PR #105, squash `f32ca31`, 2026-07-12).** Closes the last Pending core-governance ACs **AC-017/018/019/020 → Met** (INV-005 realized end-to-end): enriched hash-versioned `AuditEvent` + before/after capture in the **same transaction** as the module write (NFR-042, ADR-0026); 74 governed emit sites → `EmitEnrichedAsync`; `GET /api/audit` register + `GET /api/audit/verify` chain check gated to {Auditor, Chairman, Secretary} — Administrator excluded on SoD-5 (ADR-0027); `/audit` UI to `ACMP Lists & Registers.dc.html` (INV-014). All four CI checks green incl. the full-stack **e2e** (first proof all 11 contexts boot on the shared `DbConnection` — validates the `PersistSecurityInfo` boot fix). Deferred to P16 (logged): DB-permission immutability + nightly Hangfire verify job. Prior slice: **P13 adversarial-audit remediation — MERGED (two GO-gated slices).** Slice 1 (PR #101, squash `3a973c4`): `MinioFileStore` real coverage via a Testcontainers-MinIO integration test (the FR-056 recording store behind AC-073/074 was falsely coverage-excluded), honest AC rows, recording-tab UI/i18n/a11y minors. Slice 2 (PR #102, squash `308611d`): the **dormant** Webex hardening — fail-closed `TokenEncryptionKey` (`ValidateOnStart`), graceful refresh degradation (AC-072 `null`-on-revoke), refresh audit (`Webex.OAuthTokenRefreshed`, INV-005), webhook replay guard, per-job `Retry-After`, and a real in-memory-Hangfire dead-letter test that restores **AC-068/069 → genuinely-tested `Met`**. Live Webex validation is owed to the operator (sandbox is operator-only). Prior merges this window: **D-15** topic *Prepare*-UI (PR #100, squash `b6bb185`; OQ-049) and **P13** Webex adapter WS0–WS3b + meeting-recording (PR #99, squash `7e3394e`; AC-067–074 `Met`). `main` is green and deployable.
- **Next:** **P19 — final audit & release readiness (the LAST slice).** P17 and P18 are done (P18b lands via PR #147); the F-03 full-loop staging sign-off is a P19 gate. P16 is complete and merged; **P14 is deferred indefinitely (DEC-028)** and is NOT next — do not start it without an explicit operator instruction. **P17** Testing hardening (first task: harvest the "→ P17" deferrals from this log — several AC rows' remaining evidence is a *live* leg, e.g. AC-025/027/028; plus **D-19** flaky `DecisionPage.test`). **P18** Deployment — and note it now carries real security weight: it is where the P16 residuals actually land (C-CRYPTO Step B server-auth + MinIO/Seq certs + TDE + backup encryption; the least-priv app login that makes ADR-0031's audit `DENY` stop being inert; **OQ-040's SQL-edition choice is a security decision — Express/Web forecloses TDE *and* backup encryption, two P1 controls**). **P19** Final audit & release readiness. P16 residuals that stay open by design: OQ-027's ZAP/DAST leg **Deferred**; C-CRYPTO-01/02/03 **Partial (Operator/P18)** (SQL transit is now encrypted in-stack, but unauthenticated, and MinIO/Seq stay plaintext); Trivy **image** scan report-only; ClamAV (C-FILE-02) operator opt-in (OQ-026); **D-22** inline-style hygiene (explicitly not a CSP matter). Formerly next, now deferred: ~~**P14** Tarseem/Diagrams~~ (+ the deferred wiki version-diff), then **P17–P19** (test-hardening, deployment, release audit). **Operator / P18 residuals:** the least-priv app login that makes the audit DB-permission `DENY` effective (D-16 / ADR-0031 — until then the audit table is protected only at the app layer); **Confidentiality ABAC** feature (D-20); Webex + ngrok token rotation; AC-070 production live-confirm (D-02); **D-19** flaky `DecisionPage.test`; FR-115 source-edge backfill for pre-P15c missions; D-15 design-update-owed.

## Delivered (by module / slice)

| Area | State | Evidence |
|---|---|---|
| Platform · Membership · Topics · Meetings | Delivered | P1–P6 slices; [acceptance audit](../validation/acceptance-audit.md) |
| Minutes & Decisions (P7) · Actions (P8) | Delivered | P7a–d, P8a–d merged |
| Voting (P9) | Delivered | P9a backend + P9b UI merged |
| Risks · Dependencies · Traceability + impact graph (P10) | Delivered | P10a–g merged; FR-095/096 |
| Governance — ADRs & Invariants (P11) + Decision→ADR promotion | Delivered | P11a–e merged; FR-068/099–109 |
| Dashboards & Reports (P12) | Delivered | P12 PR1–PR3 + audit remediation merged; AC-064/065/066 Met |
| Webex integration + meeting recording (P13) + audit remediation | Delivered — AC-067–074 Met | WS0–WS3b (ADR-0023/0024); recording upload/playback/delete (ADR-0025); audit slices #101/#102 (Testcontainers-MinIO coverage + Webex hardening, AC-068/069 tested-Met); AC-070 live-attach = production residual (env caveat) |
| Research & Knowledge (P15) + design-fidelity/i18n audit remediation | Delivered — FR-112–120; AC-060/061 Met | P15a–h merged (#107–#119); INV-014/INV-009 remediation merged (#121; ADR-0029 Accepted #122); live VR EN-light + AR-dark |
| Security hardening — audit & vote crypto (P16 Batch 1) | Delivered (in-repo) — D-13 Done; D-16 job Done, DB-perm Operator/P18 | PR #124 `87dfa40`; per-ballot chain (ADR-0030) + nightly integrity verify job + audit-schema `DENY` (ADR-0031); both ADRs **Accepted**; suite `--filter Category=Security` (339); no AC verdict change |

## Gate snapshot (Keystone package)

The package is under Keystone v1.0.0 governance. Critical gates are confirmed by `python <keystone>/scripts/validate_package.py docs` (see [execution-readiness report](../handoff/execution-readiness-report.md) for the authoritative result). Coverage: the [acceptance audit](../validation/acceptance-audit.md) tracks every `AC-001…074` → verdict (G-PROGRESS); traceability matrix links MVP FR/NFR → decision/WBS/test/AC (G-TRACE).

**AC verdicts (regenerated 2026-07-19 from the acceptance-audit rollup — per-row columns re-tallied, not asserted):** **74 ACs · 57 Met · 16 Partial · 1 Pending · 0 Not-met.** The jump from the 2026-07-16 baseline (36 Met) is the two P17b legs: the live-leg slice (PR #144) flipped 18 Partial → Met (voting/SoD-1/actions/decisions/MoM/traceability/notifications/jobs — bin (a) + 5 interpretations + 2 real-fire jobs), and the decision-issuance slice (PR #145) flipped AC-014/015/016. **This refresh (2026-07-19) corrected one straggler: AC-025's verdict column had lagged at `Partial` while its own note and the adopted OQ-055 recorded the P17b flip — set to `Met` (56 → 57); a mechanical sweep confirmed it was the only column/note mismatch.** **P18a/P18b changed no verdict** — deployment hardening closed the AC-017/018 "→ P18 least-priv" residual (least-priv `acmp_svc` runtime login binds the audit DENY; D-16 fully done) without a flip. P16 Batch 1 changed **no verdict** — it strengthened the AC-017/018/019/020 audit-immutability evidence (nightly integrity job + DB-permission `DENY`) and closed their "→ P16" residual notes (verified against the acceptance-audit rows, not asserted). The 1 Pending: AC-004 (Keycloak idle-timeout, needs a live realm session policy, OQ-003). AC-060/061 (global + Arabic search) flipped Pending → **Met** in P15f/g (5-provider `/api/search` fan-out + `/search` UI; SQL FTS + LIKE booster, Arabic word-breaker lcid 1025, OQ-034 resolved; evidence `SearchApiTests`/`SearchPage.test`/`SearchProvidersFtsTests`). The governance-feature `Partial`s that had carried a "→ P17" live-leg note flipped to `Met` in P17b (PRs #144/#145); the 16 remaining `Partial`s are earlier-phase platform/meetings/kanban/minutes ACs whose notes each state their own residual (none claims `Met`). `main`'s last CI-proven code commit is `7991f93` (P18a #146 — 9/9 CI checks incl. e2e); P18b (`feat/P18b-backup-runbook`) is scripts + docs, CI-verified on its PR; this status refresh is docs-only, skipped by CI `paths-ignore` (by design). Verdicts are the ledger's per-AC test refs, not a fresh full-suite re-run.

## Open items

- Accepted-open questions: [decisions/open-question-register.md](../decisions/open-question-register.md) (the `Deferred` rows carry applied defaults).
- Deferred work: [execution/deferred-work-register.md](../execution/deferred-work-register.md) (`D-01…D-13`; Webex/AI/Gantt/Tarseem/email/per-ballot-chaining/etc.).
- Design-update-owed items from the P11/P12 audits are logged in the progress log (reference-design divergences blessed by the operator; the design is to be updated to match).
