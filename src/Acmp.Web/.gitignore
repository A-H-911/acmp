# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright (E2E) output — reports, traces, browser cache
playwright-report
test-results
/playwright/.cache

# Local visual-review (VR) artifacts — screenshots + the ad-hoc sweep driver.
# Not part of the assertion suite; kept local so they never commit or run in CI.
vr-out/
e2e/vr-sweep.spec.ts

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
