/*
 * Home route (`/`). The real role dashboard lives in features/dashboard (P12-PR2);
 * this file is the router entry point kept stable so App.tsx's import doesn't move.
 */
export { default } from '../features/dashboard/RoleDashboard';
