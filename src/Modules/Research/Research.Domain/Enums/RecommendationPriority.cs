namespace Acmp.Modules.Research.Domain.Enums;

// A recommendation's priority band (FR-113) — how urgently the committee should act on it.
public enum RecommendationPriority
{
    Low = 1,
    Medium = 2,
    High = 3,
}
