
## Tamheed progress tracking
<!-- tamheed:note v4 -->

This project executes Tamheed package `tamheed-package` (under `C:\Users\ahammo\Repos\acmp`). **The package is the record — when code and package disagree, fix the code or record a scope change; never let them drift.** **Package data lives in the git working tree** (C31): uncommitted package writes are destroyed by `git reset --hard` / `git checkout` / `git stash` exactly like uncommitted source — commit the package `data/` before branch operations. `work_bind`, the closing `progress_update`, `export_html` and `handoff_emit` all FLUSH `data/*.jsonl` AFTER the commit they record, so the tree is dirty again the moment you finish recording: run `git status --porcelain -uall` immediately before ANY branch operation — never a memory of having committed. The `tamheed` MCP server is provided by the installed tamheed plugin (no project-level .mcp.json entry needed). All package reads/writes go through the `tamheed` MCP tools — a committed script that must QUOTE the store (a review slate, a docket) reads an `entity_export` file the tool wrote under `exports/`, never `data/*.jsonl` and never a pasted display; ready-made task prompts live in `tamheed-package/prompts/` — start with `tamheed-package/prompts/README.md`, the operator guide (which prompt for which situation, semi-auto vs fully-auto); the human review surface is `tamheed-package/review.html`.

### Recording obligations (mandatory — unrecorded work is drift)

| During execution, when… | Record BEFORE moving on |
|---|---|
| you find a defect | `entity_upsert` a `defect` row (`DEF-`, honest severity — open critical/high BLOCK readiness) — then fix it |
| you find needed work that is out of scope | `entity_upsert` a `deferred-work` row (`DW-`) with an activation trigger |
| you deviate from the approved plan in any way | a `scope-change` row (`SC-`) FIRST, `decision_ref` naming the deciding `DEC-`/`ADR-`, delta edges (`scope_adds`/`scope_modifies`/`scope_removes` for plan rows; `amends` for a ruling — DEC-: full-row upsert, ADR-: supersede) naming the affected rows — after approval, apply the row changes, RE-READ them, and only then set the `SC-` to Merged |
| you hit genuine ambiguity | an `open-question` row (`OQ-`, with owner + due_by) and `[NEEDS-CLARIFICATION: OQ-NNN]` at the exact spot — NEVER assume |
| execution teaches you something durable (a mistake's fix, a practice worth repeating) | `entity_upsert` a `lesson` row (`LL-`, born Proposed; kind improve\|sustain, statement + impacts) + a `learned_from` edge to the source — the OPERATOR confirms later; only Approved lessons bind |
| you finish a unit of work | `progress_update(...)` — event_type `work-done`, `subject_id`, your `actor` string, phase/slice ids |
| you believe a slice/wbs-item is complete | set its `lifecycle_status` to **Review** (done-claimed) — `Implemented` means VERIFIED and is readiness-guarded |
| you verify an acceptance criterion | `audit_record(...)` with evidence + `verified_by` + `verification_method` + `against_commit` — never Met without proof |
| you create a commit or PR | `work_bind(ref, entity_ids=[...])` |
| you declare a slice/phase/release done | `readiness_check(scope)` first — resolve every blocking failure, or ask the OPERATOR for a `WVR-` waiver (their words; you never author your own) — `"force": true` only on the operator's explicit words |

If you cannot record (lock held, package missing), STOP and tell the operator — do not proceed unrecorded.

### Lessons (operator-confirmed — these bind every session)

- **LL-074** [improve, pinned] THE INSTANCE. `NFR-006` - *meeting-notes auto-save round trip <= 2 000 ms **from the debounce trigger** to server ACK* - was one of two requirements left unmeasured, and a whole...
- **LL-073** [improve, pinned] THE INSTANCE. `DEF-153`'s fix, 2026-09-09. A new test needed to advance a 2-second debounce, so it called `vi.useFakeTimers()` with the restore in a `finally`. `userEvent` and f...
- **LL-072** [improve, pinned] THE INSTANCE. `NFR-016` — recovery within 2 minutes of a process failure — measured on uat, 2026-09-09. Three instruments, and the first two were wrong in OPPOSITE directions. *...
- **LL-071** [improve, pinned] THE INSTANCE. `scripts/gen-dw-disposition-slate.mjs` exists because `LL-011`: an identifier is a pointer, not a reference, and the operator once refused an interview built on id...
- **LL-070** [improve, pinned] THE INSTANCE. `DEF-144`, found 2026-09-07. `PE-873` asserted that MediatR 12.5.0 makes a bare `next()` forward `CancellationToken.None`; `PE-875` measured and RETRACTED it withi...
- **LL-069** [improve, pinned] THE INSTANCE. `DEF-143`, 2026-09-07. `deploy/aws/04-ecr.sh` gave every ECR repository a `keep last 10` lifecycle rule (`imageCountMoreThan 10 -> expire`, `tagStatus any`); CI's ...
- **LL-068** [improve, pinned] THE INSTANCE. On 2026-09-07 two package-only commits (`0343b3d3`, `072faae8`, `PE-960`..`PE-963`) sat on local `main` awaiting the operator's push word (`DEC-127` d1). The harne...
- **LL-067** [improve, pinned] THE INSTANCE. Preparing the `DEF-121`/`DEF-130` interview, the sweep's within-project control was *the stock 2022 backstop container in the same job has never crashed* - true, a...
- **LL-063** [improve, pinned] **PART ONE - THE HALF `LL-045` GOT RIGHT, RESTATED WITHOUT THE FALSE FRAMING IT WAS BUILT ON.** A FIELD IS SAFE TO RE-TRANSMIT ONLY IF IT CAME FROM A READ WHOSE PURPOSE WAS TRAN...
- **LL-061** [improve, pinned] C31 says package data lives in the working tree, so uncommitted package writes are destroyed by `git reset --hard` / `git checkout` / `git stash` exactly like uncommitted source...
- **LL-057** [improve, pinned] `DW-096`/`WBS-27.2` had to stop `Acmp.Api.Tests` building ~294 `WebApplicationFactory` hosts. Everything written about it for two weeks treated ONE risk as inherent to the work:...
- **LL-056** [improve, pinned] TWO INDEPENDENT INSTANCES, ON TWO DIFFERENT DOCUMENTS, WITHIN HOURS OF EACH OTHER. **(1) `.claude/PERMISSIONS.md` and `permission-prompts-four-causes.md`** both explained permis...
- **LL-052** [improve, pinned] `DEF-121`'s crash artefact was declared insufficient twice, on two different files, by two different reading failures. `PE-784` described `info.log` as *"a binary-framed blob wh...
- **LL-040** [improve, pinned] WHEN YOU SWEEP FOR *WHAT WAS DECIDED*, YOU SWEEP DECISIONS, ADRs AND OPEN QUESTIONS - AND A REAL DECISION MAY BE SITTING IN A `Fixed` DEFECT ROW, WHICH READS AS A CLOSED PROBLEM...
- **LL-038** [improve, pinned] BEFORE 'REPAIRING' A ROW WHOSE STATUS LOOKS BEHIND ITS EVIDENCE, ASK WHETHER THE STATUS IS DOING WORK. A store constraint keyed on status - here, immutability that fires on `App...
- **LL-036** [improve, pinned] AFTER A SQUASH-MERGE, THE TREE THAT LANDS ON THE TARGET BRANCH GETS ITS OWN CI RUN, AND IT CAN DISAGREE WITH THE RUN THAT GATED THE PR EVEN THOUGH `git diff` BETWEEN THE TWO IS ...
- **LL-034** [improve, pinned] WHEN YOU SIZE WORK BY READING A CLASS, YOU ARE READING ITS OWN DECLARATION - AND WHATEVER ITS BASE CLASS ALREADY PROVIDES IS INVISIBLE IN EXACTLY THE PLACE YOU ARE LOOKING. The ...
- **LL-032** [improve, pinned] A TEST WHOSE FIXTURE IS THE LIVE REGISTER IS A TEST WHOSE MEANING CHANGES WHEN SOMEBODY DOES ORDINARY WORK, AND THE DANGEROUS OUTCOME IS NOT THE FAILURE - IT IS THE PASS. If a t...
- **LL-031** [improve, pinned] A REFACTOR CAN PUSH A FILE UNDER A PER-FILE COVERAGE FLOOR WITHOUT INTRODUCING A SINGLE NEW UNTESTED LINE, BECAUSE THE FLOOR IS A RATIO AND A REFACTOR MOVES THE DENOMINATOR. The...
- **LL-028** [improve, pinned] `LL-011`'s remedy works and is mechanical: a generator prints every cited record from the canonical store and a verifier confirms byte-identity before publishing. THE GUARANTEE ...
- **LL-026** [improve, pinned] When a test invokes a framework lifecycle method and asserts immediately afterwards, it is asserting against work the framework has not promised to have finished. If the asserti...
- **LL-025** [improve, pinned] A work row that quotes its requirement accurately can still be wrong, because the REQUIREMENT is what a decision superseded. Nothing mechanical can see it: the ids resolve, the ...
- **LL-024** [improve, pinned] When a script generates source for another file - a Python heredoc writing TypeScript, a shell heredoc writing a Dockerfile, any generator emitting code - every escape sequence ...
- **LL-022** [improve, pinned] A whole class of work here does not CREATE a failure, it IMPROVES one that already happens: a timeout that already fires but says nothing useful, an exception that already throw...
- **LL-021** [improve, pinned] In `A && B && C`, a failure in A stops the chain - but only for the commands that RUN. Any later command that would have consumed A's output can still succeed on a stale copy of...
- **LL-019** [improve, pinned] Where a Dockerfile pins a base image AND separately configures a package repository whose URL encodes the same platform - a distribution release, a product major, an architectur...
- **LL-016** [improve, pinned] A sequence in prose is invisible to every mechanical check - an id-and-status pass sees ids and statuses, never an ordinal. And a phrase can go stale without its number changing...
- **LL-014** [improve, pinned] THE INSTANCE. WBS-23.3's spike had a dependency evaluation already done and carried into the session as settled: four DOM-to-image packages ranked on registry metadata - unpacke...
- **LL-011** [improve, pinned] THE INSTANCE. Asked to disposition 53 open deferred-work rows, I built a slate that named roughly forty package entities by identifier alone - every DW row by number, plus DEC-0...
- **LL-010** [improve, pinned] NFR-039 has two clauses: the EN-AR terminology glossary shall be the SINGLE SOURCE of canonical bilingual terms, and no term shall have more than one AR translation in the appli...
- **LL-008** [improve, pinned] LL-005 and its trap tell you to sweep the adr, decision and open-question registers before disposing of a capability or putting an option in front of the operator. Batch 14 did ...
- **LL-005** [improve, pinned] Before asking the operator to dispose of a capability — wire it, drop it, record it as intentional — search the requirement register for a row that already covers it. An option ...
- **LL-004** [improve, pinned] When cross-checking git against a Tamheed package, a commit whose entire content IS the package write can never contain a reference to its own sha: the MCP write happens FIRST, ...
- **LL-003** [improve, pinned] When something needs the OPERATOR — a decision that is theirs to make, or an action only they can perform — START THE INTERVIEW IMMEDIATELY. Do not report the blocker and wait t...
- **LL-002** [improve, pinned] When a decision belongs to the operator, run the interview EVERY time. Do not bank their earlier answers as a starting position, a default, or a shortcut for a later ceremony. A...
- **LL-001** [improve, pinned] When a repair payload has been generated from canonical source data, PASTE it into the tool call — never re-type or re-transcribe it. The hand is the untrusted transport: re-typ...
- **LL-075** [improve] THE INSTANCE. 2026-09-09. A measurement needed a command the auto-mode classifier had refused. The agent described the blocker in terms of permissions; the operator added `Bash(...
- **LL-066** [improve] THE INSTANCE THAT EXPOSED THE BLIND SPOT WAS COMMITTED IN THE ROUND THAT APPROVED `LL-064`. `prm-next.md` item 1 read *"BOTH STANDING DIRECTIONS FROM `PE-909` ARE DISCHARGED, AN...
- **LL-065** [improve] `DEC-138` d1 was an OVERRIDE on the question where the agent had the strongest evidence. The agent recommended rebuilding four generators to spawn the MCP server and speak stdio...
- **LL-058** [improve] `DEF-136` ran for three sessions across two agents and one operator. The symptom: allowlisted `Bash` commands prompted, and `permissions.defaultMode: "bypassPermissions"` did no...
- **LL-051** [improve] `LL-011` and `gen-record-slate.mjs` exist so that every record put in front of the operator is byte-identical to the store, and the generator's own header already states the lim...
- **LL-050** [improve] `LL-038` says: before repairing a row whose STATUS looks behind its evidence, ask whether the status is doing work. THE SAME TRAP IS SET ON A NUMBER, AND `LL-038`'s WORDING DOES...
- **LL-044** [improve] `LL-032` records that a test whose fixture is the LIVE REGISTER changes meaning when somebody does ordinary work, and that the dangerous outcome is the PASS. A FIXED DATE IS THE...
- **LL-042** [improve] A `scope-change` row carries `scope_adds`/`scope_modifies`/`scope_removes` edges naming the rows it changes, and the obligation is to apply those changes and THEN set the `SC-` ...
- **LL-039** [improve] `skipped` IS NOT A REASON, IT IS AN OUTCOME WITH AT LEAST TWO CAUSES, AND THE ONE YOU CAN SEE IN THE WORKFLOW FILE IS THE ONE YOU WILL PICK. A job with both an `if:` condition a...
- **LL-037** [improve] WHEN YOU ARGUE THAT RE-RUNNING A SUITE CAN TEACH YOU NOTHING BECAUSE YOU ALREADY HOLD THE ANSWER TO THE QUESTION IT WAS RUN FOR, YOU ARE TREATING A WHOLE TEST SUITE AS A ONE-BIT...

Skills distilled from lessons: `trusting-a-measurement` [project] — auto-loaded where present (project: .claude/skills/; user: ~/.claude/skills/).

### Tool cheat-sheet (execution loop)

- `progress_update(entries=[{entry, event_type?, subject_id?, actor?, corrects?, phase_id?, slice_id?}])` — append TYPED progress (correct via a `correction` event, never edit)
- `audit_record(verdicts=[{ac_id, verdict: Met|Partial|Not-met|Pending, evidence?, verified_by?, verification_method?, against_commit?}])` — evidence ref = evidenced, not narrated
- `work_bind(ref, entity_ids=[...], note?)` — stamp a commit/PR onto entities
- `entity_query(type, id?, status?, columns?, limit?, after_id?, ids?, search?)` — rows + total + next_after (page with after_id; quote a known set via ids; keyword-sweep via search)
- `trace_query(entity_id, direction: out|in|both, relation?)` — typed links
- `entity_upsert(entities=[{type, id, ...}])` — FULL rows, even for updates; `expect_unchanged: [cols]` on an item refuses the write if those columns differ from the stored row (a long-row status flip is self-verifying); `{type: trace-edge, from_id, to_id, relation, retire: true}` removes that edge (journaled — retype in ONE batch: retire + the corrected edge)
- `entity_export(path, tool?, args?)` — write a read tool's WHOLE result to `<package>/exports/<path>` (digest-stamped, deterministic) for a committed script to quote from; pass a limit above total for a whole family
- `gate_run()` — mechanical gate verdict · `readiness_check(scope, id?)` — is it actually DONE (waivers honored, Review counts open)
- `export_html()` — refresh review.html · `server_info()` — version + root
- `package_verify(name?, record?)` — canonical round-trip of the on-disk store (per-file byte-equality, foreign files, digest); `record=true` journals it
<!-- /tamheed:note -->
